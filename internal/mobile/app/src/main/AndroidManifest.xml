<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 需求⑪：公告系统通知（Android 13+ 运行时权限，MainActivity 申请） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 移动版：触屏设备。v1.19（总体#2）竖屏自适应：解锁方向（横竖屏均可）；
         布局按 rememberUiScale 竖屏分支 + rememberRowColumns 列数自适应 -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="true" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.OptimalTV">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|density|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="fullUser">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- v1.20：内嵌 python 后端宿主（:backend 独立进程；热更新免杀 app 重启后端） -->
        <service
            android:name=".BackendService"
            android:exported="false"
            android:process=":backend" />
    </application>
</manifest>
