# My TV · 项目文档（详细版）

> **本文档是全项目唯一的详细文档**（2026-09-06 由 10 份专题文档合并而成，原件归档于
> `docs/_archive-20260906-文档合并/`）。项目结构介绍与快速上手见根目录 `README.md`；
> 逐版本变更流水见 `docs/版本史.md`，验收记录流水见 `docs/测试记录.md`（两者为追加式日志，非文档）。

## 1. 产品概览

**My TV（OptimalTV 工程）**：Android 原生影视应用（TV 大屏 + 手机/平板双端）+ 云端网页版，
Kotlin + Jetpack Compose + Media3，tvOS 深色风格。内容聚合自 hhkan 接口，**后端打包进 app**
（Chaquopy 内嵌 Python，设备端自给自足，无需自建服务器）；云端仅承担卡密激活、热更、电视源、
后台管理与网页版五类函数。商业化 = 卡密付费（公测版门控）。

### 产品矩阵（v1.19 起单 variant，2 APK + 1 网页）

| 产物 | 工程/位置 | 包名 | 状态 |
|---|---|---|---|
| TV 版 | `android/` | `com.qiubo.optimaltv.public` | v1.23（versionCode 25），全量功能 + 卡密门控，投屏已移除 |
| 移动版 | `internal/mobile/` | `com.qiubo.optimaltv.mobile.public` | v1.23，共享层同源，触屏 UI，含 DLNA 投屏 |
| 网页版 | `cloudfunctions/otv-web/` | —（HTTP 云函数） | v1.25（09-08 后端引擎随热更 v10 同源升级） |

### 访问入口

| 端 | 入口 |
|---|---|
| 网页版 | https://appletv-d5ge1bth794873f76.service.tcloudbase.com/web/ （首次有平台「风险提醒」中间页，点确定即过） |
| 后台管理 | https://appletv-d5ge1bth794873f76.service.tcloudbase.com/admin/ （账号 `admin`，密码 = otv-admin 函数环境变量 `OTV_ADMIN_PASS`；2026-09-05 起本地形态退役，仅云端） |

### 核心能力一览

影视点播（首页/全部/详情选集/搜索/收藏/历史）· 卫视直播（精编频道表 + 多源备份无感切换）·
IPTV 电视（400+ 频道分组选台 + 足球直播广场）· 播放三级回退（Media3 → 中继 HLS → libVLC）·
DLNA 投屏（双端）· 卡密付费（一码 2 台设备，离线可用）· 热更新（改后端不发 APK）·
后台管理（卡密/热更/公告/电视源动态管理）· 网页版（云端影视 + IPTV）· OtvNav 遥控器焦点引擎。

## 2. 总体架构

```
[TV 版 / 移动版 APK]                      [网页版浏览器]
  Chaquopy 内嵌 Python 后端 :8090    ┌─→  otv-web 云函数 /web（同一份 proxy.py）
  HotUpdateManager ← 热更包          │
        │                            │
        ▼                            ▼
  [CloudBase 环境 appletv-d5ge1bth794873f76（上海，PG + pgstore + HTTP 云函数）]
    activate /hotupdate /iptv-rebuild /otv-admin /otv-web + PG(RPC+RLS) + 公开桶 hotupdate
```

- **内嵌后端**：`proxy.py / hhkan.py / hhkan_snapshot.py + hhkan_snapshot.json / scraper.py / team_backdrop.py / decrypt_stream.js + www/`
  打包为 `assets/backend/python-backend.zip`，app 启动解压运行于 `127.0.0.1:8090`。
  **这也是热更对象**——改抓取逻辑/换图源/改 UI 资源无需发新 APK。
- **足球云端兜底**（2026-09-10）：家宽出口被 yoozb 系拉黑（403）时，设备端
  `/api/matches`（冷启动+SWR 两路径）与 `/api/stream` 解析（找不到播放器/异常两出口）
  自动回退云端 otv-web 同代码接口；云端 bootstrap 以 `OTV_CLOUD_RELAY=0` 防自环，
  兜底响应 `source` 带 `|cloud-relay` 标记。环境变量 `OTV_CLOUD_BASE` 可覆盖兜底地址。
  网页搜索同理受益于扩充后的 hhkan 快照（952 卡/698 详情：`by=3` 抓 6 页 + 详情上限 700）。
- **:backend 独立进程**（v1.20）：`BackendService`（`android:process=":backend"`）承载 Chaquopy；
  热更生效 = service 内自杀重建（app 主进程全程在线）；后端状态经 `filesDir/hu_state.json`
  文件通道回传（DataStore 非多进程安全）。
- **兼容性基线**：minSdk 30（坚果 N1 Air / Android 11），零 GMS；ABI arm64/v7a + x86_64。
- **云环境**：体验版（2027-03-01 到期，注意续期），PG 实例 pgdb-fqfjadel，免费档 3000 资源点。

## 3. 端侧功能模块

### 3.1 影视点播与搜索

- 数据源 hhkan；搜索支持拼音输入法（AOSP 谷歌拼音解码器 `cpp/pinyinime`）与首字母联想
  （构建期两张 asset 表：汉字首字母 + 全库片名快照，`tools/gen_pinyin_initials.py` / `gen_pinyin_titles.py`）。
- **hhkan 云端容灾（2026-09-09）**：好好看仍是唯一目录/卡片/元数据源。本地和 Android 优先实时抓取；
  腾讯云数据中心出口被源站风控时读取构建期 `hhkan_snapshot.json`（当前 626 卡、259 完整详情），固定三栏目、
  搜索、筛选、分页与详情均不切换目录来源。5 分钟熔断器保证同实例只做一次失败探测，到期半开恢复；快照不访问 PG。
- **macCMS 播放补充池（2026-09-09 需求⑮）**：暴风、极速、金鹰、虎牙、豪华、红牛、速播、360 共 8 个
  T1 源，仅在好好看详情之后按同名影片追加播放线路；不得生成卡片、替换目录或改变分类。站级并行测速排序
  （5min）+ 故障自动换站；线路级清晰度排序（实读 RESOLUTION，死线沉底）；pid≥100 使用站点命名空间，
  ≥720p 线路优先供播放器自动选择。补线总开关 env `OTV_ENRICH=0` 可关闭。
  **未能接入的 3 个候选源**（2026-09-08 探测结论）：nnyy.in（Cloudflare TLS 指纹墙，Python 出站必被重置）、
  NO视频 novipnoad.uk（player iframe pkey 服务端校验需真实浏览器会话，curl/无浏览器必「页面已过期」；
  播放器 URL 形态 `https://player.novipnoad.uk/v1/?url={vid}&pkey=` 已逆出，混淆 play.js 为 base91+字串数组）、
  SA视频 lsjys11.com（Nuxt SPA + API 响应体加密 + requestid/time 设备指纹头，945KB 混淆 crypto 分块，
  破解与维护成本过高；同内容 MacCMS 池已覆盖）。
- **片库板块 LazyColumn 的 item key 必须用板块名**（`lib-sec-{sec.name}-r{ri}`，2026-09-07 需求①
  跳行事故教训）：三块到货顺序与 blockOrder 展示顺序不一致（最近热门最慢却排第 0），序号 key
  会让迟到板块的插入触发全部行 item 重建 → 聚焦节点销毁、焦点乱甩（TV 端「光标跳行」的根因）。
  TV 与移动版同款布局同规则。

### 3.2 卫视直播与 IPTV

- 卫视直播：精编频道表内置（assets `iptv_curated.m3u`），台标本地图标（60 真实台标，
  `tools/build_logo_assets.py` 生成，序号=已落盘最大+1 防串台；暗标白化 normalize_logo），
  自动补源、多源备份无感切换。
- IPTV 电视：SOURCES = 云端桶 latest（首位）+ Guovin（gh-proxy/jsdelivr）+ vbskycn；快照落
  `files/iptv_snapshot.m3u` 先读秒上屏；刷新节流 ≤30min。
- 测速换台（v1.19）：`rankUrls()` TTFB 测速排序起播即最快线；BUFFERING>3.5s 且备线分值
  <0.55×当前 → 无感切换；侧边栏 60s 周期探测组内前 40 线路。
- 播放健壮性：首帧看门狗（READY 15s 无首帧→切 VLC）；fatalMsg 12s 未恢复自动跳台（环形，
  连续 3 次上限）；ac3/eac3/dts 音轨自动转 VLC。
- **SSRF 双层校验** `data/source/UrlGuard.kt`：仅 http/https、拒私网/环回/IPv4-mapped，
  `assertPublicResolved()` DNS 全 A 记录私网则拒；列表拉取侧 + 起播侧两层（云端版本同款内联实现）。

### 3.3 播放引擎

- 三级回退：Media3 硬解 → 中继 HLS → libVLC；点播直播共用会话代数管理（engineEpoch）。
- 直播 HLS 经本机中继（签名 ~9 分钟轮换 + 90s 续签热切换）；解密中继 `StreamDecryptServer`
  （2026-09-07 起 **3-WebView 解密池**：信号量+槽位并发，串行→3.2× 吞吐，替代单实例全局锁）。
- 足球解密三级分发：端内 QuickJS HTTP bridge（:8091）/ 本地 node / 云函数 `decrypt_python.py`。
- **解密池自愈（2026-09-08 需求⑬）**：StreamDecryptServer 的 WebView 加 `WebViewClient.onRenderProcessGone`
  → 销毁清槽懒重建（返回 true）；12s 零回调僵死检测兜底（渲染进程活着但页面级僵死的形态）；
  poll/loadData 全面 runCatching。修复前形态：反复加载混淆页数小时后渲染进程崩溃 → 僵死 WebView 永久
  留池 → 每次解密 12s 超时 → 三槽全僵 = 「半小时后所有信号源失效」。MuMu 实测 kill -9 渲染进程后
  下一次解密 0.5s 恢复。
- **足球直播信号源行为（2026-09-07 需求②③）**：① 起播快路径只解析首选源（1 次频道页+播放器页
  +解密）→ 立即起播；其余源 `streamUrlsIncremental` **到货即上屏**（不等最慢源，旧行为是
  awaitAll 全源到齐才渲染选择条）；② 首选源 = 上次手动选择的信号源（`liveLastSrc` 持久化；
  网页版 localStorage 同款），记忆源不在本场动态线路表时回退 bb；手动切源即保存，自动故障
  换线不污染记忆。

### 3.4 DLNA 投屏（v1.21，仅移动版；TV 版 2026-09-08 需求⑭整体移除）

SSDP 发现 + SOAP 控制（零三方依赖）；本机中继流自动改写局域网地址（后端绑 0.0.0.0）；
点播/直播/IPTV 全可投；换线换集自动重投、断开回本地续播。
**TV 版 v1.23 起投屏功能全部移除**（PlayerScreen 投屏胶囊/状态条/更多面板投屏节/CastDevicePanel/CastGlyph；
更早 v1.25 已删 TvScreen 投屏按钮）。共享层 PlayerViewModel/DlnaCast 保留（移动版在用；TV 侧无入口触达）。

### 3.5 卡密付费体系

- **码制**：`OTV-XXXXX-XXXXX`（10 有效字符，32 键字母表剔除 0/O/1/I）。套餐：monthly 30 天
  ¥9.9 / quarterly 90 天 ¥29.9 / yearly 365 天 ¥99.9 / lifetime 终身 ¥128（+weekly 周卡 7 天，
  非卖品；周卡改动需四层同步：LicenseManager.PLAN_DAYS、license_gen.py、后台 UI、
  **PG licenses_plan_check 约束**，漏改则入库报 DATABASE_23514）。
- **安全模型**：ECDSA P-256 + SHA256；**私钥只在本机**（`tools/keys/private_key.pem`），
  云端只查表/计数/原样返回票据——云端被黑也伪造不了授权。票据 `{v,code,plan,days,issued,nonce,sig}`，
  canonical 串 `v|code|plan|days|issued|nonce` 本地验签。
- **到期口径**：激活时刻 + days（非签发时刻）；叠加 `newExpiry = max(当前到期, now) + days`。
- **设备绑定**：`deviceId = ANDROID_ID + Build.MODEL` 哈希，maxDev=2；同码同设备幂等不占名额。
- **离线与防回拨**：激活后离线永久可用；联网启动静默复验（banned→降级）；本地最大时间戳账本
  `now < lastSeen−48h` 冻结计时；可信时间用 activate 响应 HTTP Date 头校准。
- **门控**：全量门控（未激活禁看一切，路由层 `LicensePlayerGate` + 播放页二重校验）；
  DataStore `license.preferences_pb` 持久化。
- 收款发货外包爱发电（主）/面包多（备）。

### 3.6 热更新（公测版专用，v1.18 起）

- **对象 = 内嵌后端 zip**（`build_backend_zip.py` 产物，~35MB）。Kotlin 层/播放引擎/权限等
  APK 级改动仍需发版（zip 结构校验也挡住越界——五个后端模块 + `www/` 前缀缺一不可）。
- 客户端 `HotUpdateManager`：启动静默 check → 下载（流式 SHA-256）→ 双校验 → 暂存 →
  下次冷启动生效（v1.20 起确认更新后 :backend 进程重建即时生效，非强更弹窗 UpdateOffer 可选）。
- **坏包三层防护**：zip 解压失败→隔离回退 assets；`import proxy` 同步执行 SyntaxError 当场抛→
  同次启动重解压 assets；健康探测 45s 不过→隔离。隔离设备停在 assets，**修复必须升版本号重发**。
- check 与下载分离：check 12s 上限即放行进 app；非强更绝不影响进入/使用。
- 版本规则：`hot_version_code` 整数单调递增，**后台自动分配（最大+1）**；适用 APK versionCode
  范围同样系统自动分配（0–MAX 全量），表单不再展示/手填（2026-09-06 起）。
- 发布方式三选：立即发布 / 定时发布（惰性求值：`status='scheduled' and publish_at<=now()` 视同
  已发布，无定时器）/ 仅上传（下线态）。
- **运维铁律**：E2E/验证包验收完必须立即下线（v1.18 事故：强更包误命中 v1.19 新装致「卡启动屏」）；
  发布强更包必须确认 apk 范围；坏包修复升版本号。
- 全局紧急停更：hotupdate 函数 env `OTV_HU_OFF=1`（check 一律无更新，不改代码）。

### 3.7 焦点导航 OtvNav

集中式遥控器焦点引擎（Activity 单点分发、层容差、精确滚动找回），`ui/components/Focus.kt`；
searchGeom 只搜同 navGroup，跨区须 navOverride 定向；台标白剪影禁白底聚焦；TV 搜索标签带
top48/h68。触屏端重写为手势交互（移动版无 Focus.kt/PinyinIme）。

## 4. 双工程共享层（TV / 移动）

- **共享层字节级一致**：`tools/sync_shared.py` 监管（清单 `SHARED_PATHS`：`data/`、`playback/`、
  `App.kt`、`EmbeddedBackend.kt`、`python/`、`assets/backend/`、`PlayerViewModel.kt`、
  `DetailViewModel.kt`、`license/LicenseManager.kt`、`ui/paywall/PaywallShared.kt`）。
  **共享层改动后必跑，必须全绿**；清单外双端文件手工同步；品牌资产两线分治不共享。
- 端口 8090 共存自愈：双端同装时绑定失败每 5s 重试、对端死后立即接管。
- 移动版触屏要点：竖屏自适应（`screenWidthDp/750f`）+ `rememberRowColumns()` 自适应列数；
  换台浮动按钮为侧边栏唯一入口；播放器双击 ±15s/横滑 seek/竖滑亮度音量；一次返回退出
  （BACK → `finishAffinity()`）；Media3 PlayerView 须 `useController=false; isClickable=false`，
  手势层为视频层之上、控件浮层之下的兄弟节点。
- **构建目录分治**：`zcode-build/optimal-tv` / `zcode-build/optimal-tv-mobile`，**不要并行构建**
  （历史上共用目录曾互相清空中间产物）。

> **修改范围约定（2026-09-05 起）**：除非明确说只改某端，**任何功能改动默认同步所有版本**——
> TV + 移动（共享层跑 `sync_shared.py`）+ 网页版（`tools/backend-src/www/` 与
> `cloudfunctions/otv-web/www/` 双份，改后端 `proxy.py` 需 `build_backend_zip.py` +
> `build_web_function.py` 双构建重部署）。

## 5. 网页版（v1.25）

- **同一份 `proxy.py`** 既是 Chaquopy 内嵌后端也是云端 otv-web 函数运行时；网关 `/web`
  （Path passthrough: Disable，剥离前缀）；前端 `BASE="/web"` 从 location.pathname 推导。
- 前端与 APK 逐行对照复刻：Compose 设计 px → CSS（`fit()` 按视口宽设 `body.style.zoom`，
  横屏基准 1920 / 竖屏 750）；Kotlin `Color(0xAARRGGBB)` 换算 rgba 时注意 ARGB≠RGBA；
  hls.js@1.5.13（iOS 原生 HLS 兜底）+ PWA manifest。
- `/api/iptv` 服务端聚合（latest 优先→Guovin/vbskycn 殿后，同名聚线 8 线上限，10 分钟缓存）；
  播放走 `/api/relay` 同源中继（redirect=False + 手动 ≤3 跳防死循环）。
- **会员系统（v1.25）**：与 App 同契约——同源 `POST /api/activate`（proxy.py 服务端转发云端
  activate，规避网关 CORS 头合并问题）→ WebCrypto P-256 验签（DER→P1363）→ 续费叠加/封禁降级；
  「我的」页会员卡 + 播放统一门控付费墙；票据存 localStorage（otvw:ticket）。
- **加载提速（v1.25）**：SWR 持久缓存全链（目录/首页/板块/筛选/片库首屏/详情，localStorage 或
  sessionStorage）+ 点卡即上屏标题封面；静态资源 `?v=` 版本号 + immutable 长缓存（HTML no-store
  发布即生效）；hhkan.get_filters 空结果不缓存（冷启动缓存毒 bug 修复）。
- **竖屏（v1.25）**：片库 3 列 + 筛选堆叠、播放器 16:9 顶+常驻控制区+全屏（原生控件）、电视播放器
  16:9+内联频道列表、左缘右滑返回（uiBack 分层）、推送页让位返回钮、搜索让位 tab 胶囊。
- **hero 切换动画（v1.25）**：heroSwap 双层叠化；不依赖 rAF/transition 时钟（挂起环境 transition:
  none + 强制回流兜底，永不黑屏）。
- 网页独有边界（有意保持）：无卡密/会员、无 DLNA、无热更/启动门闸（授权需本地私钥验签）。
- 已知边界：云端片库与 App 不完全一致（hhkan 2026-09-08 起全量 701 reCAPTCHA → 云端与设备新会话
  实际都走 MacCMS 多源池，片库由 bfzy+fdzys 供给；分类树少「短剧」、无地区/年份筛选→前端隐藏）；
  默认域名首访有平台「风险提醒」中间页（**实测同浏览器点确定后永不再现**；唯一彻底移除方案 = 绑定
  自定义域名——需用户自有域名+备案，决策留给用户）；网关路由传播 30s~3 分钟偶发 404 正常。

## 6. 后台管理系统（仅云端）

- **形态**：otv-admin HTTP Web 函数 + 网关 `/admin/` + 登录门禁。`tools/admin.html` 是云端 UI
  源码（单文件，无构建，`__OTV_TOKEN__` 占位符 + `X-OTV-Admin` 头）；`tools/admin_server.py`
  是云端函数运行时（**不能删**，scf_main.py import 它）；`build_admin_function.py` 打包部署。
- **登录**：账号 `admin`；云端密码 = 函数 env `OTV_ADMIN_PASS`；会话 = HMAC 签名 HttpOnly Cookie
  12h（多实例共用 env `OTV_ADMIN_SECRET`）；失败 5 次/60s 锁定；重部署/改 env 后旧 Cookie 作废。
- **七个标签页**：总览 / 卡密 / 激活日志 / 补货 / 热更新 / 公告 / 电视源。
- **热更新页**（2026-09-06 起）：「上传最新版本」按钮一键直传本地最新 zip（File System Access
  API 读本地目录，首次点击选 build_backend_zip.py 输出目录，目录句柄存 IndexedDB；需 Chrome/Edge
  桌面版）；或手动「上传所选文件」。版本号与适用 APK 范围系统自动分配，表单不展示。
  包列表：上线⇄下线、定时包「立即发布/取消定时」、删除（元数据+桶内对象一并）。
- **热更上传分片链路（2026-09-07 需求④）**：CloudBase HTTP 网关请求体上限实测 ~6MB（超过返回
  406 + 空响应体，前端曾报 `SyntaxError: Unexpected end of JSON input`），36MB 级 zip 必须分片：
  前端 >4.5MB 自动按 4MB/片 `POST /api/hu_upload_chunk?uid&seq`（暂存 pgstore
  `hu-uploads/{uid}/part-{seq}`，云函数多实例无粘性故状态外置；每片 3 次重试）→
  `POST /api/hu_upload_commit`（按序读回重组、尺寸精确校验、复用 `_hu_publish` 校验发布）→
  尽力清理暂存分片。≤5MB 小包仍走直传 `/api/hu_upload`（超限服务端友好拒绝）。
- **补货**：云端补货按钮需函数 env `OTV_PRIV_KEY`（未设置则按设计禁用——私钥签发默认走本机
  `license_gen.py` + `db_import.py` 入库）。
- **电视源四步工作流**：**一切改动先入库草稿，点「发布并生效」才对客户端生效**（客户端 ≤30min
  自动拉取）。①导入（M3U 文件≤32MB/粘贴/上游 URL 仅 https 公网 ≤3 跳；合并=同名补线（推荐）/
  替换=清空重导（慎用）；API 同步拉 Guovin/vbskycn 每日校验源，16MB 级数十秒勿重复点）
  →②编排（频道/线路/台标 CRUD+批量启停+排序；台标上传 ≤2MB PNG/JPG/WEBP 存
  `hotupdate/iptv/logos/`；客户端内置台标优先）→③发布（启用频道→m3u→`iptv/iptv-latest.m3u`
  +版本归档）→④回滚（复制归档对象回 latest 并**生成新版本号**，非拨指针）。
- **托管/自动双模式**：发布/回滚自动切托管（manual）并写 version.json，每日 06:30
  `iptv-rebuild` 云函数见 manual 跳过；「切自动模式」恢复每日推流（覆盖手动列表）。
- **设计系统（中性专业暗色，2026-09-05 定稿）**：石墨底 `#0f1216/#151920/#1b2029` + 蓝单强调
  `#4f8cff`（strong `#2f6be0` 专用于蓝底白字）+ hairline 分隔 + 等宽数字（Consolas +
  tabular-nums）；正文 Segoe UI/微软雅黑 13px；状态词汇封闭清单（`● 正常/● 已发布/○ 下线/
  ◌ 定时/✕ 封禁`…不得新造）；半径 6px 无阴影（浮层唯一投影）；动效预算四处 ≤280ms +
  `prefers-reduced-motion` 全禁；中文短句动词开头；禁 emoji/主题化拟物/白底/第二强调色/
  toast（底栏常驻条是唯一回执）。反模板清单与完整规范见归档 DESIGN.md。

## 7. 云端基础设施

| 云函数 | 类型 | 路由 | 职责 |
|---|---|---|---|
| `activate` | Nodejs18.15 HTTP 10s | `/activate` | 卡密激活（svc_activate signin → activate_code RPC） |
| `hotupdate` | Nodejs18.15 HTTP 10s | `/hotupdate` | check/report + GET /announce 公告下发；env `OTV_HU_OFF` 紧急停更 |
| `iptv-rebuild` | Python3.9 Event + Timer | — | 每日 06:30（cron 七段 `0 30 6 * * * *`）拉上游→测活剔死→测速 ≤8 线/频道→写桶 |
| `otv-admin` | Python3.9 Web 60s | `/admin/` | 后台管理（Timeout 必须 60s，部署后必查） |
| `otv-web` | Python3.11 Web 60s | `/web/` | 网页版（scf_bootstrap 注入 `PORT=9000 OTV_BIND=0.0.0.0` + vendor quickjs PYTHONPATH） |

- **服务账号**（配额 3 个已满，勿新增）：`svc_activate`（uid …3568002，仅 activate 函数）/
  `svc_import`（uid …620737，管理/导入白名单 = 全部 `*_admin_*` RPC 的 `auth.uid()` 白名单）。
  PG 网关对发布密钥/API Key 一律 401，唯一稳定路径 = `POST /auth/v1/signin` 换 2h token。
  改密/禁用 svc_activate = 激活端点立即断供（紧急停机开关）。
- **存储**：pgstore 公开桶 `hotupdate`（200MB 上限）——热更包 `backend-hu<N>.zip`（对象名含版本
  永不覆盖）+ IPTV 资产（`iptv/iptv-latest.m3u`、版本归档、台标）；匿名 GET 支持 Range 断点续传。
- **数据表**：`licenses` / `activate_log`（卡密）；`hotupdate_pkg` / `hotupdate_log`（热更）；
  `iptv_channel` / `iptv_line` / `iptv_version` / `iptv_admin_log` / `iptv_config`（电视源）。
  RPC 全 SECURITY DEFINER + svc_import 白名单；anon 零表直读，公开入口仅 `activate_code` /
  `hotupdate_check` / `hotupdate_report` / `announcement_latest`。DDL 迁移文件在 `tools/*_migration.sql`。
- **RLS**：桶对象 storage.objects 锁桶+锁 uid；匿名下载仅 public=true 桶放行。

## 8. 构建与部署

### APK

```bash
# TV 版（android/ 下）        # 移动版（internal/mobile/ 下，Windows 用 gradlew.bat）
./gradlew assembleNeiceRelease        # 内测版（neice：无卡密/热更门控）
./gradlew assembleRelease             # v1.19 起双 flavor 退役，单线发布（LICENSE/HOTUPDATE_ENABLED 常开）
```

产物 gradle 自动归档：`apk/TV版/MyTV-TV版-v1.21.apk`、`apk/移动版/…`；历史版本在
`apk/归档-旧版/`。签名共用 `optimaltv-release.keystore`（10 年）；不混淆（Chaquopy+反射保守）。

### 内嵌后端 zip 与热更发布 SOP

```bash
python tools/build_backend_zip.py     # tools/backend-src/ → android/app/src/main/assets/backend/python-backend.zip
# 本地自测：python proxy.py 8091
# 后台「热更新」页 → 点「上传最新版本」一键直传（或手动选文件）→ 选发布方式
```

### 云函数部署配方

- **otv-admin**：`python tools/build_admin_function.py` → MCP `manageFunctions updateFunctionCode`
  （functionName=otv-admin，functionRootPath=`cloudfunctions`）或 tcb CLI
  `printf '\n' | tcb fn deploy otv-admin --httpFn --dir . -e appletv-d5ge1bth794873f76 --force`
  （在 cloudfunctions/otv-admin/ 下）。验证：/admin/ 200 含新特征词、POST api/login ok、
  **getFunctionDetail 确认 Timeout=60 未被重置**、环境变量完好。
- **otv-web**：`python tools/build_web_function.py` → 同上 tcb CLI 部署 → `tcb fn detail otv-web`
  必查 Timeout=60（曾重置为 3s）。部署包**不放 package.json**（误判 Node.js）。
- **坑**：MCP 直传 zip 丢 scf_bootstrap 可执行位（必须 tcb CLI 或 MCP updateFunctionCode）；
  bootstrap 用 PATH 的 `python3`（绝对路径在 3.9 镜像不存在）；代码须 Python 3.9 兼容
  （`X | None` 是 3.10+）；Web 函数端口 9000；PG DDL 走 MCP execute 逐条（applyMigration 静默失败）。

### tools/ 脚本速查

| 脚本 | 用途 |
|---|---|
| `sync_shared.py` | 双工程共享层一致性检查/同步（**共享层改动后必跑**） |
| `license_gen.py` / `db_import.py` | 卡密签发（私钥签名→codes.csv）/ 入库 PG（signin 通道） |
| `build_backend_zip.py` | 打包内嵌后端 zip（= 热更对象） |
| `build_hhkan_snapshot.py` | 从好好看实时页面生成零 PG 依赖的云端/端侧容灾快照 |
| `build_admin_function.py` / `build_web_function.py` | 后台 / 网页版云函数部署包构建 |
| `build_logo_assets.py` / `logo_strip_text.py` | 真实台标拉取 / 白边切割（成品 22 个**勿重跑覆盖**） |
| `gen_pinyin_dict.py` / `gen_pinyin_initials.py` / `gen_pinyin_titles.py` / `gen_app_icon.py` | 拼音词典 / 首字母联想表 / 片名快照 / 图标 |
| `iptv_migration.sql` / `hotupdate_migration.sql` / `admin_sold_migration.sql` / `announcement_migration.sql` | PG 表 + RPC 迁移 |
| `watch_public_release.sh` / `monitor_live.sh` / `focus_drive.sh` | 发布监控 / 直播源监控 / 焦点压测 |

## 9. 测试与验收

- 环境：MuMu 模拟器（adb `127.0.0.1:16384`，Android 12/15 x86_64）装 debug 包实测：
  截图 + logcat（tag `OTV`）+ uiautomator 文本取证，逐条对照需求记录于 `docs/测试记录.md`。
- 双 app 抢 8090 端口是测试环境坑（真实场景双端共存自愈）；横竖屏切换 / TV 按键语义
  （select=OK 长按）详见测试记录。
- 网页版改动**必须浏览器实测**（curl 掩盖字段名/传参类型/脏通道三类静默炸法）。

## 10. 运维手册

| 场景 | 操作 |
|---|---|
| 卖卡 / 售后 | 后台「卡密」页查码、封禁/解封、单台/全部解绑、标记已售；激活轨迹在「激活日志」 |
| 补货 | 本机 `license_gen.py gen --plan X --count N` → `db_import.py` 入库 → 后台可见；备注含「测试/勿售」的码**绝不导上发卡平台** |
| 吊销卡密 | 后台封禁（已激活设备下次联网启动自动降级） |
| 常规热更发布 | 改 `tools/backend-src/` → `build_backend_zip.py` → 本地 8091 自测 → 后台一键直传 |
| 撤销已发热更包 | 包列表「下线」（存量设备停留当前版本，已更用户不受影响）或「删除」 |
| 坏包已推（设备隔离） | 修复后**升版本号**重发；确认 apply_failed 不再增长 |
| 全局紧急停更 | hotupdate 函数 env `OTV_HU_OFF=1` |
| 电视源日常 | 后台改→「发布并生效」；回滚走版本历史；改了没变=没发布或未到 30 分钟 |
| 公告 | 后台「公告」页发布/下线；仅前台进入时及前台每 5min 检查；云函数 5min 合并缓存；发新 id 会再弹 |
| 凭据安全 | 私钥/codes.csv/`tools/keys/svc_*` 等同密钥级：不进 git、不进云、不贴会话；长 token 走本地凭据文件不经人工复制 |
| 环境续期 | CloudBase 体验版 2027-03-01 到期；免费档 3000 资源点，超量个人版 ¥19.9 |

## 11. iOS 路线（唯一在维护：B 方案 0 成本侧载）

PWA（网页版）优先；原生壳 = Flutter + `flutter_inappwebview` 加载网页版 URL →
Codemagic 免费云 Mac 打包（500 macOS 分钟/月）→ 免费 Apple ID 签名（7 天/3 App 名额限制）→
Sideloadly 首装 + AltStore 自动续签。**一票否决检查先做**：iPhone 系统版本若在
iOS 14.0–17.0 → TrollStore 永久免签（跳过侧载流程）。壳工程 `ios_shell/`，Bundle ID 一步到位
永不更改。Windows 前置：Apple 官网版 iTunes + iCloud（微软商店版不行）。App 过期变灰**不要删**
（AltStore 刷新可救活）。完整 SOP 见归档 `iOS-B方案-0成本侧载.md`。

## 12. 版本总览（详细记录见 `docs/版本史.md`）

| 版本 | 日期 | 要点 |
|---|---|---|
| v1.1–v1.12 | 08-27~08-31 | 界面/播放器对齐 apple-tv-like-player、16~30 项需求多轮批次、焦点全站重构、性能根治、直播解码兜底 libVLC、解密桥加固 |
| v1.13–v1.14 | 09-01 | OtvNav 集中式焦点引擎；「电视」页 + IPTV 直播；播放卡顿根治（点播 20 分钟定时卡顿） |
| v1.15–v1.16 | 09-01 | 电视页进入即播 + 两级选台侧边栏；直播源精编整合 + 多源备份无感切换 |
| v1.17 | 09-02 | 台标全真实；公测移动版交付，四产物矩阵成型 |
| v1.18 | 09-02 | 热更新上线 + 后台管理系统重设计（OTV 运营终端） |
| v1.19 | 09-03 | 版本线重规划（2 APK）；电视直播五件套（测速换台/云源日推等）；移动版触控深化、双 flavor 移除 |
| v1.20 | 09-03/04 | 12 项体验优化 + 后台上云 + 电视源动态管理；追加移动 6 项 UI + 双端 7 项稳定性；热更后端进程化 |
| v1.21 | 09-04 | 网页版上线 + 双端 DLNA 投屏 + 后台改密 |
| v1.22(w) | 09-05 | 网页版 APK 同构复刻 + 足球直播解密上云 |
| v1.23(w) | 09-05/06 | 网页版逐行对照重写；足球直播信号源补全 + 卡顿根治 + 起播提速 + 列表实时刷新（三端） |
| 追加批次 | 09-05/06 | 后台重设计两迭代 + 本地形态退役；电视源页工作流重构 + 台标上传；热更版本号自动化 + 定时发布；热更页一键上传最新版本；公告/投屏/选台侧边栏/焦点多轮修补 |
| v1.22 / v1.24(w) | 09-07 | 影视页跳行根治（板块名 item key）；直播源到货即上屏 + 3-WebView 解密池（3.2×）；直播默认记住上次信号源；后台热更分片上传（网关 6MB 上限根治）——当前 APK 版本 versionCode 24 |
| **v1.25(w) + 双 APK** | **09-07 晚** | **第二批优化 12 项：网页会员系统（activate 同源转发+WebCrypto 验签+付费墙）；网页 SWR 提速+筛选缓存毒修复+静态资源版本化；网页竖屏 9 子项（播放器/电视播放器重构、3 列片库、手势返回）；热更包纳入 www 前端 + release_all.py；后台 soft 刷新+公告表单美化；TV 直播页去选台/投屏按钮（OK 直达侧边栏）；板块 hotSettled 稳定化（光标跳行收尾）；三端 hero 切换动画；移动公告系统通知** |
| **v1.23 + 热更 v10** | **09-08** | **第三批三项：⑮影视多源池（bfzy+fdzys 双站测速换站+线路清晰度探测排序+hhkan 跨站补线 pid≥100+集数 bug 修复）；⑬解密池自愈（onRenderProcessGone 重建+零回调检测，根治「半小时后所有信号源失效」）；⑭TV 投屏全删（移动保留）；hhkan 701 reCAPTCHA 适配；⑫平台中间页行为核验（首访一次，唯一解=自定义域名留用户决策）** |

## 13. 已知限制与边界

- 热更只覆盖内嵌后端；APK 级改动必须发版。
- 设备端下载失败不重试（下次启动再来）；同版本号重发无法触达已隔离设备。
- `hotupdate_check` 是公开 RPC：最多泄露已发布包元数据（本就是公开分发物）；report 可被灌
  垃圾日志（detail 截 500 字符，量级异常用 OTV_HU_OFF 止血）。
- 网页版云端片库与 App 不完全一致（hhkan 对数据中心 IP 封锁 → macCMS 降级链，契约一致）。
- 云端补货需函数 env OTV_PRIV_KEY（默认不设，私钥不出本机）。
- `D:\MyProjects\Apple TV` 是空壳，真实工程在 `D:\MyProjects\My TV`。

## 14. 归档文档索引

以下原件已合并进本文档并归档至 `docs/_archive-20260906-文档合并/`（含根目录 PRODUCT.md /
DESIGN.md，后台设计系统完整规范在归档 DESIGN.md）：

卡密付费-方案与需求.md · 移动版适配.md · 电视直播-方案与需求.md · 网页版-方案与实施.md ·
热更新-方案与实施.md · 后台管理与电视源-方案与实施.md · 后台管理-电视源-使用说明.md ·
iOS-B方案-0成本侧载.md · PRODUCT.md · DESIGN.md

更早历史：`docs/_archive-20260829-搜索对比/` · `docs/_archive-20260905-iOS旧方案/` ·
`tools/_archive-20260905-admin/`（后台旧版设计）。
