# 测试记录 — 优化版 MuMu 全流程验收

> 注：APK 产物自 2026-09-02 起按类别归档至 `apk/` 子目录（内测TV / 公测TV / 内测移动 / 公测移动）；
> 早前记录中的 `apk/OptimalTV-*.apk` 平铺路径对应现 `apk/内测TV/`、`apk/公测TV/`、`apk/内测移动/` 下的同名文件。
> 另：2026-09-02 起 TV 线品牌更名为 **My TV**（应用名/LOGO/产物前缀 MyTV-）；移动线按用户要求保持
> Apple TV 交付原貌，早前记录中的 Apple TV/OptimalTV 均为历史称谓。

## 2026-09-15 · CloudBase 后台管理系统下线 + 存量链路回归

- **删除**：`tcb fn delete otv-admin -e appletv-d5ge1bth794873f76` → `Function [otv-admin] deleted
  successfully!`；`tcb fn list` 复核只剩 iptv / otv-web / iptv-rebuild / hotupdate / activate。
- **路由**：`tcb routes delete '*' -p /admin` 与 `tcb routes edit … enable:false` 均被拒
  （`domain * belongs to a different business scope`），环境默认域名路由 CLI 不可改。
- **下线后实测**（直连 CloudBase 默认域名）：`/admin/` → 435（不再提供后台）；`/activate`（协议 v2 + 非法码）
  → 200 `{"ret":404,"msg":"not_found"}`；`/hotupdate/check`（App 同形请求 hotVersion/apkVersion/deviceId）
  → 200 `hasUpdate:true` v17 + `sha256 4f0c536d7523…`，与自建站点同一份包；`/iptv/latest` → 200 17,424B；
  `/web/api/health` → 200 `{"ok": true}`。结论：后台完全下线，且不影响存量设备链路。
- **仓库回归**：`python -m unittest discover -s tools/backend-src/tests -p 'test_*.py'` → 113 项通过。
- **同日合并**：`feat/combined-search-recovery` 并入 main（merge commit `5030982`），两端
  `assembleRelease` 均构建成功；APK dex 内只含新域名 `mytv-cloud.pengzhiyuan0724.chatgpt.site`，
  旧 `appletv-…tcloudbase.com` 已不存在。

## 2026-09-15 · 自建站点迁移核对（只读探测 + 本仓库回归）

本轮为对接核对与文档同步，未改业务逻辑。验证方式 = 直连自建站点只读探测 + 本仓库契约测试。

- **端点实测**：`/healthz` 200；`/hotupdate/check` 返回 v17 + `sha256 4f0c536d7523…`；
  `/objects/backend-hu17.zip` Range 0-1023 → 206 / `bytes 0-1023/40416518`；`/iptv/latest` 200
  17,424B m3u；`/hotupdate/announce` 200 公告 JSON；未登录 `/api/*` 401。明细见《版本史》同日条目。
- **协议一致性**：App 端 `LicenseManager.callEndpoint` 发 `{protocol:2, action, code, deviceId
  [, currentCode]}`；站点 `/activate` 首判 `protocol !== 2 → 426`。用同形请求（协议 v2 + 非法码）
  实测返回 `400 bad_request`——端点通、入参校验生效、不会误判为协议不兼容。
- **热更包一致性**：新站点下发的 `sha256 4f0c536d7523…` 与本机 v17 热更包一致，`objects/` 支持
  Range 断点续传。
- **本仓库回归**：`python -m unittest discover -s tools/backend-src/tests -p 'test_*.py'` → 75 项通过。

待办（不属本轮）：出 v1.26 APK（两端端点已改好，待构建发布）；切换前再跑一次 CloudBase → 站点
增量同步；接外部定时器调 `/iptv/refresh` 取代 CloudBase 每日推流。

## 2026-09-13 · 文档规范化回归

- 文档入口、开发发布手册、根目录 README 与详细项目文档完成链接和当前状态校对。
- 本轮未修改业务逻辑；验证方式为 Markdown 链接/路径检查、`git diff --check`，以及现有后端契约测试保持通过。

## 2026-09-12 · 竖屏自适应第二批（①足球hero缩小 ②标签栏禁横滑 ③播放闪退排查 ④影视3列 ⑥会员底部固定）+ ⑤⑦方案稿待确认

环境：`MyTV_Mobile_API30` AVD（emulator-5554，1080×2400@420dpi，**-dns-server 223.5.5.5 启动**——默认 DNS 故障是本轮此前一切网络失败/激活不可用的根因，与《版本史》2026-09-10 已知项一致）；网页 = tools/backend-src 本地 proxy 8099 + Playwright 390×844 视口。证据目录 `shots/20260912-竖屏适配/`，规格 `out/plan.md`（竖屏适配第二批节）。

### 实测结论（逐项）

- **①足球 hero 竖屏缩小**：移动 `LIVE_BAND_PORTRAIT=520`（对齐影视 VOD_BAND_PORTRAIT），内容列 top 170→120、CTA top 500→390；实测 dump：比分行 y=173-449、状态行 y=484、CTA y=593-664（=390×1.44 物理），带高 749 物理 px，CTA 带内。网页 `#liveHero` 520 + `.live-hero-content` top120 + `.cta-watch` top390（240×82），实测 CTA bottom 245 < hero bottom 270。
- **②标签栏禁横滑**：足球联赛 chips 竖屏 FlowRow 换行（10 项两行，dump y=800/919 两行）；电视分组 chips 竖屏 FlowRow（7 组两行，截图 mob-live-tv-after.png）；网页 league-chips/tv-group-chips flex-wrap（实测 scrollWidth≤clientWidth）。搜索 tabs/全部页筛选/影视分类 chips 此前已达标（回归不动）。影视板块卡片行（网页）竖屏改 3 列 grid、我的页收藏/历史行同改。
- **②影视标签栏重复（电影/电视剧/综艺重复）根因实锤**：旧 APK（v1.23 release 包）dex 内 VodRepository 引用 `DemoSource$Companion`——旧版 hhkan 失败时降级演示源，演示源分类（demo:1 电影…）与 hhkan 同名分类 id 不同 → 合并目录 chips 重复（本轮 emulator DNS 故障期完整复现 9 chips：电影×2/电视剧×2/综艺×2+动漫+短剧+全部›）。现行源码 VodRepository 已单源化（hhkan 唯一），新 debug 包实测分类恰 6 项无重复。**新包修复，无需再改代码**。
- **③影视点击播放闪退**：无法复现——DNS 修复后，旧 release 包与新 debug 包同流程（hero「四渡」→ 详情 → 立即播放）均正常起播（旧包实测首帧渲染 READY 1920×1080、进度推进；新包 BUFFERING→直连重试→READY）。PlayerViewModel 防线已齐（startSession `lines.getOrNull` 兜底、boot 全 runCatching、null 影片 fatalMsg）。判定：用户侧闪退最可能 = 演示降级混合目录期点 demo 卡（详情/线路空链路）+ 旧版个别防护缺口，均已在 9/9-9/11 修复批次覆盖；如再现需用户提供具体影片名与 logcat。
- **④影视界面卡片一行 3 个**：移动端 HomeScreen libCols=3 竖屏分支此前已达标（实测截图 mob-4tabs-after.png 第二格「最近热门」3 列）；网页本轮补齐——`.hscroll.poster-row` 竖屏改 `grid repeat(3,1fr)`（实测 cols=3、卡宽 112 物理、无横滚），「我的」页 fav-row 同构。
- **⑥会员信息底部固定**：移动 FavoritesScreen 竖屏 MemberTopRightLine docked 模式（`docked=true` 去顶栏定位/下潜动画）+ 固定底部条（bg 0xF5141418 + navigationBarsPadding + padding 40/16），LazyColumn contentPadding bottom 80→240 让位；横屏保持右上角随标签栏下潜。实测截图第三格：底部条「● 会员有效 · 周卡 · 到期 2026-09-18 · 剩 6 天 · 续费」不随滚动。网页 `.member-card` 竖屏 position:fixed 底部 + `.fav-body` padding-bottom 300，实测 top724/bottom844 贴视口底、不与标签栏（bottom61）重合。
- **⑤⑦（筛选面板/选台组件竖屏重做）**：按用户要求只出方案稿待确认——`shots/20260912-竖屏适配/方案参考-⑤筛选⑦选台.png` 四方案：A 筛选条展开（豆瓣式）/B 底部弹层筛选（爱奇艺式）/C 播放器下方常驻列表（现状基线）/D 底部弹层选台（央视频式，建议）。mockup 源 `tools/mockup_portrait_57.html`。

### 涉及文件

- 网页（v130）：`tools/backend-src/www/style.css`（同步至 cloudfunctions/otv-web/www）。
- 移动：`ui/live/LiveScreen.kt`（LIVE_BAND_PORTRAIT + FlowRow LeagueFilterBar）、`ui/tv/TvScreen.kt`（TvBrowseScreen chips 竖屏 FlowRow + OptIn）、`ui/favorites/FavoritesScreen.kt`（MemberTopRightLine docked + 底部固定条 + contentPadding）。

## 2026-09-11/12 · v1.24 第四批六项验收（海报/筛选提速 · 投屏全删 · 播放器侧栏光标 · 换源全源 · 我的页会员行）

环境：SDK AVD emulator-5556（android-30 x86_64, 1920×1080）+ 本机 backend-src 直起（127.0.0.1:18091）。
测试包：debug 构建（otvDebugLicense 注码 OTV-SBU3P-4HE5U 周卡激活）+ 新 python-backend.zip（v1.24 后端）。

### A. 后端提速定量（本机直起 18091，hhkan live 可达 + 快照双路径）

| # | 验收项 | 修复前 | 修复后 | 证据 |
|---|---|---|---|---|
| 1 | 冷图经 /hhkan/proxy | 1.10~1.38s/张（urllib 逐张 TLS） | 首张 0.12s、后续 0.01s（线程本地 keep-alive 池） | 6 张冷图总 0.16s |
| 2 | 数据响应级封面预取 | 无 | 响应后 2.5s 内 8/8 张已可秒取（磁盘缓存暖） | show p7 响应后抽测 |
| 3 | /hhkan/show live | 串行 page+page+1 ≈2×0.3~3s | 并行 1.64s（p5 冷） | t4.json source=hhkan |
| 4 | 翻页（加载更多） | 每页 2×抓取 | stash 命中 34ms | t5.json 同源同页 |
| 5 | **存量 bug：live show `lang_=` TypeError** | live 筛选查询全挂（TypeError→快照） | live 正常返回 | 修复前 log: `get_show_page() got an unexpected keyword argument 'lang_'` |
| 6 | **存量 bug：快照 show `lang_=` TypeError** | 快照兜底整体 500（用户设备快照模式筛选又慢又空的直接根因） | 快照正常（108ms） | 修复前 `{"ok":false,"error":"SnapshotCatalog.show() got an unexpected keyword argument 'lang_'"}` |
| 7 | filters 页缓存 TTL | 90s | 3600s（键级 TTL，命中检查分派） | 代码路径 + 首次 0.72s/命中 0.005s |
| 8 | 探针死线 | 无限（_fetch 10 次×25s） | 12s 超时→熔断走快照（5-8ms） | _HHKAN_PROBE_TIMEOUT |

### B. AVD E2E（六项逐条）

| # | 用户问题 | 结果 | 证据 |
|---|---|---|---|
| 1 | 影视页海报加载慢 | ✅ | 全部页海报墙 90-95% 出图（进页 ~6s，all2c.jpg）；影视页回页 85-90%（vod3c.jpg，Coil 磁盘缓存+后端预取） |
| 2 | 全部页筛选/结果慢 + 类别去「全部」 | ✅ | 类别行=电影/电视剧/动漫/综艺/短剧（无「全部」，all1.jpg）；其余行「全部」保留；初始焦点=电影 chip；repo 会话缓存+awaitBackendReady；后端 A#3/4/7 |
| 3 | TV 版投屏全删 | ✅ | TV 工程全量 grep 仅剩 2 处历史注释（PlayerViewModel 头/分区注释）；data/cast 已删；sync_shared ✓（PlayerViewModel 差异允许备案） |
| 4 | 播放器侧栏光标到不了最下面 | ✅ | 修复前复现：`OtvNav: blocked dir=3 cur=Rect(876,1007,1106,1080) canStep=null` + 池转储 20 节点 Rect.Zero；修复后：MENU→DOWN×14 焦点逐行下穿（GAINED#11…#44），UP×3/DOWN×4 往返，最终 blocked 时 canStep=false（=真正到底），焦点白色高亮实测落「画幅 4:3」chip（e2e_bottom.jpg） |
| 5 | 换源线路展示所有源 | ✅ | FlowRow 多行换行 ~5 行（蓝光1/7/FF/HN/LZ/XL/WJ/IK/JY + jszy/jinying/hongniu…，e2e_panel.jpg），全部源一次可见、逐行可导航 |
| 6 | 我的页会员行与我的收藏同行、只选「续费」 | ✅ | 标题行右侧「会员有效·年卡·到期 2026-09-17·剩余6天 + 续费」（my1.jpg）；tab DOWN 定向落焦点=54×64px 小 chip（GAINED#182 (1166,335)-(1220,399)，非整行）；白色高亮+下划线仅套「续费」两字（my6w.jpg） |

### C. 回归抽查

- 播放链路：deep-link `hhkan:373162` 起播正常（startSession line=1 → READY；relay m3u8 清单重写正常，本机 18091 抽验 /api/relay 分片重写）。
- 焦点引擎其余页面：影视页/我的页/搜索页 tab 导航往返正常；全部页海报卡聚焦正常。
- sync_shared.py：✓ 全部共享文件一致（PlayerViewModel 差异允许；data/cast/DlnaCast.kt 缺于 android 为预期——移动版保留 DLNA）。


## 2026-09-10 · 用户反馈三问题闭环验收（足球云端兜底 / 三栏海报自愈 / 网页搜索修复）

环境：宿主机（长沙移动出口，yoozb 系被拉黑=真实故障场景）、MyTV_TV_1080p AVD
（android-30，重启 + `-dns-server 223.5.5.5` 修复模拟器 DNS 故障后全程在线）、
云端 otv-web（service.tcloudbase.com/web）、浏览器自动化（ZCode IAB）。

### A. 根因复测与「足球无比赛」云端兜底（本机 + TV AVD 双层）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 故障复现（旧包） | ✅ | AVD 装 09-08 旧 APK：后端 `/api/matches` → `{"error":"HTTP Error 403","matches":[]}`，UI「暂无比赛」 |
| 2 | 拉黑范围确认 | ✅ | 直连出口 120.228.139.24（长沙移动）；yoozb.live/5cj.tv 均 403；云端同接口 200（43 场） |
| 3 | 本机兜底（列表） | ✅ | `build_api()` 43 场 `source=...\|cloud-relay`（冷路径+SWR 双路径改造） |
| 4 | 本机兜底（流解析） | ✅ | `resolve_stream('46387831')` bb/plu 均解出真实 m3u8，`cloud:true` |
| 5 | 云端解析能力 | ✅ | 3 场直播 × bb/plu/qqlive666 全部解析出 m3u8（bb: pul-tenm.gkykp.com 等） |
| 6 | 设备端到端（新包） | ✅ | 新 APK 装载：`/api/matches` 143 场 cloud-relay；UI 完整渲染（直播比分/筛选/队徽，tv_new_home3.png） |
| 7 | 稳定性 | ✅ | 跨 3 个刷新周期（~100s）连续探测全部 cloud-relay，实时比分变化上屏 |
| 8 | 防自环 | ✅ | 云端 bootstrap `OTV_CLOUD_RELAY=0`；云端自身不请求自己公网地址 |

### B. 影视三栏 / 海报（自愈确认 + 新包回归）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | hhkan 实时恢复 | ✅ | 本机过挑战 1.0s 抓通首页；`channel-sections/1` 三栏目 3×16 条 source=hhkan |
| 2 | 海报 CDN 恢复 | ✅ | vres.enbymae.com 直连 200 image/webp 21.5KB；img_url 规范化 3/3 加载 |
| 3 | TV AVD 三栏 | ✅ | 设备后端 cid=1/cid=2 均 3 板块×16 条实时；UI 海报真实出图（tv_new_vod2.png） |
| 4 | chips 无重复 | ✅ | uiautomator 视图树：电影、电视剧、动漫、综艺、短剧、全部› 恰 6 项（此前「重复标签」为图像识别误读） |

### C. 网页版五栏目 + 搜索修复（快照扩充）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 足球/影视/电视/我的 | ✅ | 浏览器实测：比赛列表+比分/推荐位+三栏/7 分类 CCTV 全系列/卡密激活+收藏 |
| 2 | 搜索缺陷定位 | ✅ | 旧快照仅 626 张小众卡，云端搜索 0.2s 空结果（熔断常态开启走快照） |
| 3 | 快照重建 | ✅ | 952 卡片/698 详情/21.8MB；庆余年一二季（30/22 线路，1190/773 集）入快照 |
| 4 | 云端搜索验收 | ✅ | `search?k=庆余年` 两季（含封面评分）；`detail/27139` 30 线 1190 集 |
| 5 | 网页 UI 实搜 | ✅ | 浏览器搜「庆余年」→ 全部 2 / 电视剧 2 分类正确、完结 36 集标签正常 |

### E. 热更 v11 发布 + 生产链路 E2E（当晚补验）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 后台 API 发布 | ✅ | admin login → hu_upload_chunk ×10（4MB/片）→ commit 自动分配 v11，返回 ok+sha256 |
| 2 | 生产 check 验收 | ✅ | hotVersion=10 设备 POST /check → hasUpdate=true, v11, 40279517B, url 指向 backend-hu11.zip；首 4MB 可下载 |
| 3 | 设备发现→确认 | ✅ | TV AVD 全新装态启动弹「发现新版本 v11·38.4MB」；OK 键确认 → logcat「用户确认更新 v11」「report downloaded v11」 |
| 4 | 下载校验应用 | ✅ | files/hotupdate/backend-hu.zip 40279517B 精确匹配；hu_state active=11；embedded_backend/.extracted=hot|11|40279517；proxy.py 含 5 处 CLOUD_RELAY；快照 generatedAt=2026-09-10 |
| 5 | 热更后功能 | ✅ | 设备 /api/matches 109 场 cloud-relay；/hhkan/search?k=庆余年 实时返回 3 条（实时源已恢复，兜底静默待命） |

> 注：发布动作经用户确认后由 AI 代执行（「帮我执行」）；未启用 forceApply，设备端仍走用户确认弹窗。

### D. 回归与部署

- 后端契约 pytest 18/18；TV AVD `connectedDebugAndroidTest` 13/13（0 失败 0 错误 0 跳过）；
  `sync_shared` 双端共享一致；otv-web 已重部署（新快照 + OTV_CLOUD_RELAY=0 bootstrap）。
- 热更包 `python-backend.zip`（40.3MB，含云端兜底+新快照）已重建并同步移动版；
  **线上发布需后台「热更新」页手动上传**（未自动发布）。TV 验证时发现热更通道会把
  新内嵌后端覆盖回已发布 v10 旧包（符合设计），测试以 hu_state active=10 跳过。
- 遗留观察：`service.tcloudbaseapp.com` 域名部分移动侧 CDN 节点回错误证书 `*.tcb.qcloud.la`
  （tcloudbase.com 域名当前正常）；绑定已备案自定义域名的操作见 `docs/02-guides/开发与发布手册.md`。

## 2026-09-08 · v1.23 + 热更 v10：优化需求第三批验收（⑮多源池 / ⑬解密池自愈+长稳 / ⑭TV 投屏移除 / ⑫中间页）

环境：本地 proxy.py（8094 hhkan 态 / 8093 mcms 态）、MuMu 实例 0（TV，adb root）、Playwright 浏览器
（云端 /web/ + /admin/）、宿主机 8099 ffmpeg testsrc2 无限流（前会话遗留，分片持续产出）+
bind-mount hosts（10.0.2.2 testsrc.local）。

### A. ⑮ 影视多源池（本地双实例 + 设备真环境 + 云端 + 浏览器四层验证）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 双站测速排序+换站 | ✅ | 本地 /hhkan/home source=mcms sections 3~4；fdzys 慢（2.5s 级）时 bfzy 排序在前；单站超时自动降权换站 |
| 2 | ID 站命名空间 | ✅ | 搜索 id 全部 >10M（fdzys 前缀 2、bfzy 前缀 1）；detail/play 反解正确站 |
| 3 | 线路清晰度排序 | ✅ | 设备 detail/20003861（蜘蛛侠3）：`1080zyk(10→实测1920x1040)` 排第 0；云端同款 1080zyk 置顶 |
| 4 | 集数 bug 修复 | ✅ | play/20040769/0/3 → 「wsym3u8·第04集」单源（旧版返回整组 8 集）；vid=7 → 第08集 |
| 5 | 外站线过滤 | ✅ | 暗影蜘蛛侠（黑白版）唯一 bilibili 线被滤（回退保留防 0 线路）；蜘蛛侠3 的 qiyi 线不进列表 |
| 6 | 探测失败不丢线 | ✅ | MuMu 弱网探测超时后详情仍有 5 线（修复前曾 0 线路） |
| 7 | hhkan 跨站补线 | ✅ | 本地 detail/262132（玩具总动员5，hhkan 态）：fdzys·1080zyk(pid151) 排第 0 + wjm3u8 + bfzy·bfzym3u8 等 5 条补线；play pid151 → `cdn.yzzy31-play.com/...index.m3u8`（1920x1040 master） |
| 8 | 标题归一化匹配 | ✅ | 「蜘蛛侠英雄无归」（hhkan）×「蜘蛛侠：英雄无归」（fdzys 全角冒号）→ 归一化+前缀降级搜索命中 |
| 9 | 设备 mcms 全链路 | ✅ | MuMu（Chaquopy 真环境）：搜索 19 条 → 详情 5 线 1080 置顶 → 播放解析 1080zyk 真链 |
| 10 | 设备播放 E2E | ✅ | 详情页立即播放 → relay 起播 1.9s 首帧（1920x1452）→ READY @119s buf 113s dropped=0 |
| 11 | 网页 E2E | ✅ | 云端 /web/：搜索→详情 6 线（1080zyk 首）→ 付费墙拦截「开通会员后可观看」→ 激活周卡 OTV-NWE3R-7XF45（otvw:ticket 落盘）→ 播放 paused=false t=3.28s readyState=4（blob 中继流）；截图 web_play_1080zyk.png |
| 12 | 云端部署核验 | ✅ | /web/hhkan/detail/20003861 线路表正确（第一轮旧代码部署被当场抓包：qiyi 网页链接混入 → 重 build_web_function 重部署后干净） |

### B. ⑬ 解密池自愈 + 长稳（MuMu adb root 直杀渲染进程）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 基线解密 | ✅ | POST /decrypt 合成页 0.5s 返回 url（含 3 次真实比赛解密 OK 前置） |
| 2 | 渲染进程死亡恢复 | ✅ | kill -9 sandboxed_process → logcat `render gone slot=0 crashed=false → 销毁重建` → 下一次解密 0.5s OK（无 12s 挂起/无需重启 app） |
| 3 | 全池恢复 | ✅ | 三槽连杀后 4 连解密全部 0.4~0.5s OK |
| 4 | V8 堆炸弹不崩 | ✅ | 2.6GB JS 字符串分配只触发 V8 RangeError（被页内 catch），渲染进程存活——真实世界崩溃源是渲染进程级 OOM/崩溃，由 onRenderProcessGone 覆盖 |
| 5 | 30min 直播浸泡 | ✅ | live:999001（testsrc2 无限流）04:44~05:15+ 连续播放：READY 全程、dropped=0、32+ 次 rebuffer 全部自愈（测试流高码率供给不足所迫，真 CDN 场景 v1.23 实测 11-23s 缓冲）、BEHIND_LIVE_WINDOW 就地追边（pos 回落 48→33s 后继续）、后端 /api/health 全程 ok、解密桥期间经历 3 次渲染进程击杀仍即时恢复 |
| 6 | hhkan 701 适配 | ✅ | 设备探针旧形态 HTTP 701 直接抛错误判源站死 → 修复后 701 入挑战码集+状态码判活；本机抓包确认 701 体为 reCAPTCHA（不可程序化通过，属站点策略，多源池承接内容供给） |

### C. ⑭ TV 投屏移除 + 回归（MuMu 实例 0，v1.23 APK）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 播放器控制条无投屏胶囊 | ✅ | uiautomator 全文无「投屏」；左上像素采样纯黑（旧胶囊位置） |
| 2 | 更多面板无投屏节 | ✅ | 面板滚动到底：选集/换源线路/倍速/画幅，无「投屏/扫描/断开」 |
| 3 | 播放回归 | ✅ | mcms 播放 E2E 见 A#10；BACK 分层/菜单正常 |
| 4 | 移动版投屏保留 | ✅ | 移动版 PlayerScreen/TvScreen 投屏入口代码未动（仅 TV 工程删；共享层 sync_shared 全绿） |

### D. ⑫ 中间页 + 部署发布

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 平台风险提醒行为 | ✅ | Playwright 新鲜上下文首访出「风险提醒」→ 确定 → 同浏览器二次访问直出 My TV（平台 HttpOnly 会话态） |
| 2 | 自有 boot 屏已删 | ✅ | /web/ 200 直出（v1.25 #boot 移除维持） |
| 3 | otv-web 部署 | ✅ | tcb fn deploy 两轮（终态含全部修复）；云端 detail/play/search 全对（A#11/12） |
| 4 | 热更 v10 发布 | ✅ | 后台浏览器 GUI 手动选文件 → 9 分片 36,738,891B 上传 → 「热更包 v10 已发布 · SHA-256 44ab4b05…」；POST /hotupdate check（hotVersion=8, apk=24）→ hasUpdate=true hotVersionCode=10 url=backend-hu10.zip |
| 5 | otv-admin Timeout/环境变量 | ✅ | getFunctionDetail：Timeout=60、env 六项完好（部署未触碰 admin） |

## 2026-09-07（晚） · v1.25：桌面优化需求 12 项全链路验收（浏览器双环境 + MuMu 实例 0/1）

需求来源：`桌面/优化需求.md`（第二批 12 项）。环境：本地 otv-web（127.0.0.1:8899）、本地
admin_server（127.0.0.1:8898，连云端 PG）、云端 /web/ 与 /admin/、IAB 浏览器（桌面 1280×800 +
手机模拟 390×844）、MuMu 实例 0（TV 实测）与实例 1（移动实测，Android 15）。

### A. 网页版（本地起服 + 云端部署后双验证）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 需求③ 启动中间页移除 | ✅ | DOM `#boot` 不存在；`pillShadow:"none"` |
| 2 | 需求⑧ 顶部标签栏半透明阴影 | ✅ | `#tabpill::before` computed `none` |
| 3 | 需求① SWR 秒开（影视 tab 切入即读） | ✅ | 100ms 内 heroTitle/81 卡/三板块自 localStorage 上屏；otvw:lib:1 等缓存键落盘 |
| 4 | 需求① 筛选面板全量显示（缓存毒 bug 修复后） | ✅ | 类别 6/类型 22/地区 22/语言 7/年份 8/排序 3 chips 全渲染（curl /hhkan/filters/1 types=22）；空结果不再入 _FILTERS_CACHE |
| 5 | 需求① 详情秒开 | ✅ | 点卡 300ms 内标题+封面上屏（卡片 DOM 直传） |
| 6 | 需求② 会员激活 E2E | ✅ | 周卡 20260907-weekly-1 经 /api/activate（同源转发）激活：「生效中 · 2026-09-14 到期 · 剩余 7 天」；WebCrypto 验签过（否则报票据校验失败）；isPremium=true、播放解锁（player 可见、无付费墙） |
| 7 | 需求② 无效卡密路径 | ✅ | `OTV-AAAAA-BBBBB` →「卡密不存在」；`123` →「卡密格式不正确」 |
| 8 | 需求② 未激活播放门控 | ✅ | 点播放 → 付费墙弹出（文案带片名）、播放器不开 |
| 9 | 需求⑦ hero 切换动画 | ✅ | 切换 150ms 时双层（during=2）、800ms 后单层、computed opacity=1；IAB 挂起环境经 transition:none 兜底同为 1（不黑屏） |
| 10 | 需求12 竖屏逐页截图核验（390×844） | ✅ | 足球（hero/今日两列网格自适应）/影视/片库（3 列+筛选堆叠+返回钮无重叠）/详情（纵向堆叠）/搜索（输入框让位 pill：inputTop 72 > pillBottom 60）/我的（会员卡）/播放器（390×219 16:9 顶+控制区）/电视播放器（16:9+26 行频道列表内联+无 FAB）——逐页视觉判定无重叠/溢出 |
| 11 | 云端部署核验 | ✅ | /web/ 含 `app.js?v=125`/paywall；app.js 含 api/activate/heroSwap/gatePlay ×8；/web/api/activate 线上返回 ret=400 bad_device（链路通）；/admin/ 含 ann-form/loadList(true) ×9 |

### B. TV 版（MuMu 实例 0，最终 APK 20:31 复验）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 需求④ 电视剧 chip DOWN 落点 | ✅ | chip(510,1015,626,1079) 按 DOWN → `GAINED @Rect(332,615,564,967)` = 232×352 海报卡=最近热门首行第 2 张（层内中心距最近）；冷启窗口期与热态均无跳行 |
| 2 | 需求④ 板块稳定化 | ✅ | hotSettledFlow 门控：就绪前仅渲染「片库板块加载中…」占位（lib-loading item），三块一次性上屏 |
| 3 | 需求⑩ 播放态 OK 直达侧边栏 | ✅ | 起播落 1dp sink → OK 瞬时 `GAINED @Rect(290,30,874,130)`（侧边栏频道行，584 宽）→ DOWN 频道二行 (290,143) → BACK 定向回分组列 chip (26,30,256,94)——全程无两钮浮层 |
| 4 | 需求⑩ 画面态换台/返回 | ✅ | UP/DOWN 换台焦点保持 sink；BACK 分层（侧边栏→画面→双击退出）正常 |
| 5 | 需求⑦ hero 切换路径 | ✅ | 影视 hero 左右键切换序列 0 FATAL/0 AndroidRuntime（Crossfade 运行路径无异常） |
| 6 | 冒烟 | ✅ | iptv 78 频道/matches 5 场/目录 256 条/影视预载 5 分类 319 封面，零崩溃 |

### C. 移动版（MuMu 实例 1）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 需求⑪ 公告系统通知闭环 | ✅ | 后台发布 notify-e2e-v3 → 66s 内 logcat `announcement: 系统通知 #15`；pm grant POST_NOTIFICATIONS 后 dumpsys notification 见完整记录（android.title=notify-e2e-v3 + BigTextStyle + otv_announce 渠道）；app 内悬浮窗代码已移除（AnnouncementBanner 0 引用） |
| 2 | 需求⑪ 权限申请 | ✅ | Android 13+ MainActivity onCreate 请求 POST_NOTIFICATIONS（manifest 已声明；MuMu adb 授权模拟用户允许） |
| 3 | 冒烟 | ✅ | 足球快照恢复 5 场/目录快照 112 条/影视预载 5 分类 90 封面，零 FATAL |
| 4 | 需求12 App 竖屏 | ⚠️编译级 | 竖屏分支（片库 3 列/筛选 FlowRow/播放器 16:9+操作区+全屏/电视内联列表）全部编入 BUILD SUCCESSFUL APK；MuMu 像素级验证受环境限制（改 VM 分辨率实例起不来、wm size 被固定显示管线忽略），以同规格网页端逐页截图为准；真机竖屏待自然验证 |

### D. 后台（本地 8898 实测 + 云端部署核验）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 需求⑥ 增删行不整页刷新 | ✅ | 「标记出售」MutationObserver 全程 skSeen=0（零骨架屏闪现）、表格 51 行保持、按钮即时变「取消出售」（数据已刷新非旧表）；操作后还原 |
| 2 | 需求⑨ 公告表单美化 | ✅ | 独立卡片+字数计数（0/60、0/600 实时更新）+分隔操作栏；视觉判定与暗色主题协调无破损 |
| 3 | 公告发布链路（联测 C-1） | ✅ | ann_publish #13/#14/#15 三连发，移动版 60s 轮询全收到 |

### E. 热更链路（需求⑤）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 热更包含 www 前端 | ✅ | build_backend_zip 产物 zip 内 www/app.js 含 api/activate+heroSwap、index.html 含 v=125、proxy.py 含 activate 转发；www 条目=4 根文件+logo |
| 2 | HotUpdateManager 包校验兼容 | ✅ | REQUIRED_ENTRIES(5)+www/ 前缀在包内均存在 |
| 3 | release_all.py 一键链路 | ✅ | 组成三步（web 构建/部署+热更 zip），脚本就绪；热更 zip（36.7MB）已重建未发布——留给用户走后台上传 |

### F. 环境备注（非本批代码问题）

- MuMu 实例 0 在改 VM 分辨率做竖屏后无法再启动（恢复配置后仍起不来，疑似 .nemu 状态残留）——
  需在 MuMu 主程序里修复/重装实例 0；期间新建实例 1（16416）完成移动版测试。
- 双工程 flavor 已于本日早些时候（v1.19 重规划）退役：构建命令一律 `assembleRelease`
  （`assembleGongceRelease` 会 task not found；管道 `| tail` 会吃掉失败退出码，构建务必看 BUILD
  SUCCESSFUL 字样）。
- 测试周卡 `20260907-weekly-1` 已用于网页激活 E2E（deviceId=web 随机 UUID，不影响 APK 端名额）。

## 2026-09-07 · v1.22：桌面优化需求四项全链路验收（MuMu + 云端 + 浏览器 GUI）

需求来源：`桌面/优化需求.md`。环境：MuMu 实例 0（Android 15，127.0.0.1:16384，app v1.22/
versionCode 24）、本地 admin_server（127.0.0.1:8791）、云端 otv-admin/otv-web、Playwright。

### A. 需求① 影视页电视剧/动漫标签光标下移跳行（MuMu）

**先复现后修复**（旧版 v1.21 抓 logcat 定位，修复后 v1.22 复测）：

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 旧版复现：冷启进电视剧 tab 边走边到货 | ❌→定位 | logcat 12:52:06.782 `GAINED#58 @Rect(836,0..127)` 焦点闪跳到屏幕顶部旧节点后自愈——「最近热门」板块迟到插入致序号 key 全体重排、行 item 重建（`shots/20260907-影视跳行修复/repro*.jpg`） |
| 2 | 修复后：冷启进动漫 tab（全新 cid 板块必边走边到货）16+ 连 DOWN | ✅ | 每步 `GAINED` 均落 `Rect(80,667,312,1019)` 逐行推进零跳行；板块插入瞬间仅 1ms 瞬时闪立即回位同坐标（`shots/20260907-影视跳行修复/fix*.jpg`） |
| 3 | 中屏直搜路径（UP 后再 DOWN）与列内 RIGHT 步行 | ✅ | 直搜单行往返正常；RIGHT 步行曾见零尺寸节点抢焦点即自愈（旧版已知行为，非本批回归） |
| 4 | v1.22 最终版安装冒烟 | ✅ | 影视页正常加载、tab 切换正常、零 FATAL |

### B. 需求② 直播信号源解析速度（MuMu 解密桥直测 + 调试流）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 解密池并行度：真实 qqliveHD666.php 播放器页（wtmdjxkq.com 直取 40KB）打 8091 桥 | ✅ | 串行 3 次 1.34s → 并发 3 次 **0.42s**（3.2×=池大小）；三次均解出真实流 `pulltv.car2link.com/live/481df…` |
| 2 | 增量上屏：999001 调试流（任意 src 命中本地测试源）多源补齐 | ✅ | lines 顺序=完成顺序（qqlive102 落第 2 位而非源表序 bb）→ 逐源到货即 append，非 awaitAll 一次性 |
| 3 | 本地测试流链路（hosts bind-mount testsrc.local→10.0.2.2 + ffmpeg HLS） | ✅ | `engine state → READY（出画面）`、`hb READY playing=true`（testsrc2 活流） |

### C. 需求③ 直播默认记住上次信号源（MuMu 全闭环）

DataStore 种子：`live_last_src=qqlive100`（手工构造 preferences_pb，Value.string=field5——field1
会 `CorruptionException: Value not set` 崩溃，已实测绕开）。

| # | 步骤 | 结果 | 证据（logcat） |
|---|---|---|---|
| 1 | 进直播（有记忆 qqlive100） | ✅ | `bootLive：需求③ 直选上次信号源 qqlive100（bb 兜底）`→ fast-path 起播 READY |
| 2 | 信号源条手动切源（OK→RIGHT→OK） | ✅ | `session start line=1` + `直播源记忆已更新: qqlive102`，READY（出画面） |
| 3 | 退出重进 | ✅ | `需求③ 直选上次信号源 qqlive102` → 1.1s 内 READY |
| 4 | 记忆源不在本场线路表时回退 | 逻辑审查✅ | bootLive preferred 校验 `srcTable.any { it.first == remembered }`；实测现场无比赛（m.html 0 场），该分支留待比赛窗口自然验证 |

残留：真实比赛动态 channels（qqlive15~33/666）多源场景当时段无比赛未自然覆盖——999001 走的是
同一 `srcsFor/streamUrlsIncremental/appendRemainingLiveSrcs` 代码路径，仅数据源不同。

### D. 需求④ 后台热更上传（curl 根因定位 + 本地全链路 + 云端网关 + 浏览器 GUI）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 根因定位：36.6MB zip 直传 | ❀ | HTTP **406 空响应体**（6.4s 即拒）；阈值探测：1/4/6MB→200（函数 JSON 报错=已过网关），8/16/24MB→406 空体 → 网关 body 上限 ~6MB，`r.json()` 解空体报 SyntaxError |
| 2 | >5MB 直传友好拒绝 | ✅ | `{"error": "包体 5MB 超过网关直传上限（~5MB），请使用分片上传"}` |
| 3 | 本地分片全链路（9×4MB + commit） | ✅ | v7 offline 发布，`sha256=27d93154…` 与源 zip 逐字节一致；commit 后 `hu-uploads/{uid}/part-0000` 404=暂存清理生效 |
| 4 | 云端真实网关分片（部署后） | ✅ | 同 9 片 31.9s，v7 offline SHA 一致；otv-admin Timeout=60/环境变量完好 |
| 5 | 浏览器 GUI（Playwright，云端后台） | ✅ | 登录→热更页→手动选 zip（36.6MB）→发布方式=仅上传→「上传所选文件」→ `热更包 v7 已上传（下线态）· 34.9 MB · SHA-256 27d93154…`（`shots/20260907-后台分片上传/`） |
| 6 | 测试数据复位 | ✅ | v7（本地+云端两笔）均 hu_delete 删除，ret=0 |

### E. 三端同步与部署

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | sync_shared 四文件一致（SettingsStore/LiveRepository/StreamDecryptServer/PlayerViewModel） | ✅ | `sync_shared.py` 复检「所有共享文件在已存在的版本间保持一致」 |
| 2 | 双 APK v1.22（versionCode 24） | ✅ | `apk/TV版/MyTV-TV版.apk`（161MB）/ `apk/移动版/MyTV-移动版.apk`（160MB），MuMu 装机 versionName=1.22 |
| 3 | 网页版部署 | ✅ | otv-web updateFunctionCode → `/web/app.js` 200（89848B）与源码 diff 一致、含 `liveLastSrc`×2；www 漂移（竖屏优化）先回灌 backend-src 再叠加 |
| 4 | 移动版 HomeScreen 同类 key 修复 | 逻辑同步✅ | 触摸端无焦点引擎，收益=板块插入时滚动位置不再漂移（未单测） |


## 2026-09-04 · v1.21：网页版云端验收 + 双端 DLNA 投屏冒烟 + 后台改密验证

### A. 网页版（外网 HTTPS 全链路，Playwright 390×844 移动视口 + curl）

入口 `https://appletv-d5ge1bth794873f76.service.tcloudbase.com/web/`（首次访问有平台
「风险提醒」中间页属默认域名策略，点确定即过）。本地开发态（127.0.0.1:8790）与云端两轮验证：

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 首页（hhkan 模式） | ✅ | 本地：9 板块 + 5 轮播，海报/轮播图经 /hhkan/proxy 正常出图 |
| 2 | 首页（云端 mcms 降级） | ✅ | 云端 hhkan 挑战不过 → source=mcms，4 板块×12 + 轮播 5，imgLoaded=true |
| 3 | 影视库分类/筛选/无限滚动 | ✅ | 6 分类签 + 类型/地区/年份 chips + 分页 has_more（mcms 模式筛选条自动隐藏） |
| 4 | 搜索 | ✅ | 「狂飙」→ 20 结果含正主（云端 mcms）；本地 hhkan 模式同验 |
| 5 | 详情/选集 | ✅ | mcms：疯丫头第一季 28 集 + bfzym3u8 线路；hhkan：狂飙无人区之狼 60 集×2 线 |
| 6 | 点播播放（本地 hhkan 源） | ✅ | 播放推进 36s/总143.8s，720×1280，进度/换集正常 |
| 7 | 点播播放（云端 mcms 源） | ✅ | 播放推进 11s/总830s，1920 宽，readyState=4；单分片瞬时 502 被 hls.js 重试吸收 |
| 8 | 电视直播 | ✅ | 7 分组/78 频道；CCTV-12 1280×720 播放推进（本地+云端同验）；线路失效自动切换 |
| 9 | 中继重定向修复 | ✅ | 74.91.26.218:82→81 签名跳转：修复前 too many redirects → 修复后清单+分片全通（分片 1.2MB 200） |
| 10 | 续播/历史 | ✅ | localStorage 进度/历史落盘；首页「继续观看」行回显（含百分比角标） |
| 11 | 后台管理密码 | ✅ | 云端新口令 200 / 旧口令 401；本地文件已同步 |

### B. DLNA 投屏（MuMu 实例0，v1.21 双端 release 包）

模拟器网络无 DLNA 渲染器 → 设备发现空态属预期；投屏 UI 全表面 + 后端绑定实测：
**真机 + 实体电视的发现/投放/控制需现场补测**（`data/cast/DlnaCast.kt` 零依赖实现，
SSDP/SOAP 为标准协议，风险点主要是各电视品牌 SOAP 兼容性）。

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 后端绑定 0.0.0.0:8090 | ✅ | 模拟器 /proc/net/tcp：8090 LISTEN 00000000（投屏前提，v1.20 为 127.0.0.1） |
| 2 | 移动版·点播播放器投屏入口 | ✅ | `hhkan:333674` 调试路由起播 → 控制层左上 Cast 图标 → 设备浮层（扫描 4.5s → 空态文案 + 重扫可用）；截图 `docs/assets/v1.21-投屏浮层-移动版.png` |
| 3 | 移动版·电视页投屏入口 | ✅ | 电视 tab → CCTV-1 起播（engine READY）→ 右上「投屏」chip → 对话框（投屏到设备/未发现/重扫/关闭）；截图 `docs/assets/v1.21-投屏入口/对话框-移动版电视页.png` |
| 4 | TV 版·更多面板投屏区 | ✅ | `hhkan:333674` 起播（00:33 播放中）→ 遥控键路径开更多面板：投屏区（空态 + 重新扫描 chip）+ 换源线路 16 条正常共存；截图 `docs/assets/v1.21-投屏区-TV版更多面板.png` |
| 5 | 双 APK 构建 | ✅ | TV/移动 BUILD SUCCESSFUL（versionCode 23）；DlnaCast 入 classes2.dex；backend zip 含 mcms/OTV_BIND |
| 6 | 共享层一致性 | ✅ | `sync_shared.py` 检查：所有共享文件双端一致 |

边界记录：TV 版电视页（TvScreen 内嵌播放）无投屏入口——TV 端自身即大屏（投屏发送方场景
集中在移动端）；移动版电视页与双端影视/足球播放页均已覆盖。


## 2026-09-04 · v1.20 追加批次：移动版 6 项 UI 优化验收（MuMu 实例0 release 包，竖屏+横屏）

APK：`apk/移动版/MyTV-移动版.apk`（versionCode 22，本批两次构建：初版 → 会员页竖屏修复版，
以下均为修复版实测）。截图证据 `.zcode-tests/{baseline,after,final}/`；激活卡 OTV-QFXHF-TNEPE
（pm clear 后走输码页系统输入法重新激活=第 2 台绑定，`license activated: plan=weekly` 完整链路）。

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| ① | 返回按钮无背景框+贴左上角 | ✅ 全部影视页左上角纯白 ‹ 图标（旧版深色圆角方块已去）；点按 route all→vod（logcat）；详情/搜索/会员/输码页同步换装 | final/req1_all.png + logcat |
| ② | 我的页未开通态=统一会员页 | ✅ pm clear 后我的 tab = 完整会员页（会员/尚未开通/四套餐/二维码/固定底部输码+左上返回钮）；**过程中复现并修复真 bug**——竖屏套餐区横排 Row 溢出（年卡/终身/二维码排屏外，uiautomator 无节点），修复=2×2 FlowRow+二维码居下，复测四卡全可达（月卡[301,448]/季卡[753,448]/年卡[301,955]/终身[753,955]）、滑动二维码可见 | req2_mine_unactivated_v2.png / req2_paywall_scrolled.png |
| ③ | 会员信息一行小字+我的收藏上移 | ✅ 「会员有效 · 周卡 · 到期 2026-09-11 · 剩 6 天」单 text 节点一行（旧三行卡片块已去，无背景框）；我的收藏空态 topPad 400→150 设计px（竖屏实测 y576→y216） | final/req3_mine.png |
| ④ | 电视页标题区去除 | ✅ 「电视直播/选择频道开始观看·共78个频道」节点消失（uiautomator 全文无此文本） | final/req4_tv.png |
| ⑤ | 影视首页轮播上移+chips多行 | ✅ 竖屏 hero 带 700→520 设计px（最近观看 y1042→y784 物理px，上移 25%）；分类 chips FlowRow 两行（电影/电视剧/动漫/综艺 y1011 + 全部› y1123），横屏单行不回归 | final/req5_vod.png |
| ⑥ | 电视选台菜单两列原样式 | ✅ 浏览态=左分组列（央视白底选中）+右频道列（CCTV-1..12 纵向滚动），分组切换卫视→7频道正确；播放态 FAB 呼出侧边栏=分组纵向列（央视 AccentBlue）+频道列（正在播放标记），点 CCTV-5 换台 `switchIptv: CCTV-5体育` 收起正常；BACK 播放→浏览→退出分层正常 | final/req4_tv.png / req6_sidebar.png + logcat |
| — | 激活链路回归 | ✅ 输码页（adb 注码首测误带 OTV- 前缀串码报"卡密不存在"=校验生效；改输 10 位后缀）→ `license activated: plan=weekly` → 自动进足球页 | logcat |

**稳定性修复（P1/P2）验证**：
- UrlGuard 简写拦截：代码级验证（"127.1"/"10.1" 非四段全数字 → numeric shorthand 拦截；
  "1.2.3.4" 公网四段/合法域名不受影响）；直播拉源/换台回归正常（iptv 源命中 latest.m3u
  78 频道，CCTV-1/CCTV-5 起播出画面）。
- 热更 P1：本机构造强更场景成本高（需服务端 forceApply 包），按代码级验证记录——
  check 超时只包元数据（实测 `hotupdate: check 超时` 日志正常放行进入 app），强更下载
  移出超时作用域；正常更新路径无回归（无阻断屏）。
- 后端/片库：冷启动 `startup gate 放行 156ms`、目录快照 256 条、影视 tab 预载 5 分类/144
  封面——解压线程 try 包裹后启动路径无回归。

**构建**：移动版 assembleRelease ✅（两次）；TV 版 assembleRelease ✅（共享层修复同步后）；
`tools/sync_shared.py` 检查全绿（EmbeddedBackend/IptvRepository/VodRepository/UrlGuard 同步，
BackendService/HotUpdateManager 手工双端同改）。

**过程发现**：
- MuMu 竖屏：`settings put system user_rotation 0`（1=ROTATION_90 横屏）；screencap 与
  uiautomator 坐标偶有 stale（旋转后首帧），以 uiautomator dump + 实际 tap 效果为准。
- adb input text 到输码页隐藏 BasicTextField：须只输 10 位后缀（OTV- 前缀是固定展示），
  全码注入会被大写过滤+截断串码。

**TV 版同批补齐验收（adb 注键 D-pad，最终 APK = v1.21(23) 合并构建）**：
| 项 | 结果 | 证据 |
|---|---|---|
| ④ 浏览态标题去除+两列 | ✅ 电视页无「电视直播」标题；左分组列（央视白底选中）+右频道列（CCTV-1..12 纵向），DOWN DOWN hover-select 切卫视（卫视·7个频道），RIGHT 落频道列，OK 起播东方卫视（`switchIptv`+首帧日志） | final/tv_req46_browse.png、tv_nav2_channel.png |
| ⑥ 播放态侧边栏两列 | ✅ 左键呼出：分组纵向列（卫视 AccentBlue）+频道列（东方卫视白聚焦/湖南/浙江/江苏/北京），DOWN+OK 换台大湾区卫视 ✓ | final/tv_sb_fixed.png + logcat |
| P1 焦点重锚修复 | ✅ 修复前：浏览态起播后 LEFT/MENU 呼不出侧边栏（焦点死亡无人重锚，临时日志无 compose 记录佐证）；修复后复测呼出+换台全通 | logcat tvdbg 对照 |
| ③ 我的页一行小字 | ✅「会员有效 · 周卡 · 到期 2026-09-11 · 剩余 6 天」单行无背景框；我的收藏紧随标签栏 | final/tv_mine.png |
| 侧边栏布局缺陷修复 | ✅ 旧代码 Row 内分组行 width(980) 占满、频道列 weight 剩 0 宽（频道列不可见）；两列重写后频道列正常渲染 | 代码对照 + tv_sb_fixed.png |
| 回归 | ✅ 冷启动门闸 102ms 放行、route→live、换台链路正常 | logcat |

**浏览态改版验收（深夜追加：分组顶行 + 频道网格，双端）**：
- TV 版（D-pad）：tab DOWN→chips「央视」白底 ✓；RIGHT 行内移动 hover-select 切「卫视」
  （网格即时刷新 7 频道+回顶）✓；DOWN 进网格首卡东方卫视 → OK 起播（switchIptv+首帧日志）✓；
  4 列网格、纵向滚动 ✓（final/g1_chips.png、g2_hover.png、g3_play.png）
- 移动版（触屏）：横屏 4 列 ✓ 点「卫视」→ 点「东方卫视」起播（switchIptv 日志）✓；
  竖屏 2 列、chips 超宽横滚（「港…」裁切即横滚示意）✓（final/g4_mob_land.png、g5_mob_port.png）
- 播放态侧边栏（两列）不受影响 ✓；分组切换网格回顶 ✓

**搜索首字母联想验收（2026-09-05，TV 版）**：
- 注入 aqgy（otvDebugQuery 调试通道）→ 候选栏「爱情公寓3/4/5/第二季/第一季」5 条 ✓
  （final/py4_aqgy_ok.png）；注入 heysn → 「花儿与少年」5 季 ✓（py5_heysn.png）
- 闭环：DOWN 到候选 → OK 上屏「爱情公寓3」→ query 变中文自动搜索 → 结果区出爱情公寓
  系列 ✓（py6_apply.png）
- 数据：pinyin_titles.txt 1795 行（全站可播快照）；pinyin_initials.txt 6763 字；
  索引构建/匹配 IO+Default 线程，无主线程阻塞
- 电视页选中效果改版（同批）：频道卡聚焦 白底→深底微亮+AccentBlue 2dp 边框+白字
  （白底上白色剪影台标消失=用户报"看不清"根因）；三区光标打通：网格首行 UP/首列 LEFT
  定向回分组行（引擎垂直搜索限定同 navGroup，跨区须 navOverride 显式定向——Focus.kt
  searchGeom 过滤 it.group == group 实锤）、分组行 UP 回 tab、tab DOWN 进分组行 ✓
  （final/fx1_new_focus.png 深底蓝框效果、fx2_up_chips.png 网格 UP 回分组）

**会员行同带+续费验收（2026-09-05，双端）**：
- 移动横屏：会员行 y58-106 与标签胶囊（我的 y62-102）同一行带右对齐；行尾「续费」蓝色下划线 ✓；
  tap 续费 → route→paywall ✓（final/m1_member_line.png）；竖屏 tap 同带区域命中会员行进会员页 ✓
- TV：tab DOWN 定向落会员行（焦点环整行高亮，final/tv_member_focus3.png）→ OK route→paywall ✓；
  有历史记录场景下可达（memberFocus 修复验证）✓
- 随标签栏下潜：移动版滚动内容时会员行与标签栏同步隐藏（chromeGone 动画）✓

**网页版核查（同日另一批次 /web/）**：`/web/` 200、`/web/api/iptv` 78 频道实时返回 ✓；
6 项需求均为原生 App UI 范畴，网页版无对应界面（首页/影视/电视/搜索四 Tab，无我的/会员页、
无浮动返回钮、无电视标题块、电视菜单本就是 chips+列表网页范式），无需改动。

## 2026-09-04 · v1.20 12 项优化全量验收（MuMu 实例0 release 包 + 云端实测）

APK：`apk/TV版/MyTV-TV版.apk`（versionCode 22）+ `apk/移动版/MyTV-移动版.apk`，均 14:19 构建
（含片库 filters 空结果自动重试修复）。激活用卡：OTV-QFXHF-TNEPE（周卡，本批 tools/out 签发）。

**TV 版（D-pad/adb 注键）**：
- ⑤ 电视 tab 光标：影视→电视 hover 切页后光标停在「电视」tab（聚焦胶囊高亮），不自动下移 ✓（tv_tab_test.png）
- ⑩ 未激活我的页：仅「会员」充值界面（四套餐卡+尚未开通+扫码+输码入口），无收藏/历史 ✓（tv_fav_test.png）
- ⑦ 未激活标签可切：会员页 UP 上标签栏 → LEFT×2 → OK → route → live ✓（logcat route 佐证）
- ④ 激活态我的页：右上角紧凑小块「会员有效·周卡·到期2026-09-11·剩6天·卡密兑换›」+ 收藏/历史全宽 ✓
  （tv_fav_activated.png；release 重装后激活保留 ✓ tv_release_fav.png）
- ③ 片库筛选：片库页选「类型=动作」→ 内嵌后端日志 `GET /hhkan/show/0?...&type=%E5%8A%A8%E4%BD%9C` 200 ✓
  （python.stdout 抓取，与无筛选结果集不同）
- ⑧ 公告一次：`announcement: #9 本机已提示过，跳过`（60s 轮询反复确认跳过）✓
- 激活链路：debug 包（release 签名）otvDebugLicense 注码 → `license activated: plan=weekly`
  （云端查表+绑定+验签完整链路，无安全豁免）✓
- IPTV 托管列表：启动即拉 latest（15364 字符）→ 78 频道/128 线（=后台发布产物）✓

**移动版（adb tap/注键）**：
- ① 返回按钮：片库页 tap 左上返回钮 → route → vod ✓（z-order 修复前被 LazyGrid 拦截点不到）；
  输码/会员/详情/搜索同步改为内容后声明
- ② 返回钮样式：深色圆角方块（iOS squircle #B31C1C1E）+ 白粗折角‹，与用户参考图一致 ✓
- ③ 片库筛选：tap「动作」→ `GET /hhkan/show/0?...&type=%E5%8A%A8%E4%BD%9C` 200 ✓；
  **过程中复现并修复真 bug**——filters 拉取失败被 runCatching 吞掉后类型/地区/语言行只剩「全部」
  （用户报「筛选不生效」的形态）；修复=空结果自动重试×4（间隔 2.5s），pm clear 冷启动实测 chips 完整加载 ✓
- ④/⑩ 我的页：未激活=整页充值界面无收藏历史 ✓；激活（系统输入法注码 QFXHFTNEPE →
  `license activated: plan=weekly`）→ 右上小块「会员有效·周卡·到期·剩·续费/兑换›」✓
- ⑦ 未激活会员页标签可点：充值界面/拦截会员页 tap 足球→route live、影视→route vod ✓
- ⑨ 选台 FAB：电视页选 CCTV-1 进播放 → 触屏唤醒 → 左缘 55% 高度圆形深色半透明 FAB + 频道列表图标 ✓
- 输码页左对齐（总体③）：页头/码位/按钮/标语统一左构图 ✓

**后台/云端（curl + 本地实测）**：
- 登录门禁：无 Cookie whoami/stats → 401；错误密码 401；正确登录 → HttpOnly Cookie（12h）→ 全 API 放行 ✓
  （本地 8791 与云端 /admin 双通道验证）
- 云端部署：otv-admin HTTP Web 函数（Python3.9）+ 网关 /admin 外网 HTTPS。
  排障记录：① MCP 直传 zip 丢 scf_bootstrap 可执行位 → 改 tcb CLI COS 通道；② bootstrap 绝对路径
  /var/lang/... 在 3.9 镜像不存在 → 改 PATH python3；③ `X | None` 语法 3.10+ 不兼容 → 移除。
  最终 whoami=401/login ok/stats ok/iptv_latest ok 全通（页面/图标/登录浮层 200）✓
- IPTV 动态源：导入（replace 78 频道/129 线）→ 发布 v1 → 发现分组丢失（export 别名 grp≠group）→ 修复
  → v2 发布（managed=true）→ 回滚 v1→v3 → 回滚 v2→v4（latest=78 频道带分组，生效中）✓；
  每日云函数 manual 守卫：invoke → `{"ret":0,"skipped":true,"reason":"manual mode…（后台托管发布，自动推流暂停）"}` ✓
- 补货：本地 admin 生成周卡 1 张（20260904-weekly-1）→ 入库 → 设备激活成功 ✓（云端未注入私钥，补货仍本地）

## 2026-09-02 · v1.17 公测版移动端移植 + 电视页门控缺口修复（MuMu 实例 0，四包并装）

APK：`apk/公测移动/MyTV-公测移动-debug.apk`（com.qiubo.optimaltv.mobile.public）等四包同机并装。
详细 10 项验收表与过程发现见 `docs/移动版适配.md` §7。要点：

- **公测移动端全链路**：未激活电视页拦截（会员页四卡+尚未开通）→ 注码激活 OTV-E3QUC-8YNDG（云端+验签）
  → 电视页放行出画面 → 我的页「剩余 29 天」；输码页系统输入法（小写自动过滤大写、10 位截断、
  封禁码红字回显）；内测移动零门控（清数据直接起播、无任何会员元素）。
- **双端同修的三个 bug**（公测 TV 也中招，已重发双 flavor）：
  ① 电视页内嵌播放不走 player 路由，未激活可白嫖 IPTV → tv 路由包 LicensePlayerGate + 失效停播；
  ② activate() 只认 OTV- 前缀全码而输码键盘无前缀键 → 归一化宽容 10 位后缀；
  ③ 输码页主线程协程同步 HTTP（NetworkOnMainThreadException 被吞成"网络不可用"）→ callEndpoint 切 IO。
- **工程基建**：两工程原本共用构建目录 zcode-build/optimal-tv，并行构建互相清空中间产物
  （R 假性 Unresolved / KSP 损坏，且毒产物进 Gradle 本地缓存）→ mobile 改独立目录 + --rerun-tasks 重验；
  跨工程构建必须顺序执行。
- sync_shared.py 全绿（新增 license/ 与 PaywallShared.kt 共享项；品牌资产分治不共享）。

## 2026-09-02 · My TV 品牌更名 + 卡密后台管理系统（MuMu 实例1 + 本机）

**品牌更名（Apple TV/OptimalTV → My TV）**：
- 新字标 `tools/gen_app_icon.py` 生成（Segoe UI：My=Semilight + TV=Bold，白字透明底 551×203），一键产出 `drawable/my_tv_logo.png`、`mipmap-{x,xxx}hdpi/ic_launcher_fg.png`、`drawable/ic_banner.png`；旧 `apple_tv_logo.png` 已删除
- 应用名：My TV 内测版 / My TV 公测版（aapt2 badging 核对）；启动屏改为纯字标 300dp + 转圈（MuMu 截图验证）；TV 桌面图标/名称截图验证
- 产物改名：`apk/公测TV/MyTV-公测版-v1.17-release.apk`、`apk/内测TV/MyTV-内测版-v1.17-release.apk`（debug 同前缀）；HomeScreen 描述文案、admin 工具标题同步
- 包名 `com.qiubo.optimaltv`（及 .public/.mobile）**保持不变**——改包名会让存量用户无法覆盖升级且丢收藏/激活数据
- 更名后回归：公测版冷启动 → 门闸放行 → 主界面正常，激活状态保留；移动工程不在本次范围（移植会话按 docs/移动版适配.md §5 同步品牌）

**卡密后台管理系统（本地网页版）**：
- `python tools/admin_server.py` → http://127.0.0.1:8791（只绑本机回环 + 每次启动随机 token 注入页面 + API 须带 X-OTV-Admin 头，无 token 请求 403 实测）
- 四个页面：总览（各套餐 总数/未激活/已封禁 + 24h 激活动作）、卡密（搜索/筛选/封禁/解封/解绑单设备/解绑全部/复制/分页）、激活日志（按码过滤）、补货（本机私钥签发→自动入库→给出 cards.csv 路径）
- 云端新增 5 个管理 RPC（admin_stats / admin_licenses / admin_set_status / admin_unbind / admin_logs，全部 SECURITY DEFINER + svc_import uid 白名单）；封禁↔解封往返、单设备解绑、审计日志（admin_banned/admin_active/unbind）实测全通
- 补货全链路实测：后台生成 monthly×1（批次 20260902-monthly-1 覆盖写入，新码 OTV-56MMR-UK8W6 已入库）→ cards.csv 交付路径返回正常
- 安全边界：私钥永不出本机（补货签名在本地 subprocess 完成）；云端凭据仅 svc_import（管理员账号），activate 端点用的 svc_activate 无管理 RPC 执行权
- Playwright 实开页面点验：总览五卡数据正确、卡密表 50 行/页 + 操作按钮齐全

## 2026-09-02 · v1.17 卡密付费公测版/内测版 双版本验收（MuMu 实例1 + CloudBase 线上）

环境：`apk/OptimalTV-公测版-debug.apk`（com.qiubo.optimaltv.public）+ `apk/OptimalTV-内测版-debug.apk`（com.qiubo.optimaltv）并装；激活端点 `https://appletv-d5ge1bth794873f76.service.tcloudbase.com/activate`；方案验收清单（方案文档 §8.1）10/10 通过。

| # | 验收项（§8.1） | 结果 | 证据 |
|---|---|---|---|
| 1 | 未激活任何播放入口拦截 | ✅ 直达 `iptv:` 播放 → 会员页（四卡+二维码+激活按钮），route 不进 player | uiautomator 文本：会员/尚未开通/月卡¥9.9…/输入卡密激活 |
| 2 | 有效码激活、到期=激活+days | ✅ `license activated: plan=monthly`，我的页「剩余 29 天」 | OTV 日志 + 我的页文本 |
| 3 | 同码同设备幂等 | ✅ 云端 rebind（activate_log result=rebind ret 0）；App 端同码保持到期不叠加 | 云端审计日志 |
| 4 | 第3台设备 bound_full | ✅ 已满2台的码在 MuMu 激活 →「该卡密绑定设备数已满（一码 2 台）」 | OTV 日志 |
| 5 | 断网冷启动离线可用 | ✅ 飞行模式 force-stop+冷启动：`startup gate 放行(iptv=79 频道)` + 直达播放 route→player（门控放行，授权从 DataStore 离线加载） | OTV 日志（离线窗口期内） |
| 6 | banned 拒绝+降级 | ✅ 新激活→「该卡密已被封禁」；已激活联网重启→`license banned by server → downgrade`→我的页「未开通」 | OTV 日志 |
| 7 | 回拨>48h 冻结计时 | ✅ adb root `date` 回拨60天冷启动：剩余 29→30 天（不虚增至89天）；顺带实测回拨下 HTTPS 激活因证书失效被拒（合理防护） | 我的页文本对照 |
| 8 | 伪造票据本地验签拒绝 | ✅ 篡改云端 ticket.sig → 云端照常返回 → App「票据校验失败（E2）」拒绝（云端被攻破也伪造不了授权） | OTV 日志 |
| 9 | 续费叠加 | ✅ 月卡剩余29天+季卡90天 =「剩余 119 天」（max(now,expiry)+days） | 我的页文本 |
| 10 | 焦点/遥控全流程 | ✅ 会员页四卡初始焦点+QR 联动；输码页三行键盘 dpad 输入（OTV-ABD__-_____ 实时刷新）、行间导航、⌫/清空 | uiautomator + 截图 |

内测版对照（防混淆）：
- 包名/应用名：`com.qiubo.optimaltv`「Apple TV 内测版」vs `com.qiubo.optimaltv.public`「Apple TV 公测版」（aapt2 badging 核对）；并装互不影响。
- 内测版未激活任何码直接播放 CCTV-13：`engine 首帧已渲染 → READY，playing=true`（零门控）。
- 内测版我的页无「会员与激活」入口、无续费角标。

云端侧联调（curl/MCP）：
- activate 端点：ok/rebind/bound_full/not_found/banned/bad_code 全路径正确；审计 activate_log 各 result 逐条对上。
- 255 码入库全量 md5（code+sig 聚合）与本地一致。
- 导入通道：`tools/db_import.py`（svc_import signin）重导入可修复票据（on conflict do update）。

过程事故与恢复（重要经验）：
- `adb shell cmd connectivity airplane-mode enable` 会把 MuMu adb 桥**永久卡死**（实例重启无效）；**解药：`MuMuManager.exe sh -v <idx> --cmd "cmd connectivity airplane-mode disable"`**（管理器 shell 通道不依赖 adb 桥），已写入记忆。
- 发布密钥/API Key 在 PG HTTP API 网关被 INVALID_CREDENTIALS 拒绝（平台侧）；signin access_token 路径稳定——架构已按此落地（方案 §11.1）。
- 长密钥人工转录两次出错（JWT、ECDSA 公钥各丢失/错字符），均被「双誊写 diff + 解码字段校验/PEM 比对」确定性拦截——长 token 必须走本地文件管道，不进人工复制。

遗留：
- M5（用户操作）：爱发电/面包多上架、导入 cards.csv 库存、替换 App 内 `PurchaseLinks`（现为 afdian.com 占位）。
- R8 混淆未开（Chaquopy 反射风险，已知取舍）。
- 同设备并装两版时本地 8090/8091 端口先启动者独占（测试时 force-stop 另一版即可）。

## 2026-09-02 · v1.17 台标全真实+九项修复（MuMu 实例 0，宿主 adb 逐键 + 焦点日志 + 截图）

APK：debug（versionCode 18）全量验收 + release 冒烟（卸载重装）· 方法：logcat -s OTV/OptimalTV + 截图

| # | 项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 台标 100% 真实 | ✅ 78/78 频道命中资产表（60 张 PNG 全有效，同品牌共享）；CCTV-2/4/6/7 等由字母占位变真实台标 | 截图 + assets/logos/index.json 校验脚本 |
| 2 | 侧边栏半透明 | ✅ 80%→60% 渐变，视频透出、文字可读 | 电视页截图（诗词大会画面透出） |
| 3 | N线角标移除 | ✅ 频道行右侧仅「正在播放」 | 截图 |
| 4 | 刷新按钮移除→自动重试 | ✅ 分组列尾无按钮；失败路径 `scheduleAutoRetry` 60s 起指数退避（代码走查+空态提示文案） | 代码 + 空态文案 |
| 5 | 分组列 UP 修复 | ✅ 央视 DOWN 卫视 UP 回央视；美国/英国组可自由上下（修复前 UP 整列封锁） | 焦点日志 120→200→120 |
| 6 | BACK 分层 | ✅ 频道列 BACK→分组列 chip(46,120)；分组列 BACK→关侧边栏(sink)；画面态双击→route live | 焦点/route 日志 |
| 7 | 启动门闸 | ✅ 首装启动屏（Apple TV 标+转圈）→`startup gate 放行(iptv=79 频道)`→主界面即有数据；45s 兜底超时 | 截图 + OTV 日志 |
| 8 | 历史卡灰底角标 | ✅ 「上次看到 第1集 00:09」灰底角标入封面右下角，卡下副标题删除 | 影视页截图 |
| 9 | IPTV 大缓冲 | ✅ media3 45s/150s、VLC caching 6s；美英 ABC 源实测供给不足（源/网络现实），备用轮换自愈不死锁 | hb 日志 + 已知限制说明 |
| 10 | 影视首页分类记忆 | ✅ 选电视剧/动漫→切走→回来：hero DOWN 直落选中 chip(214/376,880)，内容区同分类 | 焦点日志 ×2 轮 |
| 11 | 分类 chip OK 制 | ✅ autoSelect 移除：光标路过不再切分类/覆盖记忆（误切根因修复） | 行为对比 |

### 过程发现并修复
1. **IptvRepository 启动崩溃**：`logoAssets by lazy` 声明在 init 块之后，快照恢复路径先访问 → NPE；改方法级懒加载
2. **启动屏崩溃**：`painterResource(R.mipmap.ic_launcher)` 不支持自适应图标 → 改 drawable/apple_tv_logo.png
3. **影视首页分类记忆失效根因**：hero DOWN 几何落第一个 chip + hover-select 立即切换并覆盖偏好；修复=DOWN 定向选中 chip + autoSelect 改 OK 制
4. **台标抓取工程化**：gh-proxy/wsrv 限流随机失败 → 重试退避 + 跨次运行缓存收敛；wikimedia 缩略图必须用 iptv-org 原始 960px URL
5. **环境**：weread 遗留 app 卸载（disable 后旧任务仍被拉起）；MuMu serial 漂移导致卸载打到另一实例

# 测试记录 — 优化版 MuMu 全流程验收

## 2026-09-01 · v1.16 直播源精编整合+多源备份+四页修复（MuMu 实例 0，宿主 adb 逐键 + 焦点日志 + 截图）

APK：debug（versionCode 17）全量验收 + release 冒烟（卸载重装）· 方法：logcat -s OTV/OptimalTV + 截图像素/内容核验

### 电视页（精编直播源整合）
| # | 项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 精编名单与分组 | ✅ 7 分组（央视/卫视/体育/地方/数字付费/港澳台/美国-英国）79 频道，央视 26 | 截图 + `iptv 内置精编表上屏 79 频道` |
| 2 | 首装零网络兜底 | ✅ 卸载重装（无快照/无历史）秒开完整名单并即播 CCTV-1 | release 冒烟截图（开学第一课画面） |
| 3 | 上游自动补源 | ✅ gh-proxy 命中 iptv4.m3u → 合并 `79 频道 / 备用线路 57 条（安全剔除 1）`，台标随补源上屏 | OTV 日志 + 频道行「2线/4线」角标截图 |
| 4 | 快照恢复 | ✅ 二次启动 `iptv 快照恢复上屏 79 频道` | OTV 日志 |
| 5 | 进入即播+上次频道 | ✅ 换台后重进电视页自动恢复 CCTV-3综艺（`进入即播…·上次频道`） | OTV 日志 |
| 6 | 两级选台 | ✅ 分组列 hover-select（卫视→体育，右列实时刷新）→ 频道列 OK 换台 广东体育→播放 | 焦点日志 + 首帧日志 |
| 7 | 画面态换台环形 | ✅ OK 后 DOWN 跳下一台（广东体育→精品体育），UP/DOWN 全表环形 | OTV `switchIptv` 序列 |
| 8 | 无感换台 | ✅ 复用引擎 prepare 换源，换台 0.4-4s 出新首帧，无黑屏闪烁（视频层 key=engineEpoch，仅 VLC 回退重建） | `engine 首帧已渲染` 时间线 |
| 9 | 双击 BACK 回直播广场 | ✅ 单击提示+呼出侧边栏，2s 内二击 `route → live` | OTV 日志 + 截图 |
| 10 | 播放稳定性 | ✅ hb READY playing=true pos 18→38s 推进，buf 120s 锯齿回填，dropped=0 rbuf=1 | hb 日志 |
| 11 | 坏源形态说明 | ⚠️ 精品体育（epg.pw 线）上游为「NO SIGNAL」占位画面流（流活、内容占位）——源内容状态，非 app 缺陷；备用线路轮换+每周测活重建兜底 | 截图 + hb 正常 |

### 足球页（数据修复+中超）
| # | 项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 有数据 | ✅ 今日比赛 3 场（足协杯）+ 明日 1 场；比分/分钟实时（18'→31'） | 截图 ×2 |
| 2 | 防死页三级回退 | ✅ 白名单零命中日 `主流白名单零命中 → 回退全部足球 60 场兜底` 日志实锤（修复前该形态=空页+光标无落点） | OTV 日志 |
| 3 | 中超通道 | ✅ 中超+足协杯入白名单/重要/hero 权重；中超 chip 实测过滤后埃及超消失、仅剩中超系 | 修复前后截图对比 |
| 4 | 空态可交互 | ✅ 空数据页「重新获取」可聚焦按钮（InitialFocusEffect） | 代码路径（当日未复现全空） |

### 影视页（全部影视：类别记忆+焦点回归）
| # | 项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 类别记忆 | ✅ 选「电影」→退出→重进：焦点/选中都在「电影」chip（244,52,308,106），不回「全部」 | 焦点日志 + 截图 |
| 2 | 落焦竞态修复 | ✅ 初始落焦先等 catLoaded（修复前焦点抢先落「全部」chip 的实测竞态） | 修复前后行为对比 |
| 3 | 筛选区逐行遍历 | ✅ DOWN×5/UP×5 严格单行往返 y52→115→178→241→304→367→…→52，零跳行零乱跑 | 焦点日志逐条 |
| 4 | 筛选↔卡区过渡 | ✅ 排序行↔首行卡往返单跳；行内左右单跳 | 焦点日志 |
| 5 | BACK 分层 | ✅ 卡区 BACK→滚回筛选区；筛选区 BACK→回影视页（返回落焦 hero 钮） | route 日志 + 焦点日志 |

### 全站（顶栏自动隐藏）
| # | 项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 下移隐藏 | ✅ tab→DOWN（hero 钮 top500>300）`chrome hide=true`，顶栏淡出 | 焦点/chrome 日志 + 截图（无顶栏） |
| 2 | 回顶显示 | ✅ UP 回 tab `chrome hide=false (top=0)`，顶栏淡入且足球 tab 聚焦 | 日志 + 截图 |

### 过程发现并修复
1. **AllScreen 初始落焦竞态**：catChipFocus 在 prefs 载入前未挂载 → 落「全部」chip；修复=落焦循环等待 catLoaded（≤3s）
2. **足协杯被白名单误杀**：当日 3 场中超球队比赛（足协杯）全被过滤 → 三级回退第③级兜底混入埃及超；修复=足协杯/中超正式入白名单+「重要」+hero 权重
3. **环境**：MuMu 实例 0 遗留 weread.tv 反复抢前台（disable 后仍被拉起）→ 卸载；MuMu 已知坑：task-to-front 后 input 失效（force-stop 重启恢复）、logcat 主缓冲仅 256K 被 audit 刷爆（logcat -G 4M）


## 2026-09-01 · v1.15 电视页「进入即播 + 侧边栏两级选台」改版（MuMu 实测，实例 0）

### 验收标准逐项证据

**a) 进入电视 tab 直接起播上次频道（截图非黑屏）** —
- 首次使用（无历史）：卸载重装后遥控 RIGHT×2 进电视 tab，日志
  `tv 进入即播: CCTV3（央视频道）· 无历史默认` → `首帧已渲染` → `READY`；
  截图：CCTV3《星光大道》演出画面清晰出画，侧边栏焦点环落 CCTV3 行 +「正在播放」标记。
- 有历史：force-stop 重启后进电视 tab，日志
  `tv 进入即播: CCTV5（央视频道）· 上次频道`；截图：CCTV5《美网第1轮》网球直播出画。
- 多次换台后重启：恢复「风云剧场/东南卫视」（最后一次观看），日志与截图双证。

**b) 播放界面无进度条/无播放暂停菜单** — 各状态截图核验：画面态全屏视频仅顶部 tab 浮层；
侧边栏态为分组/频道面板浮层。全页无进度条、无播放/暂停按钮、无 seek 控件（IPTV 无 seek 语义，
TvScreen 不组合 VodPlayerOverlay/LiveSourcePicker，纯直播结构）。

**c) 侧边栏两级选台（≥2 次换台成功）** —
| # | 路径 | 换台结果 | 证据 |
| --- | --- | --- | --- |
| 1 | 频道列 DOWN→OK（CCTV5→CCTV8） | CCTV8 电视剧出画 | 截图 + `switchIptv: CCTV8` |
| 2 | 分组列 DOWN（autoSelect 切「卫视频道」，右列实时刷新 25 频道）→ RIGHT → 选东方卫视 OK | 东方卫视综艺出画（台标+「直播」角标） | 截图 |
| 3 | 画面态 UP 直接换台（增强交互）×2 | 换台生效（含一个上游占位流频道） | 日志 `iptv switchIptv` 连续 |
- 附带验证：呼出侧边栏自动定位——分组切到播放分组、列表滚到播放行、焦点环+「正在播放」
  落焦该行（CCTV5+/东南卫视/风云剧场 三次呼出定位全部命中）。

**d) 退出重进恢复上次频道** — force-stop → relaunch → 进电视 tab：
`tv 进入即播: CCTV5+（央视频道）· 上次频道` / `tv 进入即播: CCTV5（央视频道）· 上次频道`
（两次不同历史分别命中）；截图侧边栏「正在播放」标记与画面一致。debug→release 覆盖安装
后记忆仍保留（DataStore 持久化跨包生效）。

**e) 四向导航闭环无死焦（焦点日志逐键核验）** —
1. tab DOWN → 频道列首行（contentDownFocus 定向，GAINED @346,274）
2. 频道行 DOWN/UP → 列内几何+滚动兜底（`tv focus: CCTV5/CCTV8` 连续）
3. 频道行 LEFT → 回分组列选中 chip（GAINED @46,274 定向回栏）
4. 分组 chip RIGHT → 定向进频道列（GAINED @346,274）
5. 分组列滚到底 DOWN → 「刷新直播源」chip 可达（navGroup 归组，无跨列斜跳）
6. 频道列到顶 UP → 回「电视」tab（截图：tab 焦点胶囊出现）
7. tab DOWN → 回频道列（截图：焦点环回东南卫视行，往返闭环）
8. OK 选台 → 侧边栏收起 → 焦点回 navSink 键宿（GAINED(sink) 持续，画面态）
9. 画面态 LEFT/OK/MENU → 呼出侧边栏；BACK → 收起回 sink
两列 navGroup 隔离（tv-groups/tv-channels），跨栏全走定向；全程无死焦、无斜跳、无焦点丢失。

**f) 断网/源失效快照回退** — 按 v1.13 结论沿用（禁用 svc data disable/飞行模式防 adb 通道
报废）：冷启动日志 `iptv 快照恢复上屏 351 频道 / 11 分组`（磁盘快照先上屏，侧边栏即刻可渲染），
拉取失败保留旧列表代码路径与 v1.13 外网异常期实测一致；本轮每次冷启动快照均生效。

### 本轮换台健壮性实测（验收项 6 的真实故障演练）

- **media3 解码挂死黑屏（MuMu 已知形态首次在 IPTV 换台复现）**：CCTV8/CCTV5+ 换台后
  media3 `READY playing=true` 但无「首帧已渲染」→ 黑屏假播放（无错误回调，三段回退够不着）。
  **v1.15 新增首帧看门狗**实测日志：
  ```
  首帧看门狗：READY 15s 无首帧渲染（解码器挂死形态）→ 引擎回退/失败链
  → 切 libVLC 兜底引擎（自带软解）
  session start line=0 engine=vlc …
  vlc playing（画面输出中） → vlc vout established（视频输出建立）
  ```
  切换后画面正常出画（截图：CCTV8 电视剧）。硬解→软解全程无需用户干预。
- **死流自动跳台**：风云剧场上游流中断 → VM 20s 起播看门狗 → 同线重试耗尽 → 错误态
  （TvScreen 轻提示错误面板）→ 页面层 12s 自动跳台接管 → 自动换到下一台并稳定播放
  （`hb vlc READY playing=true pos=61s→71s→221s` 位置持续推进）。全程无黑屏卡死、无崩溃。
- **上游占位流辨识**：部分频道当前时段上游返回「由于播出安排暂不支持播放该时段内容」占位
  画面（流本身存活、有画面输出）——属公开聚合源的源内容状态，非应用故障，应用不干预。

### 开发过程中发现并修复的问题
- **换台渲染层崩溃（实测复现）**：换台时 key 变化重建 AndroidView，factory 返回的仍是同一
  引擎实例的 renderView，旧 AndroidViewHolder 未移除时新节点 addView 同一 View →
  `IllegalStateException: The specified child already has a parent` FATAL。修复：factory 挂载前
  先 `(view.parent as? ViewGroup)?.removeView(view)`（切 libVLC 新实例场景为 no-op）。复测换台
  连续 10+ 次零崩溃。
- **侧边栏跨列斜跳隐患（代码审查发现）**：两列未设 navGroup 时 OtvNav 几何搜索 group=null
  会跨列找候选（分组列滚到底 DOWN 可能跳频道列底行）。修复：tv-groups/tv-channels 两组隔离，
  跨栏移动全走 navOverride 定向。
- **侧边栏「刷新直播源」不可达**：floating=true 使其不参与垂直候选。修复：归入分组列 navGroup。
- **上游占位流黑屏假播放**：见上「首帧看门狗」。

## 2026-09-01 · v1.13 「电视」标签页 + IPTV 直播（MuMu 实测，实例 0/1/4 轮换）

### 验收标准逐项证据

**R1 电视 tab 位置** — debug 构建冷启动截图（vision 核验）：顶部标签栏 足球/影视/**电视**/我的
顺序正确，「电视」紧随「影视」；进入电视页后 tab 高亮正确。

**R2 列表自动刷新** — otv.log 实录：
```
iptv 源命中：https://gh-proxy.com/raw.githubusercontent.com/vbskycn/iptv/master/tv/iptv4.m3u（77438 字符）
iptv 拉取成功：425 频道 / 12 分组（安全剔除 0）
```
多源回退（gh-proxy 优先）、EXTM3U 解析、分组排序全部工作；节流逻辑验证：外网异常期
（距上次成功 13min）refreshIfStale 正确跳过未发无效请求。

**R3 原生播放零 WebView** — 代码审查：bootIptv → EngineRegistry（Media3/libVLC），无任何
WebView 参与（8091 解密桥仅足球影视源使用）；日志 `startSession line=0 engine=media3`。

**R4 Apple TV 样式一致** — 截图 vision 核验：深色背景、左栏白底黑字分组胶囊、右栏深色
圆角频道卡 + 白描边焦点环 + 台标图，与既有页设计语言一致。

**R5 频道实播 ≥2-3 个**（am start 直达通道，日志+截图双证据）：
| 频道 | 路径 | 起播 | 证据 |
| --- | --- | --- | --- |
| CCTV1 综合 | 字面 IP http | 5.6s | `READY line=0`+`视频轨道 1280x720`+`首帧已渲染`；截图：央视新闻演播室+主播+「CCTV-1 综合」台标 |
| 东南卫视 | 域名 live.zohi.tv（含 DNS 校验链） | 0.55s | `首帧已渲染`+`视频轨道 1920x1080`；截图：综艺节目舞台+「东南卫视」台标 |
| CCTV5+ 体育 | 域名 myip.pdtvhd.com | 0.8s | `首帧已渲染`+`视频轨道 1920x1080`；截图：赛事画面+CCTV5 标识 |
三频道心跳 `playing=true dropped=0`（buf 14~29s），零丢帧。

**R6 断网/源失效回退缓存** — 证据一：多次冷启动 `iptv 快照恢复上屏 425 频道 / 12 分组`
（磁盘快照秒开）；证据二：实例 1 外网异常窗口（SntpClient 全超时、gh-proxy 不可达）
持续 20+ 分钟，app 反复 am start/force-stop 零 crash，快照列表正常展示。
（注：「拉取失败保留旧列表」分支未单独取证——MuMu 上 svc data disable / airplane-mode
会同时切断 adb shell 通道（平台特性，实例报废两台），按代码路径与外网异常期行为佐证。）

**R7 URL 安全校验** — am start 注入恶意地址，otv.log 实录（未交给播放器，无 startSession）：
```
iptv UrlGuard 拒绝起播（字符串级）：private 192.168/16 url=http://192.168.1.1:8080/live/test.m3u8
iptv UrlGuard 拒绝起播（字符串级）：private 10/8 url=http://10.0.0.5/stream.m3u8
iptv UrlGuard 拒绝起播（字符串级）：scheme ftp not allowed url=ftp://74.91.26.218:82/live/x.m3u8
```
另实测 127.0.0.1 环回同类拦截；解析列表 425 条真实源全部放行（安全剔除 0，无误杀）。

**R8 四向导航焦点闭环** — 实例 0 宿主 adb 逐键注入，焦点日志逐键核验：
1. tab DOWN → 央视分组 chip（GAINED @60,294）
2. chip DOWN → 卫视 chip；3. UP → 央视 chip（列内双向）
4. chip RIGHT → 网格首卡 CCTV1（`tv focus: CCTV1`，定向跳转）
5-6. 网格 RIGHT → CCTV3 → CCTV5（同行 moveGeom）
7. 网格 DOWN → 下行（x 最近）
8-9. 网格 LEFT → CCTV13 → CCTV8
10. 首列 LEFT → 回左栏央视 chip（GAINED @60,294，定向回栏）
11. 内容到顶 UP → 回「电视」tab（GAINED @905,54）
12. tab DOWN → 回央视 chip（闭环）
深滚：DOWN×8 至第 5 行（风云剧场/中国交通），UP×12 逐行回 CCTV1（滚动兜底+回收行重组）
全程无死焦、无斜跳、无焦点丢失。截图：焦点环完整贴 CCTV1 卡边缘。

### 开发过程中发现并修复的问题
- **bootIptv URL 解析笔误**：vodId 无「|」分隔（am start 直达形式）时 url 误取空串 →
  静默 fatal「播放地址缺失」（该分支无日志，表象酷似挂死，配合 vision 幻觉误导排查良久）。
  修复：无分隔时整串即 URL + 入口日志 `bootIptv 入口`。
- **player→player 直切互锁**：debug-nav 连续直达两频道时旧 VM 引擎释放与新 destination
  组合互锁（组合冻结、Handler 层照常）。修复：navigate 前检测已在 player 则先 popBackStack。

### 环境备注
- 并行会话同日进行 OtvNav 重构 + 播放卡顿修复（v1.13/v1.14），源码多轮合流构建；
  本功能全部验收基于最终合流产物（apk/OptimalTV-v1.14-release.apk 同源 debug 包）。
- MuMu adb 安装 APK 路径不能含空格（D:/temp 无空格路径安装正常）；svc data disable /
  airplane-mode enable 会切断 MuMu adb shell 通道且重启不恢复（平台缺陷，勿用）。

## 2026-09-01 · v1.13 光标移动彻底重构（OtvNav 集中式导航引擎，MuMu 多轮遥控模拟实测）

### 需求（用户，2026-09-01）
投影仪上遥控器操控光标特别不流畅、经常乱跑、有些地方去不了——彻底重构光标移动逻辑，
严格模拟遥控器上下左右/返回/确认多轮测试；参考 GitHub 成熟方案；顺带更新文档、整理项目。

### 方案调研
- BBC [lrud-spatial](https://github.com/bbc/lrud-spatial)：方向候选打分（direct/adjacent 距离、
  30% 重叠并入）——借鉴其「方向候选 + 重叠层」模型；
- W3C Spatial Navigation / Mozilla SpatialNavigator（Firefox TV）：主轴边距 + 正交中心偏移打分；
- Compose 官方 moveFocus / focusRestorer：仍有「请求方向无候选时兜底选任意节点」的斜跳问题
  （本工程 v1.4 实测根因），不可直接依赖。
- 结论：**自研集中式引擎**（单点分发 + 确定性几何 + 精确滚动找回），Compose 焦点系统
  仅保留 focusable/requestFocus 基建，方向键在其到达前全部拦截。

### 架构（Focus.kt 重写，全站 8 屏迁移）
| 旧（v1.4~v1.12 补丁堆叠） | 新（v1.13 OtvNav） |
|---|---|
| focusRow/crossRowNav/scrollRowNav/各屏 onPreviewKeyEvent/FocusBus 五层分散处理 | MainActivity.dispatchKeyEvent 四方向键单点交 OtvNav.dispatch，处理不了也消费 |
| Compose 兜底几何搜索斜跳（乱跑根因） | Compose 收不到方向键，斜跳物理性消失 |
| 层容差每屏调魔法数（60/100px） | 自缩放层容差 = 0.5×两者较小高度（下限 8px） |
| 目标行未组合：animateScrollBy+delay 轮询 4 次 | 瞬时滚动「精确露出步长」（先补齐被裁行）+ 一帧后重试 |
| 长按 repeat 一律丢弃 | 110ms 节流 + 滚动防重入，按住连续移动 |
| 特殊跳转散落各屏按键回调 | 节点 navOverride / 区域 LocalNavOverride 声明式，引擎统一调用 |

### 实测发现并修复的引擎缺陷（共 7 个）
| # | 症状 | 根因 | 修复 |
|---|---|---|---|
| 1 | 比赛卡 DOWN 直接过 app 崩溃（FATAL） | 引擎协程 withFrameNanos 缺 MonotonicFrameClock | delay(50) 等帧 + CoroutineExceptionHandler（引擎异常只降级不崩） |
| 2 | 滚动找回一次跳 700px（跳行） | 步长取 item0→1 间距（≈hero 高） | 先补齐视口内被裁剪行（overshoot+1），完整可见才退回行距估计 |
| 3 | 屏底只露 38px 的卡聚焦后环被裁 | ensureVisible 用 Resources.getSystem() 在 ROTATION_90 得自然方向高 1920 | MainActivity 注入 WindowMetrics 窗口高（onConfigurationChanged 同步） |
| 4 | 页面切换后首键 DOWN 在搜索 chip 上无效 | 自愈路径跳过 override 检查 | heal 返回节点后继续走 override |
| 5 | 空收藏页自愈直接跳进搜索页（劫持） | 就近恢复落到 autoSelect 节点（chip）触发跳页 | 自愈候选排除 autoSelect（方向搜索与就近恢复都排除） |
| 6 | 视频播放中方向键全部失灵（每键 self-heal） | navSink(1dp) 未挂 onFocusEvent 且尺寸<2px 判无效 | navSink 补焦点登记；隐藏键宿不受几何尺寸校验 |
| 7 | **tab 切回影视页，光标自动下移到「立即播放」**（用户报障 2026-09-01） | ReturnFocus 记忆只写不消费：曾在影视页聚焦过 hero（按一次 DOWN 即写入 mark("vod","hero")），tab 切走再切回时被当「返回」场景恢复到该钮（logcat 实锤：tab 落焦 ~190ms 后自动 GAINED hero） | ①`take()` 改消费式读取（remove，一次性）；②`navigateToTab` 进入前 `clearAll()`（tab 切换=新进入，一律落 tab）；③搜索 chip 同理（搜索页一律落键盘）。回归：BACK 从详情返回仍回原卡（需求 影视#6-2 不受影响） |

### 缺陷#7 复现与修复验证（MuMu 实例2 adb 127.0.0.1:16448）
- 复现序列：影视tab→DOWN hero（写记忆）→UP回tab→RIGHT 电视tab→LEFT 影视tab
  → 修复前：tab 落焦后 ~190ms 自动 GAINED hero(45,255)；修复后：稳定停 tab(396,27)
- 保留验证：DOWN hero→OK 详情→BACK → GAINED 回 hero（返回落焦本意正常）

### 逐屏验收（MuMu 实例0 adb 127.0.0.1:16384，versionCode 14，逐键独立注入 0.45~0.8s）
| 屏 | 序列（日志逐键核对 GAINED 坐标） | 结果 |
|---|---|---|
| 足球 | tab→DOWN hero→DOWN 筛选chip→DOWN 比赛卡→RIGHT×4→LEFT×2→UP hero→UP tab→UP 封锁 | 全对，零乱跳零自愈 |
| 影视 | DOWN hero→DOWN chips→RIGHT×3 切分类→DOWN 网格逐行×7（一键一行，落点恒 y667）→LEFT 行首封锁×7→UP 回顶→逃逸 tab | 全对；今日仅 7 场时下方封锁正确 |
| 搜索 | 键盘 's'（方向+OK 输入）→DOWN 候选→OK 上屏→DOWN 分类 tab→DOWN 结果网格→RIGHT→UP tab→UP 键盘→UP 搜索 chip | 全对；键盘焦点守卫（重组抢焦恢复）工作正常 |
| 全部 | 6 行筛选逐行 DOWN→海报墙→RIGHT×2→UP 逐行回顶 | 全对 |
| 我的 | 空页（无收藏/历史）DOWN/UP | 无劫持、无跳页（缺陷⑤修复后回归） |
| 电视 IPTV | tab DOWN→分组列→RIGHT 网格首卡→RIGHT×2→DOWN×3 同列→LEFT×3 回选中分组→DOWN 列内 | 全对 |
| 详情 | 立即播放→DOWN HD中字→RIGHT HD国语→UP 收藏→LEFT 立即播放→DOWN 选集→BACK 返回原卡（返回落焦） | 全对 |
| 播放器 | 隐藏态 UP 唤出/LEFT RIGHT seek→控制条 UP 更多/DOWN 回→MENU 浮窗组内 DOWN×3 到底封锁/UP×3 到顶封锁→BACK 关浮窗→双 BACK 退出 | 全对（组隔离生效） |
| 直播源条 | OK 呼出→焦点当前源 chip→四方向封锁（单线路数据态）→超时回根键宿 | 全对 |
| 终局 | 冷启动足球 12 键 10 落焦零异常日志；影视 19 键 16 落焦仅正常到底封锁 | 通过 |

### 项目整理
- 删除 tools/backend-src/__pycache__；focus_drive.sh 串口改环境变量参数化（SER=…）；
  新增 tools/brace_check.py（大括号配平校验，批量改 Kotlin 后用）；
- 目录结构复核：android/（工程）、apk/（最新 debug+release）、docs/（README/测试记录/需求）、
  tools/（后端源码+实测脚本），无备份/临时文件残留。

## 2026-08-31 · v1.12 五项追加优化（MuMu 实测）

### 需求（用户追加，2026-08-31）
1. 角标字号再大点（在 v1.11 的 16 基础上）
2. 播放器浮窗不是去掉选集——**电影才去掉**，电视剧/动漫/短剧要保留
3. 「全部」页占满全屏，不要最上方的标签栏
4. 「全部」页很多卡片封面图片没有，特别是筛选之后
5. 播放器暂停/播放按钮显示的和实际播放状态有时不一致，特别是按快了

### 根因与修复
| # | 根因 | 修复 |
|---|---|---|
| 1 | 字号 16 仍不够大 | 16→20（行高=字号不变，五处统一） |
| 2 | v1.11 把选集区无差别移除 | PlayerUiState 新增 isMovie；boot() 按 目录 categoryId → classifyVod 启发式 → 「2~4 集且集名无集/期/话（HD中字/蓝光国语等版本名）按电影」三级判定；MorePanel 仅 `!isMovie && epCount>1` 渲染选集，onPickEpisode 恢复 |
| 3 | 全部页渲染了纯展示 MainTabBar + contentPadding top 180 | 移除 MainTabBar，top 180→36 |
| 4 | **logcat 实锤**：每张冷图「首请求 502、1ms 后重试即 200」——源站 CDN（enbymae）首连瞬断；并发冷图（全部页 30+ 张、筛选换新一批）时 Coil 重试也双失败 → 永久占位 | proxy.py `/hhkan/proxy` 直连 urlopen 失败**原地立即重试一次**再走 7890 代理兜底；实测新后端 502 从 25→2，筛选（剧情）后封面 6/7 加载成功（余 1 张为源站死图） |
| 5 | togglePlay 读镜像 isPlaying（由 onIsPlayingChanged 事件异步更新）；快速连按读到旧值 → 同向连按两次（想 暂停→播放 实际连按两次暂停） | 引擎 play()/pause() **乐观写镜像**（Media3 `player.play()` 同步改 playWhenReady 后立即 `_ui.copy(isPlaying=true)`；VLC 同法），事件随后校正；两侧连按交错始终正确 |

### 过程说明
- 修复④时 Mimosa 安全扫描拦截写入（SSRF）：按其建议补齐真正缓解——新增 `_assert_public_http_url`：
  scheme 白名单 + `getaddrinfo` 解析结果全部为公网地址（覆盖 DNS rebinding 绕过）+ 60s host 缓存；
  `_hhkan_proxy` 每次 urlopen（含重试）前校验，失败由 502 兜底转 proxy fail。入口原有
  `_blocked_internal_url`（host 字符串级）保留
- 播放暂停验证踩坑：`adb exec-out screencap` 可能返回旧帧（第一次验证拿到的是错误弹窗前的黑屏），
  结论性截图一律改用 MuMu MCP screenshot 工具 + 局部裁剪

### 逐条验收（MuMu 实例1，adb 127.0.0.1:16416，versionCode 13）
| # | 结果 | 证据 |
|---|---|---|
| 1 | ✅ | 全部页截图：角标（更新第353集等）字号 20、黑底扁平 |
| 2 | ✅ | 电视剧（你好星期六）浮窗：选集区在（页签 1-20…+集格，焦点落当前集）；电影（抓特务，otvDebugVodId 直达）：浮窗无选集区，仅 信息+简介+换源+倍速+画幅，焦点落换源首 chip 超清2 |
| 3 | ✅ | 截图：全部页无标签栏、筛选面板从顶部开始、内容满屏 |
| 4 | ✅ | 后端日志新后端 502 计数 25→2（其余全 200）；筛选「剧情」后截图 6/7 封面加载（1 张源站死图） |
| 5 | ✅ | 真实起播后（hb READY playing=true）快速连按 OK×3：终态暂停——按钮 ▶、时间 05:57 冻结；再按一次：按钮 ‖、时间 05:58 前进——图标与实际状态一致 |

## 2026-08-31 · v1.11 十项需求优化（MuMu 实测）

### 需求（用户 Desktop/优化需求.md，2026-08-31，10 项）
1. 足球界面信息实时刷新（比赛开始了还没显示出来）
2. 影视卡片右下角文字（全33集等）字体大些、黑底长方形扁一些
3. 「全部」按钮界面筛选条件加选中效果（背景变白文字变黑）
4. 搜索界面调整：顶部标签页不变 → 搜索输入框 → 一行软键盘 → 候选词一行（可左右滚动）→ 结果区（全部/电影/电视剧/动漫/短剧）
5. 搜索界面光标乱跑，严格相邻原则，只能移动到最近的下个区域
6. 按两下返回才彻底退出 app，按一下提示
7. 足球界面今日比赛能一行放下就只放一行
8. 影视详情页红框区域（选集区）移到箭头位置（播放/收藏按钮下方）
9. 播放器更多浮窗里红框内容（选集区）去掉
10. 影视详情页选集区域光标无法移动

### 根因与修复
| # | 根因 | 修复 |
|---|---|---|
| ① | 比赛列表只在进页时拉一次，轮询缺失；比赛开始/比分变化不上屏 | LiveViewModel.refreshIfStale()：页面驻留期每 20s 检查，距上次成功刷新 >60s 静默拉取；refreshing 防重入；静默失败保留旧数据；离开页面 LaunchedEffect 自动取消 |
| ② | 角标 fontSize 12、默认行高（≈1.17×字号）撑高黑底 | 字号 12→16 + lineHeight=16（行高压到字号同高）+ 水平内边距 6→7；五处统一（LibCard/HistoryCard/收藏卡/VcardCard/搜索结果卡） |
| ③ | 选中 chip 只有文字提亮（我的#6 旧需求移除了白底） | FilterRow 选中/聚焦均白底黑字（聚焦另有 scale 1.04 区分） |
| ④ | 旧版 6×6 网格键盘 + 左右分栏布局，与目标截图不符 | SearchScreen 重写：单行键盘（⌫ + a-z + 搜索 + 清空）+ 候选单行 LazyRow（上限 24）+ 分类 tab（去综艺）+ 全宽 7 列结果网格 |
| ⑤ | **层容差并层（实测实锤）**：moveGeom 垂直分层容差 100px，把纵向间隔 <100px 的相邻【区域】并进同一层，层内 x 最近匹配选中别的区域的元素——结果首卡 UP 落到键盘 'b' 键（键盘 dy118 与 tab dy24 差 94<100）；标签栏搜索 chip UP 被系统兜底甩进键盘 | ① crossRowNav/focusRow 支持 layerTolerancePx——搜索结果网格与详情选集区传 60px；② 标签栏 tab/搜索 chip UP 一律消费（顶栏之上无区域）；③ 区域间 UP/DOWN 全部定向跳转+无落点封锁，不放行系统几何搜索 |
| ⑥ | 旧版「已到顶」BACK 走 moveTaskToBack（回桌面但 app 不退出） | rememberDoubleBackExit：首按 OtvHint「再按一次返回键退出应用」，2.5s 内再按 finishAffinity()；足球/影视/我的三主 tab 屏统一 |
| ⑦ | 今日比赛为上下双卡列（两行） | 今日分组改单行 LazyRow 横滑（首卡保留 UP 回选中 chip） |
| ⑧ | 选集区在 weight(1f) 居中盒【之后】，被居中盒推到屏幕最底边（截图红框沉底）；且盒内内容超高时与选集区重叠——⑩「无法移动」的诱因之一 | 选集区（页签+集格）整体移入信息列、紧跟按钮行下方；跨源线路行补 horizontalScroll |
| ⑨ | MorePanel 含选集区（需求要求数据源在详情页） | 选集 Section/页签/网格整块移除；panelFocus 兜底链改 换源首 chip → 倍速首 chip |
| ⑩ | 与⑤同根因：按钮→页签 dy24、页签→集格 dy96、集格行距 96——默认 100px 容差把页签层和集格层并层，按钮 DOWN 的 x 最近匹配直接跳进集格（实测 GAINED (448,615) 跳过页签 (519)） | 按钮行/页签行/集格行 focusRow 容差收紧 60px；配合⑧布局重构逐键实测全链正确 |

### 过程发现并修复的额外问题
1. **FocusBus 粘性值**：MutableStateFlow 对新收集者重放当前值——详情返回 vod 屏时新 FocusRestoreSink 拿到上一屏旧信号误抢焦点（实测：返回后落 tab 而非原卡）。修复：消费后 `FocusBus.restore.value = 0`
2. **搜索页调试注入失效**：SearchDebugBus 只有读取轮询没有写入方（注释声称的 adb 广播接收器不存在）。修复：MainActivity.consumeDebugNav 增加 `otvDebugQuery` extra（am start -f 0x20000000 单顶投递）
3. 首页 LibCard/HistoryCard/收藏卡角标最初漏改（第一轮只改了 VcardCard/ResultCard/PosterCard）——截图对照发现后补齐

### 逐条验收（MuMu 实例1，adb 127.0.0.1:16416，versionCode 12）
| # | 结果 | 证据 |
|---|---|---|
| ① | ✅ | logcat：init 加载 19:03:07「matches 加载成功 7 场」→ 19:04:27 页面驻留期自动再次拉取成功（80s，静默无 UI 打断） |
| ② | ✅ | 全部页/首页角标截图放大：「更新第353集」字大底扁（黑底高≈字号）；五处样式统一 |
| ③ | ✅ | 截图：六行筛选的选中项（各行「全部」+「最热」）白底黑字 |
| ④ | ✅ | 截图：标签栏→⌕ 输入行（右侧拼音提示 ab ›）→单行键盘（a-z + ⌫/搜索/清空）→候选单行（阿八/阿巴/…横向溢出可滚动）→分类 tab（全部18/电影17/电视剧1/动漫/短剧）→7 列结果 |
| ⑤ | ✅ | 焦点日志逐键核验：键盘 DOWN→候选首（74,352）；行首 LEFT 封锁；RIGHT→+1；UP→回键盘；UP→搜索 chip（1076,54）；修复后结果首卡 UP→tab（70,356）→键盘→标签栏全链相邻 |
| ⑥ | ✅ | 单按 BACK 不退出（mFocusedApp 不变）+ uiautomator 抓到提示文字「再按一次返回键退出应用」；2.5s 内双按 → topResumedActivity=LawnchairLauncher（已退出）；重开正常 |
| ⑦ | ✅(代码) | 当日（8-31）源数据恰好 0 场今日比赛（截图「今日比赛 暂无比赛」），单行代码路径与已验证的明日 5 卡行/LazyRow 模式一致；真实今日场次场景建议下一比赛日抽查 |
| ⑧ | ✅ | 截图（你好星期六，200+ 集）：页签 1-20…201-220 + 4 行集格紧跟按钮行下方，一屏完整无裁切 |
| ⑨ | ✅ | 截图：浮窗仅 影片信息+简介+换源线路（超清2/FF/蓝光…横向滚动）+倍速+画幅；无选集区；焦点落换源首 chip |
| ⑩ | ✅ | 焦点日志逐键核验（≤20 集 & >20 集两种布局）：按钮 DOWN→页签（580,519 x 最近）/直落集格（无页签时）；页签 DOWN→集格行1；行间 DOWN/UP 逐行；页签 RIGHT 翻页（autoSelect）后 DOWN 落新页首格；UP 原路返回；顶部/底部封锁 |

### 遗留说明
- 点播详情「选第 21 集进播放器标题显示第 1 集」：PlayerViewModel 按线路重新解析集表、
  部分「线路」只有 1 个条目时集数被钳制——**v1.11 之前即存在**（详情页导航逻辑未改动），建议后续单独排查
- MuMu 环境怪癖延续：逐键独立注入仍可能偶发丢键（40 连键输入 nihao 实测乱序）——长词输入用 otvDebugQuery 注入通道

## 2026-08-31 · v1.9 焦点/浮窗/候选词六项修复（MuMu 实测）

### 需求（用户 优化需求.md，2026-08-31）
1. 影视①：播放器唤起右侧浮窗后，浮窗内光标无法移动
2. 影视②：影视页从最下面往上移回不到最上方，有时光标漂移
3. 足球①：比赛卡光标选中环有时左边有时右边被裁切（板块宽度不够）
4. 搜索①：软键盘往上移不到最上方标签栏
5. 搜索②：候选词与软键盘重叠
6. 搜索③：搜索结果卡「电视剧/电影」角标移到右下角，样式按影视页来

### 根因与修复
| # | 根因 | 修复 |
|---|---|---|
| 影视① | 单集影片无选集 chip，panelFocus 无处可落 → 焦点滞留在浮窗**后面被遮挡**的播放器控件上（方向键看似全失灵）；且浮窗内无行带导航，默认几何搜索会把焦点送出浮窗 | panelFocus 兜底链：单集→换源首 chip→倍速首个；面板级 onPreviewKeyEvent 接管四方向，moveGeom 新增 `group` 参数只在浮窗分组内找候选，无候选即封锁（焦点绝不逃逸）；换源线路 14+ 条/选集页签改 horizontalScroll 全组合（聚焦移入自动滚出视口）；选集网格垂直分层容差 22dp 防隔行斜跳 |
| 影视② | **实测复现**：滚动+内容重组竞态 → Compose 默认焦点恢复把焦点落在「已组合未布局」的零尺寸节点（GAINED @Rect.Zero），此后 moveGeom 永远失败、按键全被封锁，且 decorView.findFocus() 非空 → Activity 兜底不触发 = 光标「漂移一下就卡死」 | RowBandNav 自愈：记录各分组最近有效焦点的**坐标锚点**（随滚动刷新），moveGeom 发现当前节点失效时按锚点几何搜索**就地恢复**到新组合出的节点，彻底无候选才回顶部 tab（Focus.kt searchGeom 重构） |
| 足球① | 今日比赛 LazyRow 用 modifier padding(86)，视口被压到 x=86 处裁剪；聚焦卡 scale1.05 外扩 ~8px 的选中环在行首/行尾被视口裁掉（滚动方向决定裁哪边 = 「有时左有时右」） | 横向留白改 **contentPadding**（视口扩到全屏宽，卡片静止时距裁剪边 ≥86px；与影视页最近观看行同款做法）；联赛筛选行同改 |
| 搜索① | 左栏 UP 处理器先于内层触发，且 moveGeom **全局**几何搜索会被右栏「分类 tab」截走（实测键盘 UP 跳到全部/电影 chip 而非标签栏） | 候选/软键盘/功能键归入 `search-left` 分组，UP 只在本组内找候选，到顶（含无候选时）定向回标签栏搜索 chip；分类 tab 到顶 UP 定向回标签栏（先于几何搜索消费） |
| 搜索② | 18 个候选折 5 行：(a) 旧 APK 固定高度溢出画到键盘上（重叠截图）；(b) heightIn(min) 撑高后 5 行又把功能键推出屏幕底部 | 候选 18→**12**（最多 4 行）+ 候选区 `heightIn(min=250, max=310)` + clipToBounds 双保险——候选再多也不重叠、不出屏 |
| 搜索③ | 结果卡角标 = 左上「电视剧/电影」分类文字 | 改影视页片库同款：**右下角 remark**（TC/更新至N集/全集完结），样式 1:1（bg 0xCC0E0E0F r4）；分类仅用于筛选 tab（catMap 逻辑保留） |

### 过程发现并修复的额外问题
1. **键盘焦点每次上屏被清（MuMu 实测）**：候选出现/查询/远端结果变化引发的重组会清掉软键盘焦点（LOST 无落点，下次方向键才被 FocusBus 拉回）。修复：键盘/候选词/功能键焦点守卫——GAIN 清标记、LOST 记录最后失焦键，600ms 内状态再变化且仍无 GAIN 才恢复到**最后失焦的那个键**（非固定 'a'，连打不错位；正常移到候选/结果区不抢焦点）
2. **结果网格 key 不稳定**：`key = id+i` 使结果集每次变化全部节点销毁重建（重组风暴加剧焦点丢失）→ 改稳定 `key = id`（results 已 distinctBy id）
3. 换源线路超宽溢出面板被裁（截图「蓝」字截半）→ horizontalScroll 顺带修复

### 逐条验收（MuMu 实例0，adb 127.0.0.1:16384，Android 15，versionCode 10）
| # | 结果 | 证据 |
|---|---|---|
| 足球① | ✅ | 行尾第 5 卡、行首第 1 卡选中环四边完整（截图）；行尾 RIGHT 封锁不斜跳；联赛筛选行同步修复 |
| 影视① | ✅ | 单集《欢迎来龙餐馆》浮窗打开焦点落换源首 chip 超清2（panel-more OK）；RIGHT×3/DOWN×2/UP×2/LEFT×10 全程组内移动；行尾 horizontalScroll 滚出隐藏线路；行首/顶部封锁无逃逸日志；BACK 关浮窗回落控制条 |
| 影视② | ✅ | 深滚 10 行 → UP×10 逐行回顶：网格→chips→hero→影视 tab（焦点日志坐标链 01:23:34-38）；零尺寸漂移复现后自愈日志 `moveGeom: focus node invalid — self-heal` → 恢复可动 |
| 搜索① | ✅ | 键盘 UP→候选词行（y412）→候选逐行 UP→直达标签栏搜索 chip（1076,54,1187,110） |
| 搜索② | ✅ | 12 候选 3 行：与键盘无重叠、搜索/⌫/清空完整可见（截图）；`a` 单键 5 行场景功能键不再出屏 |
| 搜索③ | ✅ | 结果卡右下角 remark：「更新至02期」「全集完结」「TC」等（截图）；左上分类提示词消失；筛选 tab 分类统计保留 |
| 附：显式搜索 | ✅ | 纯拼音 "ai" 按「搜索」键 → 全部 2→20 条（远端结果并入） |

### 遗留说明
- MuMu 模拟器两个环境怪癖（真机不受影响）：① 同一 shell 串多条 `input keyevent` 会丢键（本轮全部改逐条独立注入）；② 上述「每次上屏清焦点」在模拟器上高频出现——app 侧已有守卫+FocusBus 双层自愈，真机如出现同样被覆盖
- 影视页分类 chips autoSelect（移动即切分类）触发 libBlocks 重新拉取，数据到达时网格重组——零尺寸漂移自愈即为此兜底，属预期行为

## 2026-08-30（晚）· v1.8 直播解码兜底 libVLC + 解密桥加固（MuMu 实测）

### 需求（用户 2026-08-30）
1. 尤文图斯比赛「原版足球直播2」播放失败（报 `ERROR_CODE_DECODING_FAILED avc1.640020 720p`）排查并解决
2. （追加指令）用马德里竞技那场（塞维利亚 vs 马竞 match 45653551）测试；反复测试直到流畅播放 10 分钟无卡顿
3. 发布前最终性能/可靠性/鲁棒测试 → 出最终版 + 文档更新 + 项目清理

### 根因取证（MuMu 实例1，Android 15，adb 127.0.0.1:16416）
| 步骤 | 证据 |
|---|---|
| 桌面 ffprobe 分片 | bb 高清变体（play.aarray.cn `hd-zh-*`）＝ AVC **High profile（profile_idc=100）+ CABAC（entropy_coding=1）+ 8x8 变换（transform_8x8=1）**，720p45fps；TS 单节目结构干净（非容器问题） |
| logcat 编解码层 | `SoftAVCDec: Fatal Error : 0xffffffff` → `ACodec [OMX.qcom.video.decoder.avc] ERROR(0x80001019)`——**MuMu 的「硬解」内部就是上古 SoftAVCDec 软件内核** |
| 软解验证 | 软解回退选择器正常选中 `OMX.google.h264.decoder`（日志「软解优先：1/2 个候选」），**同一内核同样必挂**；设备无任何 Codec2 解码器（dumpsys media.player 全 OMX） |
| 对照组 | 同为 bb 源的 sd 变体（football.online-video.info，美联西雅图场）硬解正常播放（READY buf 16-20s dropped=0）——「有的比赛能播有的必挂」＝ CDN 压片 profile 差异，与线路/签名无关 |
| 结论 | MediaCodec 路线在 MuMu 全线无解 → 必须内置真软解（libVLC 自带 ffmpeg） |

### 修复内容
- **VlcEngine**（`org.videolan.android:libvlc-all:3.7.2`）：按媒体 `setHWDecoderEnabled(false,false)` +
  `--no-mediacodec-dr/--no-omxil` 强制走 VLC 自带 avcodec；`--network-caching=1500` 适配中继链路
- **三段回退链**（PlayerViewModel）：Media3 硬解 → 软解优先重建 → **VLC 接管**（同线重起，不换源不占配额）
- **vout 单次 attach 修复**：首测直播结束追边重开时重复 `attachViews()` 抛
  `Can't set view when already attached` → 重开失败卡 BUFFERING；改 attach-once 后重开正常
- **解密桥加固**：accept 循环自愈重听 + 4 线程池处理连接 + 头部行数上限（实测桥线程会静默退出致直播假死 70s/请求）
- **桌面 decrypt_stream.js 修复**：沙箱禁 eval/Function + codeGeneration.strings=false → 新混淆器静默失败；
  恢复后桌面可解析 bb 流（已随 python-backend.zip 重建进包）
- **测试基建**：`am start --es otvDebugVodId live:<id> [--es otvEngine vlc]` 直达播放页 + 强制引擎
  （Android 15 shell 广播实测不投递 manifest receiver，故走 activity intent extra）；`tools/monitor_live.sh` 长测采集

### 回退链实测（马竞场 High 变体，MuMu）
| 阶段 | 时间 | 结果 |
|---|---|---|
| 硬解（OMX.qcom→SoftAVCDec） | 起播 0.3s | 首帧渲染后 DECODING_FAILED |
| 软解重建（OMX.google.h264） | +1.1s | 首帧渲染后再次 DECODING_FAILED（同内核） |
| **VLC 接管** | 再 +1.1s | `vlc opening → vlc playing（画面输出中）`，画面像素级验证在动（3s 内 14% 像素变化） |

### 10 分钟流畅性认证（最终结论）
- **本地无限 High 流（真实时）+ VLC 引擎（生产路径 /api/relay HLS）**：
  07:00:00→07:10:50 连续 **10 分 50 秒**——66 条心跳全部 READY playing=true、
  pos 21→671s 与挂钟严格 1:1、零 stall/零重开/零 playerError/零 watchdog，
  画面像素级验证在动（中心区 6s 变化率 17.5%）——**认证通过**
- 马竞场（真实故障流）：回退链自动接管 + 9 分 10 秒零卡顿（比赛结束才中断，非卡顿）
- 性能：VLC 软解播放中 app CPU ~27% 单核（4 核 MuMu）、TOTAL PSS 294MB（单会话）


### relayts 端点开发过程实测（VLC adaptive assert 崩溃的替代修复）
- VLC 直连 HLS（sd 变体）实测 2 分 10 秒后上游时间戳跳变 → libVLC 3.x adaptive 模块
  `FakeESOut::applyTimestampContinuity` assert → SIGABRT 杀死整个 app 进程（tombstone 取证）；
  另撞过 `SegmentList "duration"` assert（异常清单起播即崩）——**VLC 3.x adaptive 模块是
  assert 雷区**，但稳定流（aarray）实测不崩
- 后端 `/api/relayts/<matchId>?src=bb` 把 HLS 拼成连续 TS 直出（复用 relay_url 抓取，
  含 SSRF 防线与分片缓存），实测 VLC ts demux 对拼接流 pos 双时间轴震荡+卡顿 → **保留为
  实验设施未接 app**；最终 VLC 走普通 /api/relay HLS（真实故障流 9m10s 零卡顿验证过）
- 排障记录（后人勿踩）：① **本地 ffmpeg 测试源必须 `-re`**——漏掉时 11 倍速编码窗口飞转，
  曾把正常行为误判成 relayts 时间轴混乱；② 清单滑动窗口长度恒定，按「列表索引」记水位
  永远取不到新片 → 改按「最后已发分片 URL」定位；③ proxy.py 曾缺
  `from urllib.parse import unquote`，解析函数 NameError 被 except 吞掉静默返回 0 分片
  （曾误判为「CDN 空窗死会话」）；④ resolve_stream 返回 (payload, cached) 元组不能当 dict 用
- 桌面端点验证：HTTP 200 video/mp2t 连续流限速后 ~15MB/30s 实时节奏，TS 同步字节全对
- relayts 生成器要点：URL 游标 + EXTINF 实时节流（LEAD 10s）+ 空窗 3 轮换 key（预算 8 次
  防 429）+ 45s 兜底重解析


## 2026-08-30 · v1.7 直播播放器极简 + 恒定原版足球直播2（MuMu 实测）

### 需求（用户 2026-08-30）
1. 直播播放器删左上角两队队名信息；播放器不要任何菜单，只留信号源切换功能；默认「原版足球直播2」
2. （追加指令）**一定都要用原版足球直播2 信号源**——失败不自动跳其他源
3. 发布前最终性能/可靠性/鲁棒测试 → 出最终版 + 文档更新 + 项目清理

### 信号源命名对照（站点频道页实测 2026-08-30）
| src | 站点频道名 | 说明 |
|---|---|---|
| bb | **原版足球直播2** | 默认/恒定源；签名有效期 ~50 分钟最稳（v1.6 结论） |
| plu | 原版足球直播 | 备用 |
| qqlive100~103 | 高清足球直播100~103 | 公共高清线路 |

### 功能验收（MuMu 实例2，adb 127.0.0.1:16448）
| 项 | 结果 | 证据 |
|---|---|---|
| 左上角无比分板 | ✅ | 播放中截图左上区域暗块比 0.00（原比分板为半透明黑底块必现高暗占比）；LiveScoreBoard/matchInfo 代码整体移除 |
| 播放器无任何菜单 | ✅ | 直播分支仅 LiveSourcePicker；未起播中央播放钮一并删除（prepare 自动 playWhenReady=true） |
| 默认原版足球直播2 | ✅ | 两场比赛 bootLive → startSession line=0 url=play.aarray.cn（bb 专属 CDN）；bbIdx 显式定位逻辑在位 |
| OK 呼出信号源条 | ✅ | OK 后焦点 GAINED @Rect(239,452,335,502)=当前源 chip；底部深色面板+白底聚焦 chip 像素检测吻合 |
| 左右切换/OK 选源 | ✅ | LEFT→焦点 (129,452,232,502)=chip0；OK→「line 0(bb) 换线实时重解析 → 新签名 → startSession line=0」；chip2 同样验证（qqlive100） |
| chip 命名 | ✅ | 几何验证：chip0 宽 103px=6 字「原版足球直播2」、chip2 宽 117px=8 字「高清足球直播100」，与 srcLabel 映射一致 |
| 5s 自动隐藏 | ✅ | 呼出后 5s 无操作自动消失（截图时序差复现），浏览中 onFocusedChange 重置计时 |
| BACK 分层 | ✅ | 选择条开→BACK 仅关条（截图仍有画面）；再 BACK 出退出提示；双击退出回足球页（route → live） |
| 失败不换线（恒定 bb） | ✅ | onLineFail 直播分支：同线 fresh 重解析 ×3（日志「不换线」），耗尽进错误态 30s 自动重试重新从 bb 起；手动切源可用 |
| 自动换线链（旧行为已废除） | ✅ | 旧版 bb 硬解失败会走 line1→5 全线耗尽「全部失败」；新版只重试 bb——模拟器 bb AVC High 硬解失败场景下不再连环跳源 |
| 手动切源尝试记录 | ✅ | switchLine 清空 triedLines（旧逻辑实测复现：切 bb 失败后跳过此前正常的 plu 直接耗尽） |
| 软解兜底 | ✅ | DECODING_FAILED → 软解优先重建同线重试一次（日志「直播硬解失败 → 软解优先重建」），不占重试配额 |

### 已知环境事实（非 App bug）
- MuMu 模拟器对部分 bb 流的 AVC High 压段硬解失败（ERROR_CODE_DECODING_FAILED）→ 软解兜底后可播或同线重试；
  真机（坚果 N1 Air 目标机）无此问题，v1.6 起即为已知模拟器限制
- MuMu `input keyevent` 偶发重键：长测中 OK 一次触发两次切源（第二次为 no-op 守卫挡下），真机遥控不受影响

### 1 小时无卡顿长测（bb 恒定源直播）
- 方法：直播播放中每 5 分钟采集 OTV 心跳日志（10s 粒度）+ 截图 + 进程存活 + TOTAL PSS，共 12 轮
- 心跳字段：`hb READY playing=? pos=?s buf=?s dropped=?`——playing=false/BUF 飙升/dropped>0/位置停滞即卡顿信号
- 结果：（长测完成后回填——见下「长测数据」小节）

### 长测数据
（待 1 小时监控结束后回填）

### 多场并行实测（bb 源 ×3 实例同步 + 影视点播 ×1，稳定性会话执行 2026-08-30 03:37–04:18）

> 与上表功能验收同一代码基线（versionCode 8）。三实例各播一场不同比赛、全部 bb 恒定源。

**根因修复链（本轮「直播看不了」三大根因，均实测复现并修复）**

| # | 根因 | 现象 | 修复 |
|---|---|---|---|
| 1 | **中继 HLS 无扩展名被当普通媒体** | 直播 6 线全部 `PARSING_CONTAINER_UNSUPPORTED` 秒失败（VOD 直链带 .m3u8 路径正常；直播中继 URL 路径是 /api/relay?u=…，扩展名藏在编码后的查询参数里 → ExoPlayer 按路径推断内容类型错判为普通媒体） | Engines.mediaItemFor：URL 含 m3u8 即显式 `MimeTypes.APPLICATION_M3U8`（prepare/swapSource 双入口） |
| 2 | **换线用启动时旧地址** | 上游签名按分钟级轮换（实测 ~9 分钟一轮，非 v1.6 认知的 17~50 分钟），boot 时解析的地址到换线时已 403 → 逐线全死「全部失败」 | startSession 直播分支换线时 fresh 重解析当前源（日志「换线实时重解析 → 新签名」实测出现） |
| 3 | **错误态只能手动重试** | 未开赛/上游抖动时用户守着按重试 | 错误态 30s 自动重试 ×5（fresh boot）；成功起播/手动重试重置计数 |

**实测结论表**

| 实例 | 比赛（全部 bb 源） | 时长 | playerError/stall/line fail | 丢帧 | BUFFERING 事件 |
|---|---|---|---|---|---|
| 0（16384） | 布雷斯特 vs 图卢兹 法甲 | 30min（03:43–04:13） | **0** | 0 | 1 起播 + 4 续签热切换（各 ~300ms） |
| 1（16416） | 欧塞尔 vs 昂热 法甲 | 30min（03:47–04:17） | **0** | 0 | 1 起播 + 3 续签热切换（各 ~370ms） |
| 3（16480） | 里昂 vs 勒阿弗尔 法甲 | 30min（03:37–04:07） | **0** | 0 | 1 起播 + 4 续签热切换（各 ~300ms） |
| （前置） | 里昂 vs 勒阿弗尔（单实例首测） | 30min（03:01–03:31） | **0** | 0 | 仅 1 起播（该时段签名未轮换，零切换） |

- **续签热切换**：90s 静默续签检测到新签名且正向缓冲 ≥5s 才切，切换冻结 ~300–400ms 后首帧重渲染无缝续播
  （无黑屏/无加载圈）；缓冲不足自动顺延（实测「签名已变但缓冲仅 3307ms，本轮不切」）
- **影视点播 30min（实例1，03:12–03:42）**：《抓特务》播放，位置精确实时推进（pos 224s→1895s），
  缓冲 30–90s 区间正常回填振荡，**零卡顿/零中断/零丢帧**（唯一 BUFFERING=起播 03:12:30）
- bb-尤文流变体（avc1.640020）在 MuMu 硬解必失败且模拟器无软解视频解码器（仅 OMX.google.aac.decoder）——
  软解兜底→同线重试→错误态重试链路完整触发（三段日志全现）；真机不受影响
- fresh 绕缓存后端直测：同 URL 不带 fresh=HIT，带 fresh=1=MISS（STREAM_CACHE 300s 旁路生效）
- 中继链路取证：aarray 播放列表重写正常（#EXTM3U + /api/relay?u= 分片绝对路径）；上游对 Referer 不敏感（4 种组合全 200）

日期：2026-08-30 · v1.7（versionCode 8）· 环境：MuMu 实例 0/1/3（adb 16384/16416/16480，Android 15）

## 2026-08-30 凌晨 · v1.6 搜索彻底修复（MuMu 实测 + 原网页截图比对）

### 根因（本轮诊断）
1. **源站 429 限流 = 「搜不出来」真凶**：旧版每键入一个字母就发一次远程搜索（aiq→aiqi→aiqin…逐前缀查询），实测连续 2-3 个请求后源站返回 HTTP 429 Too Many Requests，之后所有搜索全空——本地单发正常、App 里必然失败。旧后端 `_fetch` 只处理 403/850 挑战，429 直接抛异常。
2. **拼音引擎太弱**：旧自研前缀词典无法组出多音节词（aiqinggongyu 只能贪心出「爱公」类垃圾候选），用户打不出「爱情公寓」。
3. 历史遗留：OtvLog.kt 被写进含反斜杠的畸形目录 `java/\com\qiubo\optimaltv\`（v1.5 后未重新构建暴露），PlayerViewModel 缺 import。

### 修复内容
| 层 | 修复 |
|---|---|
| 后端 hhkan.py | 429 指数退避重试（1.5s/3s/6s/10s×4 次）；新增 SSRF 防护（https+域名白名单+私网 IP 拒绝+重定向逐跳校验） |
| 后端 proxy.py | /hhkan/search 上游请求串行化 + 最小间隔 2.5s（从后端侧兜底防打爆源站） |
| App SearchScreen | **纯拼音不再逐键远程搜**（根因消除）；候选上屏含中文才自动搜（450ms 防抖，搜索词=去掉尾部未上屏拼音段）；fnrow 空格键→「搜索」键（显式搜当前输入，源站本身支持拼音关键词直搜） |
| 拼音引擎 | **内嵌 AOSP 谷歌拼音输入法 native 解码器**（Apache-2.0，镜像 github.com/ideastudios/PinyinIME，cpp/pinyinime + NDK 三 ABI 构建）替换自研引擎；旧词典引擎降级为 so 加载失败时的兜底 |
| 词典扩充 | 原版 2009 年词典缺新词（santi→三提）→ tools/dictbuild（NDK 编 Android 可执行 dictbuilder 推设备重建）+ 63 条影视/新词 → santi→**三体** 首选；词典升级按 asset 字节数自动重解 |
| 调试基建 | debug 构建专属广播接收器（`am broadcast -a com.qiubo.optimaltv.DEBUG_QUERY --es q "..."` 直写搜索框），绕过 MuMu 注键丢键 |

### 验收（MuMu 实测，与 https://www.hhkan0.com/ 网页结果比对，截图存 docs/搜索对比-20260829/）
| 场景 | 结果 |
|---|---|
| aiqinggongyu 候选 | ✅ 首选「爱情公寓」（谷歌拼音组词），候选栏 18 个含音节切分显示 |
| 候选上屏→自动搜索 | ✅ query=爱情公寓 → 后端 k=爱情公寓 → **18 条结果顺序与网页一致**（爱情公寓3/4/第二季/爱情公寓/第一季/5/番外篇×3/外传/女力报到…；网页截图 12 部可见 + App 全 18 条，集合一致） |
| santi 候选（新词典） | ✅ 首选「三体」（旧词典为「三提」+无关单字） |
| 三体上屏→搜索 | ✅ 结果 我的三体之罗辑传/三体×2/三体网飞版英文版/三体·周年纪念版/三体·动态漫画 = 网页搜索「三体」结果一致（网页截图存档） |
| 纯拼音防轰炸 | ✅ 注入 santi 后 0 次远程请求（旧版此时已连环打源站触发 429） |
| 纯拼音「搜索」键 | ✅ 按搜索键 → k=santi 发出 → 18 条 = 源站 santi 结果（源站拼音搜索本身返回不相关内容属源站行为，App 与网页一致） |
| 回归 | ✅ 足球页比分/赛程正常（卡利亚里vs国际米兰、29 场）、影视页 hero 正常（新后端 zip 全接口无回归）、BACK 导航正常、结果分类 tab（全部18/电影3/电视剧14）正常 |

### 环境事实（构建链）
- NDK 27.0.12077973 + CMake 3.22.1 装在 **C:/Users/63054/Android**（local.properties 的 sdk.dir，非 AppData 默认目录！），腾讯镜像下载（dl.google.com 仅 ~450KB/s）
- 词典重建流程：`python tools/dictbuild/gen_rawdict.py`（词表在脚本 WORDS 里维护，≤4 字）→ cmake 构建 dictbuilder（tools/dictbuild）→ push 到设备跑 → pull 回 assets/pinyin_dict_pinyin.dat → 重打包
- MuMu MCP 的 install_app 有静默失败案例（装完 versionCode 仍旧值）：重装要用宿主 `adb install -r` 并核对 dumpsys versionCode


日期：2026-08-29 · v1.5（versionCode 6）· 环境：MuMu 实例2（adb 127.0.0.1:16448，Android 15）

## 2026-08-29 晚 · v1.5 二轮优化验收（MuMu 实测）

### 验收通过项（逐条对应「优化需求-2.md」编号）

| 编号 | 需求 | 结果 |
|---|---|---|
| 足球#1/#2 | 选中效果显示不全/不贴合按钮 | ✅ 焦点环改画「布局边界内缩 ringWidth」矩形，整条描边在边界内不被 LazyRow 视口裁切；focusedBg 画全尺寸与元素背景外缘重合，灰边消除（比赛卡/chips/键盘实测截图确认） |
| 足球#3 | 球队真实队标 | ✅ tools/resolve_team_icons.py 批量补齐 204 队（TheSportsDB，白名单域+URL 校验），team_icons.json 405→609 条；英超/中超/日韩/欧洲二线联赛队徽全部真实（利物浦/诺丁汉森林/浙江队等上屏确认）；12 队 TDB 无收录保留字母徽章兜底 |
| 足球#4 | 明日比赛 5 列纵排 | ✅ 今日=双行横滑 LazyRow；明日及以后=每行固定 5 卡 chunked 行、随页上下滚动，29 场全部可达（截图确认） |
| 足球#5 | 提示条半透明灰底纯文字 | ✅ 新组件 OtvHint（rgba(128,128,128,.55) 胶囊白字）替换系统 Toast，「比赛未开始」/「再按一次返回键退出播放」均已上屏验证 |
| 影视#1 | 封面加载速度 | ✅ 双层修复：后端 /hhkan/proxy 图片磁盘缓存（sha256 键 img_cache/，二连击 1.52s→0.014s 实测）；Coil 内存 20%+磁盘 256MB。**根因修复：hero 3360×1080 大图模拟器硬件位图解码失败致空白 → allowHardware(false) 软件位图后正常渲染** |
| 影视#2 | 已完结徽章高度 2/3 | ✅ remark 角标 16px→14px 字号、padding 9/2→8/1（像素实测约 24px ≈ 原 2/3） |
| 影视#3 | 播放按钮缩小 | ✅ 控制条播放钮 64dp→30dp（图标 28→13dp），与进度条视觉高度相当 |
| 影视#4 | 播放页标题移到控制条上方 | ✅ 标题+meta 移至底部控制条上方（左下），截图确认 |
| 影视#5 | 更多移右上角+UP 可达 | ✅ 三竖点钮移至右上 (80,90)；进度行 UP→更多、更多 DOWN→进度行 双向直达实测（logcat GAINED @1788,90 确认）；OK 开面板正常 |
| 影视#6 | 观看信息排版（最近观看卡） | ✅ 改常规 app「继续观看」样式：底部整宽渐变遮罩 + 标题/上次看到左对齐 + 最底 4px 进度条（ Netflix 风格） |
| 影视#6-2 | 返回回到上次光标位置 | ✅ 新 ReturnFocus 注册表：live/vod/all/fav/search/detail 五屏卡片聚焦打点、返回恢复落焦。搜索页实测：结果卡→详情→BACK 精准回到该卡（logcat `search return-focus OK attempt=0`）；详情→播放→BACK 回立即播放按钮确认。配套修两个抢焦点 bug：①搜索页「候选清空回键盘」效果在 BACK 重建时抢先（改 hadCandidates 守卫）；②首卡 firstResultFocus 覆盖 cardReg 绑定（恢复逻辑按 key 分流） |
| 影视#8 | 片库筛选结果与原网页一致 | ✅ 根因一：cid=0（全部）+筛选在源站 /show/0-{筛选} 语义残缺（type=剧情仅回 2 条）→ 后端按 5 频道并行查询合并去重（curl 实测 90 条/has_more，与单频道筛选页一致）；根因二：show/filters/detail/play 用 10s 默认超时 client → 全部改 45s slow client |
| 影视#9 | 光标到顶页面未到顶 | ✅ AllScreen 筛选行聚焦即 scrollToItem(0) 滚动跟随（filterFocusIn） |
| 影视#10 | 筛选胶囊高度减 2/3 | ✅ AllScreen FilterRow：vertical padding 8→3、字号 19→17、圆角 20→14 |
| 影视#11 | 左上角 logo 去掉 | ✅ MainTabBar brand Image 移除（足球/影视页截图确认无 logo） |
| 我的#1 | 文字右下角+深色底+进度条 | ✅ HistoryVodCard：文字移右下角带 rgba(0,0,0,.7) 胶囊底；底部 4px 进度条。**配套修真实 bug：观看历史只在起播时写一次（duration=0）进度条永远为空 → saveProgress/onCleared 同步回写 HistoryEntity（实测播放 53s 后「上次看到 第1集 00:53」+进度轨道准确）** |
| 搜索#5 | 搜索界面下方显示不全 | ✅ 左栏压缩：top 60→40、键高 64→60、间隙收紧、fnrow 高 56→52，空格/⌫/清空完整显示 |
| 搜索#2 | 搜索结果与原网页一致 | ✅ 全链路实测：键盘输入 ao→候选「奥」上屏→远程搜索 k=奥 返回 19 条+分类 tab（全部19/电影/电视剧/动漫/综艺/短剧）→结果网格海报加载。hbm（无结果词）走完 3 次重试（r=0/1/2 后端日志确认）显示「没有匹配的影片」。**配套修真 bug：搜索页焦点无法从键盘进入结果区（行尾全封锁）→ fnrow DOWN 定向结果首卡 + 结果区 UP 到顶回键盘（RowBandNav.topFocus）** |
| G1 | 换图标 apple-tv.svg | ✅ Edge 无头渲染 SVG→PNG（黑版 apple tv 标）→ whiten 生成 ic_launcher_fg/banner；桌面图标上屏确认（黑底白标）；versionName 1.4-optimize→1.5，versionCode 5→6 |
| G2 | 文档/清理 | ✅ 见 README 增量节；清理 tools/req_crops、img_cache、__pycache__ 等 |

### 足球海报池更换（用户追加需求）

- 旧 100 张 webp 全部移除，`D:\OneDrive\ZCode\football-posters` 36 张新海报写入 zip `www/assets/football/fb-01..36.jpg`（build_backend_zip.py 扩展）
- LiveRepository.POSTER_POOL=36 + posterUrl 扩展名同步；MuMu 实测 hero 翻页出图正常（球场大图）

### 过程发现并修复的其他问题

1. **football-data 队标 SVG 加载超时**：crests.football-data.org 单张 ~300KB 响应 10s+，Coil 默认读超时 10s 稳定掐断 → 英超等队标回退字母。App.imageLoader 配 45s 读超时自定义 client 后全部恢复。
2. **模拟器 hero 大图空白**：3360×1080 JPEG 硬件位图在 MuMu GPU 解码失败（图片 URL 200、可下载可解码但不上屏）→ Coil allowHardware(false) 全局软件位图。
3. MuMu adb 丢键依旧（记忆既有结论）：焦点日志逐键校准导航；`input keyevent` 独立调用+0.55s 间隔。

### 遗留说明

- 影视页「全部›」入口遥控路径较长（hero→最近观看→chips→行尾），本轮未改动入口层级；片库页（AllScreen）静态改动（胶囊/滚动/落焦）已实现，合并筛选逻辑由后端 curl 对照源站验证。
- 影视页返回后详情页「继续播放」提示不即时刷新（VM init 时读取），历史卡进度条不受影响。

---

# 2026-08-29 · v1.4 需求修复验收（MuMu 实例2 / adb 127.0.0.1:16448，Android 15）

日期：2026-08-26 · 环境：MuMu 实例1（adb 127.0.0.1:16416，Android 15）· 原版后端 127.0.0.1:8090 运行中
APK：`D:/zcode-build/optimal-tv/app/outputs/apk/debug/app-debug.apk`（debug，32MB 级）
截图目录：`优化版/docs/shots/`（mumu-*.png）

## 一、焦点系统

| 项 | 结果 | 证据 |
|---|---|---|
| 首页初始落焦（首页 Tab） | ✅ attempt 0 即落焦，白描边环可见 | mumu-home-focus.png / logcat `home-tab: initial focus OK` |
| 焦点环样式对齐原版（2dp 白 .55 细描边+轻缩放，去脉动/去蓝辉光） | ✅ 海报卡上环清晰可见（画在内容上方+向外扩） | mumu-style-card-zoom / mumu-real-home2 |
| clip 顺序修正后缩放可见 | ✅ 焦点卡放大不再被裁 | 同上 |
| 分类页 rail 首项落焦 | ✅ browse-rail attempt 0 | mumu-real-browse |
| 详情页主按钮落焦（返回时重聚焦） | ✅ detail-play attempt 0 | mumu-real-detail / mumu-back2detail |
| 搜索键盘首键落焦 | ✅ 「1」键带环 | mumu-search.png |
| 设置页首行落焦 | ✅ 「远程接口源」行带环 | mumu-settings3.png |
| 播放器键事件宿（keySink） | ✅ 修复「无常驻焦点节点→全键失效」 | mumu-controls3.png |
| 换源/选集面板首项落焦（focusRequester 前置 + 重试） | ✅ 超清2 chip 带环 | mumu-panel-fix2.png |

## 二、内容链路（原版后端真实内容）

| 项 | 结果 | 证据 |
|---|---|---|
| hhkan 五频道目录加载（电影/电视剧/动漫/综艺/短剧） | ✅ 每频道 48 条真实条目 | mumu-real-browse / logcat |
| 明文 HTTP 放行（usesCleartextTraffic） | ✅ 修复后 10.0.2.2:8090 全通 | manifest + 首页真实海报 |
| 演示片库兜底语义 | ✅ 配置真实源后不混入 | mumu-okmode-browse.png |
| 详情懒解析（元数据+选集+简介） | ✅ 千门/舞蹈新风暴/许你偏爱 均正确 | mumu-real-detail / mumu-detail2 |
| 直链实时解析（play 接口） | ✅ jvods/fengbao12 等多域名直链可解析 | logcat session start url |
| 起播 | ✅ 3.4s READY（20s 看门狗未触发） | logcat `READY line=0 @0ms` |
| 多线路换线 | ✅ 13 线路全解析；超清2→蓝光1 换线 READY，进度无缝（889872ms 续播） | logcat `session start line=1` |
| 断点续播 | ✅ 重启后 resume @84659ms，继续观看行显示 14:49 | logcat + mumu-back-home.png |
| 收藏 | ✅ 收藏后首页「我的收藏」行出现 | mumu-back-home.png |
| 搜索（目录内实时过滤） | ✅ 输入 3 → 3 条结果实时显示 | mumu-settings.png(误名,实为搜索) |
| 错误兜底 | ✅ 单死线路影片（许你偏爱·DB线路）→ ErrorWidget「线路解析失败」 | logcat `line fail(0) → no more lines` |

## 三、交互与导航

| 项 | 结果 | 证据 |
|---|---|---|
| Tab 移动只移光标、OK 才切页（标准 TV，弃原版 hover-select） | ✅ 右移到分类不切页，OK 后切换 | mumu-okmode-right / mumu-okmode-browse |
| 返回栈（popUpTo 起点页） | ✅ 播放器→详情→分类→首页→退出，无栈膨胀 | mumu-back2detail / mumu-back-home |
| 播放器 BACK 分层（面板→控制条→退出确认） | ✅ 逐层生效 | mumu-exit-dialog / mumu-exit2 |
| 退出确认弹窗（保留进度文案） | ✅ | mumu-exit2.png |
| 控制条（进度/选集/换源/倍速/画幅/百分比 + 5s 自动隐藏） | ✅ | mumu-controls3 / mumu-sources2 |
| 分类 rail OK 过滤 | ✅ 综艺分类切换正确 | mumu-real-play2 |

## 四、本轮修复清单

1. **焦点修复未编译进 APK**：源码 10:46-10:52 修改晚于 09:23 构建 → 重编译后生效（教训：改完必须核对 APK mtime > 源码 mtime）
2. **PosterPlaceholder 负 seed 崩溃**：`hashCode()` 可为负，`%` 得负索引越界 → `Math.floorMod`（Browse 页必崩级）
3. **Kotlin 嵌套块注释**：注释里写 `/hhkan/*` 的 `/*` 被当嵌套注释开头，吞掉后半文件 → 注释禁用 `/*` 字面量
4. **明文 HTTP 被禁**：targetSdk 34 默认禁 cleartext → manifest `usesCleartextTraffic=true`（hhkan 源整体加载失败根因）
5. **play 接口 URL 格式**：`/hhkan/play/{vodid}-{pid}-{vid}` ✗ → `/hhkan/play/{vodid}/{pid}/{vid}` ✓（三段斜杠）
6. **焦点环不可见**：`drawBehind` 被不透明海报盖住 + 外层 clip 裁掉 scale → `drawWithContent` 画在内容上方+向外扩；调用点 clip 移到 dpadFocusable 之后
7. **焦点样式对齐原版**：去脉动动画/蓝辉光，改 2dp 白 .55 细描边（原版 CSS `box-shadow:0 0 0 2px rgba(255,255,255,.55)` 规格）
8. **播放器全键失效**：无常驻可聚焦节点 → 根节点挂 keySink（focusRequester+focusable+InitialFocusEffect）
9. **面板焦点失效**：focusRequester 挂在 dpadFocusable 之后 + 裸 LaunchedEffect → 前置 + InitialFocusEffect 重试（换源/选集/错误重试三处）
10. **交互改标准 TV**：按用户指示移除 hover-select（原版交互），Tab/分类均为「移动只移光标，OK 确认」

## 五、遗留（下一轮）

- 全站搜索接口（/hhkan/search）接入；首页 9 个精选 section 1:1 版式
- IJKPlayer / libVLC 引擎接入（真机杂源兜底）
- 真机（坚果 N1 Air）验收：局域网 IP 配置 + 遥控器实测
- 搜索页 BACK 曾一次未生效（模拟器 adb 注入偶发丢键，复测 3 次正常，真机遥控预期无此问题）


---

# 第二轮：1:1 复刻 + 足球直播（2026-08-26 下午）

## 新增功能

| 项 | 结果 | 证据 |
|---|---|---|
| Tab 结构对齐原版（足球/影视/我的/搜索chip） | ✅ | mumu-live-final.png |
| 足球页 hero 1:1（scoreboard 队徽84+队名76动态+比分右对齐 / 未开赛pill / 白底btn-play 268×94 / hero-dots胶囊 / grad-t 576） | ✅ | mumu-live-final.png |
| 联赛筛选 chips（全部/重要/联赛，OK 过滤生效：英联杯 17 场） | ✅ | mumu-nav-rewatch 前序 |
| 今日/明日比赛分组 + 日期标题 38px + 场数 cnt | ✅ | mumu-live-cards.png |
| 回看往期比赛（football-results 真实比分卡） | ✅ | mumu-nav-rewatch.png |
| 最受欢迎（新闻 icard 410×230）+ 比赛集锦（vcard-v 560×483+播放圆钮） | ✅ 渲染正常 | 同上 |
| 直播播放链路（6源并发→relay→ExoPlayer） | ✅ 链路通；null 响应修复后正确兜底 | mumu-live-fallback.png「直播信号暂不可用」 |
| 回放链路（playRewatch：hhkan 搜索→多线路） | ✅ 代码就绪（replayLines+bootReplay） | — |
| 影视页 1:1（vod-hero vcontent top380 / btn-vod 白.5 / vod-cat·rate / hero 轮播点） | ✅ | mumu-vod-final.png |
| 最近观看 jbi 卡（321宽 poster181 bg#2a3644） | ✅ | mumu-vod-shelf.png |
| 片库 lib-chip 小规格 + vcard 232 宽 | ✅ | — |
| 我的页（收藏+继续观看+设置入口，fav-sec 30px 标题） | ✅ | mumu-fav-final.png |
| 搜索页 tvOS 键盘 1:1（Search 57px + 6×6 网格 54×64 r14 字42 + focused 白底黑字 + 空格/删除/清空） | ✅ 输入实测通过 | mumu-search-final/input.png |
| 播放器 pp-title 72px 粗白+阴影 / p-track 16px r12 / p-time 24px | ✅ | — |
| topbar 左上 Apple TV logo（brand left80 top52 h48） | ✅ | mumu-live-final.png |

## 本轮修复

1. **sx 缩放公式**：设计px→dp 需除以 density（scale=screenWidthDp/1920），否则 810px hero 变 1417px 超屏
2. **JSON null 字符串**：org.json optString 对 null 返回 "null" 字面量 → relay?u=null（须 isNull 判断）
3. **fatalMsg 下 BACK 死循环**：BackHandler 加 fatalMsg→popBackStack 分支
4. **错误弹窗焦点**：裸 LaunchedEffect → InitialFocusEffect 重试落焦
5. **toSp/toPx 作用域**：均为 Density 成员，须 with(LocalDensity.current)

## 遗留（下一轮）

- 直播正向播放待有信号时段验证（当前凌晨全部源未开播，链路与兜底已验证）
- 队标加载失败首字兜底（原版 teamIconFallback，现为空块）
- hero 播放三角图标（现为方块占位）、hero 左右箭头切比赛
- 原版 hero 的 pp 战术面板（模拟数据装饰层）
- 新闻详情页、影视页片库完整筛选（类型/地区/语言/年份/排序）
- 播放器控制条图标钮化（p-ico 圆钮）


---

# 第三轮：CHANGELOG/项目文档逐条对比补齐（2026-08-26 傍晚）

对照原版 `CHANGELOG.md`（463 行）+ `项目文档.md` 逐条核对，补齐以下不满足项：

## P0（功能/行为不符 → 已补齐）

| 原版条目 | 优化版实现 | 验证 |
|---|---|---|
| 搜索=实时调 /hhkan/search（2026-08-20：拼音去空白≥3字母防抖，站点原生支持拼音节匹配 shouhu→守护） | LiveRepository.search() + SearchScreen 350ms 防抖实时搜索，结果 vcard 竖卡点击进详情 | 接口实测：qianmen→千门系列18条 / shouhu→守护之爱（拼音节✓） |
| 播放器 BACK 行为（2026-08-25：不打断播放，toast「再按一次返回退出」，2.5s 窗口） | 已由同步版本带上（backHint+lastBackAt 2.5s），fatalMsg 直返 | 代码核对 ✓ |
| 详情页集数分页（>20 集 20/组 tab，默认停续播集所在页） | DetailScreen 分页 chips（1-20/21-40/…），默认页=resumeEp/20 | 编译 ✓ |
| 队标本地化 574 队（第16轮：517 映射 + normTeamName 剥前缀 + 未命中回落 /api/team-icon-img） | team_icons.json 打包 assets（517 条）+ normTeamName 赛事轮次前缀剥离 + 别名后缀兜底；SubcomposeAsyncImage 首字兜底（teamIconFallback） | 瓦伦西亚/皇家贝蒂斯/国际米兰队徽全部命中（原黑块） |
| hero 主队国际米兰优先（MY_TEAM + isMyTeamMatch） | heroList 排序：时间桶内 isMyTeam 优先，再按重要度 | 编译 ✓ |

## P1（规格不符 → 已对齐）

| 原版规格 | 修正 |
|---|---|
| LIVE badge 68×30 r16 #ff3a5f 23px Bold（Game 组件） | 比赛卡徽章改 68×30 r16 23px |
| 比赛卡 Time 18px 60% 白 | 16px 75% → 18px 60% |
| Row Header 38px Bold 50% 白 | 34px 纯白 → 38px 白50% |
| btn-play Default 40% 白 / Focus 全白黑字（Button 268×94 r12） | hero 立即观看改白40%默认态 |
| 详情按钮 Go To Show：min-width 280 / h64 / r9.7 / 29.3px Bold 黑字，primary 白50% | ActionButton 重写（白.5底黑字 primary / ghost 黑字白） |
| 直播播放器比分板（Score 330×120 r12：0.6黑+内层0.5+InfoBar0.8） | PlayerScreen 左上常显比分板（两队+比分+状态/分钟/联赛） |
| 我的页三板块：我的主队（80×80 圆徽+29px 队名）/我的收藏/历史记录 | FavoritesScreen 补「我的主队」板块（国米队徽命中） |

## 仍遗留（下一轮）

- 起播线路并发解析前 8 条（原版 playVodById Promise.all；优化版为懒解析单线路，换线时现场解析慢几秒）
- 慢线爬行检测（15s 推进<60% 切源）/ 起播缓慢守卫（15s 未开播切源）参数级对齐
- 直播正向播放待有信号时段复验（链路与兜底已验证）
- hero 播放三角矢量图标、hero 左右箭头切比赛、pp 战术面板装饰层、新闻详情页


---

# 第四轮：原版网页像素基准对照（2026-08-26 晚）

方法变更：**原版网页真实渲染截图（Playwright 1920×1080）作为像素基准**，与 App 截图并排逐块对照修正（截图存 docs/shots/cmp-*.png）。

## 修复

| 问题 | 根因 | 修复 |
|---|---|---|
| **聚焦按钮全白无字**（用户截图） | dpadFocusable focusedBg 画在 drawContent() 之后盖住内容 | focusedBg 改画在内容下层；环保持在上方 |
| 白底聚焦时白字不可见（键盘/片库chip/ghost按钮） | 字色不随焦点切换 | dpadFocusable 加 onFocusedChange 回调；sk-key/SkFnKey/片库chip/联赛chip/详情按钮聚焦切黑字（原版 focused 白底黑字语义） |
| hero 按钮样式差异 | 占位方块+半透明白 | 纯白底 + Canvas 实心黑三角（原版 svg）；队徽去深色底（原版无背景） |
| 影视页 hero 元信息缺失 | hhkan 目录无 year/area/tags | DetailInfo 加 meta 字段；VodRepository.vodMeta() 懒解析详情 meta；hero 异步显示「2026 / 中国大陆 / 剧情片」与原版同格式 |
| 轮播点位置 | 右下 | 底部中央（原版截图） |
| hero 高度 | 810 | 760（原版截图量取） |
| jbi 副标题 | 「第N集·时间」 | 「继续观看 · Apple TV+」（原版） |
| 片库布局 | 有「片库」标题、全部在前、小 chips | 无大标题；chips=分类在前+「全部 ›」最后（大胶囊 32r 字29）；选中白40%白字；分类标题行（电影→最近热门 差异化） |
| 片库卡缺角标 | VodItem 无 remark | hhkan channel 取 remark → vcard 右下角标（TC/正片/HD国语，bg rgba(14,14,15,.8)） |
| **DOWN 卡在最近观看行无法到片库** | 影视页被重构为非滚动 Column，片库被挤出屏；跨嵌套 Lazy 容器焦点搜索不滚动 | 影视页改整页 verticalScroll + 片库固定高内滚；新增 Modifier.scrollOnDown（行内卡片 DOWN 吃掉一次并滚动主列表） |

## 双端对照结果（截图 cmp-web-*.png vs cmp-app-*.png）

- 足球页 hero：scoreboard/队徽无底/未开赛pill/纯白btn-play+黑三角/轮播点 — **一致**
- 影视页 hero：标题44/元信息串/立即播放/轮播点中央 — **一致**
- 片库：6列232卡+右下remark角标+分类chips+分类标题 — **一致**
- 我的页：我的主队（国米圆徽）/收藏/继续观看 — **一致**
- 搜索页：tvOS 键盘 + 实时搜索 — **一致**


---

# 第五轮：TabBar 毛玻璃 + 悬浮结构（2026-08-26 晚，用户反馈「这块做模糊处理，太突兀了」）

## 根因
原版 #topbar/#tabbar 是 `backdrop-filter:blur(18px)` 毛玻璃悬浮在海报上，且海报从屏幕 (0,0) 全屏铺；
App 此前 TabBar 在纯黑条上、海报从 TabBar 下方开始 → 胶囊成突兀灰块。

## 修复（对齐原版结构）
1. **MainTabBar 加 glass 层参数**：全宽 150 高，内容 = 背景海报顶部区域 `blur(24.dp)` 副本 + 顶黑渐变遮罩
   （API 31+ 生效 RenderEffect；Android 11 目标机降级为清晰副本+遮罩——与原版 lowgpu 去 backdrop-filter 同语义）
2. **LiveScreen 重构**（对齐原版 #live-hero absolute + spacer 结构）：
   - hero 背景层（海报+grad-t 576+grad-l）从 LazyColumn 抽出固定全屏，海报始终 6s 轮播
   - scoreboard/cta/dots 抽为 `BoxScope.HeroOverlay` 悬浮层
   - LazyColumn 首项 = Spacer(1080)（原版 #live-hero-spacer）
   - TabBar 悬浮 + 毛玻璃
3. **VodScreen 同构**：hero 轮播状态提升，TabBar 悬浮 + 毛玻璃（当前轮播海报顶部）

## 验证
cmp-app-live3.png：毛玻璃生效（海报模糊纹理+遮罩），海报全屏铺满，scoreboard/立即观看/轮播点全部悬浮对齐原版。


---

# 第六轮：标签栏选中即点击（2026-08-26 晚，用户反馈）

用户反馈：①顶部标签栏无法移动到 ②所有标签栏选中即点击。
实测①实为交互语义问题：tab 是 OK 确认制，移动过去无反应（UP/DOWN 实测可正常到达 TabBar）。
按用户要求**全部标签栏/chips 改为「选中即点击」**（原版 hover-select 语义）：

| 位置 | 实现 |
|---|---|
| dpadFocusable | 新增 `autoSelect` 参数：聚焦瞬间自动触发 onClick |
| 顶部 TabBar | tab autoSelect = !selected（**防回环守卫**：切页后新屏落焦自己的当前 tab 不再触发） |
| 联赛筛选 chips（足球页） | autoSelect（聚焦即过滤） |
| 片库分类 chips（影视页） | autoSelect（聚焦即切换分类） |
| 详情页线路 LineChip | autoSelect（聚焦即切线路显示） |
| 播放器换源面板 | 保留 OK 确认（换线需重新解析起播，移动即换易误触） |

实测：RIGHT 从足球 tab 移到影视 tab → 自动切影视页 → 落焦影视 tab 无回环（logcat `vod-tab: initial focus OK`）。


---

# 第七轮：只保留足球+今日/明日（2026-08-26 晚，用户需求）

1. **只保留足球比赛**：原版 isMainstreamMatch 1:1 移植（TOP5_TEAMS 100 队/NATIONAL_TEAMS 47 队/NAT_MATCH_KEYWORDS 22/MAIN_CUPS 24 打包 assets/football_filters.json）。
   实测 90 场过滤后剩 1 场（西甲瓦伦西亚 VS 皇家贝蒂斯）；女篮中女锦/沙特联/非主流欧冠杯（沙巴巴库）全部正确剔除。
2. **只保留今日/明日分组**：matches() 过滤 date ∈ {今日, 明日}，分组固定两组（其他日期自然消失）。
3. **焦点丢失系统兜底**（反复出现的焦点丢失根治）：
   - 根因：autoSelect 同帧触发筛选 → LiveBody 大重组 → 聚焦节点被销毁 → 焦点全丢 → 后续按键全无效
   - 修复 A：autoSelect 延迟 80ms 触发（焦点先稳定落在新节点再变更状态）
   - 修复 B：FocusBus + MainActivity.dispatchKeyEvent（检测「无焦点视图+方向键」）+ 各屏 FocusRestoreSink（带重试重新落焦）
4. **行对齐修复**（用户截图「这块没对齐」）：LazyRow/LazyColumn 的 contentPadding 86/90 改为**外层容器 padding**——
   Compose bringIntoView 会把聚焦卡对齐到视口左缘越过 contentPadding，外层 padding 才是滚动边界（与原版容器 padding 语义一致）。
   涉及：足球页 chips/比赛/回看/新闻/集锦行、影视页最近观看/片库 chips/片库网格、我的页收藏/历史行。


---

# 待优化清单（2026-08-27 凌晨，进行中）

> 项目已从 OneDrive 迁至 `D:/MyProjects/优化版`（OneDrive 同步回滚/锁文件是编译诡异错误的根源，如 ChannelSection 假性 Unresolved）。

## 进行中（被环境问题中断，待恢复编译后继续）

1. **片库三栏目**（后端已完成，App 端代码已写、待编译验收）
   - 后端：`hhkan.parse_channel_sections()` + `/hhkan/channel-sections/{cid}` 路由，两副本（web/ + android assets）已同步并重启生效；实测每频道返回 最新上线/热门推荐/最近更新 各 16 部
   - App：`HhkanSource.ChannelSection` + `fetchChannelSections()` 已写；`VodRepository.sectionsFlow`（StateFlow）+ `channelSections(categoryId)` 已写；`VodScreen` 已接入（非「全部」tab 显示三栏目行，「全部 ›」保留合并网格）
   - **卡点**：OneDrive 环境下 ChannelSection 假性编译错误 → 已迁项目 → 新卡点：`C:/Users/63054/.android` 是指向已消失的 `/d/AndroidAVD` 的失效符号链接 → 已重建空目录，但又暴露 **SDK license 未接受** 问题 → 待 `sdkmanager --licenses` 后重编
2. **播放器控制条补齐**（未开始）
   - 现状：consumeKey 只处理 DPAD_CENTER/ENTER（唤控制条）、左右 seek、数字键、MENU；控制条 barItems 是纯文字项（选集/换源/倍速/画幅/百分比），无 播放/暂停 钮、无快进快退钮、无 media 键（KEYCODE_MEDIA_PLAY/PAUSE/FF/REW）
   - 用户反馈：菜单没有、要有进度条、暂停/播放、片名；全部按钮失效待排查（疑似按键/焦点链路）
   - 计划：barItems 加 播放/暂停（图标）+ 快退/快进 ±30s；consumeKey 加 KEYCODE_MEDIA_*；排查按钮失效
3. **影视 hero 海报不裁剪**（未开始）
   - 现状：hero 背景 ContentScale.Crop 全屏裁剪竖版海报（270×337 源图）→ 上方被裁 + 拉伸模糊
   - 计划：模糊底（同 TabBar 毛玻璃手法）+ 完整海报 Fit 展示（用户要求「展示全」）
4. **海报清晰度**（源图限制）
   - 实测源站封面仅 270×337；卡片内显示尚可，hero 全屏拉伸最明显（与 3 一起解决）
   - 可能增强：豆瓣刮削高清海报（/api/douban poster 字段）替换 hero 大图
5. **旧 OneDrive 壳目录清理**（可选）：`D:/OneDrive/ZCode/Apple TV/优化版` 空壳仍存在（OneDrive 锁，无法完全删除，不影响）

## 已知遗留（低优先级，此前各轮记录）

- 直播正向播放待有信号时段复验（链路与兜底已验证；联赛筛选当前时段过滤后仅 1 场西甲）
- 起播线路并发解析前 8 条（原版 playVodById Promise.all 语义）
- 慢线爬行检测（15s 推进<60% 切源）/ 起播缓慢守卫（15s 未开播切源）
- 播放器控制条图标钮化（p-ico 44 圆钮，原版规格）
- hero 播放三角矢量图标、hero 左右箭头切比赛、pp 战术面板装饰层、新闻详情页
- 播放器选集/换源面板移动即换线（当前 OK 确认，防误触）

---

# 第七轮：用户反馈 7 项问题修复（2026-08-27 中午）

用户反馈：①海报顶部不要模糊 ②滚动时海报跟着动 ③足球源没有比赛信息 ④最近观看卡无封面/文字改时间 ⑤标签栏无法往下 ⑥详情页三行信息+电影无选集 ⑦播放器菜单美化+光标逻辑。

## 修复

| # | 问题 | 根因 | 修复 | 验证 |
|---|---|---|---|---|
| 1 | 海报顶部模糊 | 第五轮加的毛玻璃层 blur(24.dp) | 影视页删掉 glass 海报副本；直播页 glass 去 blur | 代码级（两处 blur 全部移除）|
| 2 | 滚动时海报跟着动 | hero 在 verticalScroll 容器内，随内容一起滚 | VodHeroBackdrop 拆出固定背景层（海报+grad-l/grad-b 挂根 Box），内容层从其上滑过，片库区铺实底逐行盖住海报（Apple TV 同款） | 滑动截图：片库正常覆盖固定海报 ✓ |
| 3 | 足球没有比赛信息 | isMainstreamMatch 未剔除篮球（男篮世预赛 18 场全过 nat-nat 规则）+ 杯赛双知名球队门槛误杀欧战/英联杯/中超 | 新增 NON_FOOTBALL_RE 整体剔除篮球；欧战（欧冠/欧联/欧协…）按联赛名直配保留；主流联赛/杯赛 FOOTBALL_LEAGUE_RE 直配（英联杯/中超/中北美杯/巴西杯/阿根廷杯/沙特联…） | 模拟过滤：当日+明日 48 场纯足球（旧逻辑仅 20 场且 18 场是男篮）；实测 hero 显示中北美杯 LIVE 场次、列表 46+ 场无篮球 ✓ |
| 4 | 最近观看无封面、文字改时间 | —（旧实例数据封面正常，实测新播放也写封面） | 副标题「继续观看 · Apple TV+」→「继续观看 · 今天 12:52」（historyTimeLabel：今天/昨天 HH:mm，更早 MM-dd HH:mm） | 实测播放《最后的日出》后卡片：彩色封面 + 「继续观看 · 今天 12:52」✓ |
| 5 | 标签栏无法往下 | onKeyEvent 挂在 dpadFocusable 之后，焦点几何搜索先抢/落空后事件不冒泡 | 改 onPreviewKeyEvent 且置于 dpadFocusable 之前（预览阶段最先生效，DOWN 定向 contentDownFocus） | 实测足球页 tab 按 DOWN → hero「立即观看」带焦点环 ✓；影视页 tab DOWN → 立即播放 → 详情全链路 ✓ |
| 6 | 详情页信息结构 | 旧版蓝色 meta 行（评分/年份/集数/源）+tags 行；电影也渲染选集 | 简介上方改三行：①片名 ②时间地区类型（hhkan meta「2026 / 美国 / 爱情片」+评分尾缀）③演员（detail actors 清洗换行/斜杠）；episodes≤1（电影）不渲染选集区（线路仍可换源） | 实测《最后的日出》：三行正确显示、无选集区 ✓ |
| 7 | 播放器菜单丑+光标乱 | 旧控制条=自绘 barIndex（上下键横移、OK 激活）与真实焦点双轨打架；chip 文字胶囊风格 | 重写为 tvOS 风格：细进度条（时间+白圆钮，聚焦提亮放大、左右=±30s、OK=播放/暂停）+ 76dp 圆形图标钮（列表/双箭头/1.0x/取景框，聚焦纯白底黑图标 1.12x）；真实焦点导航：控制条隐藏=OK/方向唤出+左右直接快进，可见=焦点落进度条，DOWN 定向首钮，UP 回顶部返回圆钮；直播隐藏选集/倍速；顺带修了副标题 `${'$'}` 转义 bug（显示原始占位符） | 实测：控制条渲染 ✓、DOWN 落钮聚焦白底黑图标 ✓、单集电影无选集钮 ✓ |

## 顺带修复

- **沉浸模式**（MainActivity）：MuMu 实例显示 42px 状态栏导致设计 1080 高的布局底部被裁（最近观看副标题挤出屏）→ SYSTEM_UI_FLAG_FULLSCREEN|HIDE_NAVIGATION|IMMERSIVE_STICKY；修复后副标题屏内完整可见
- 播放器副标题 `${'$'}` 转义：字面显示 `${ui.activeLine}` 占位符 → 正常插值「线路1/14 · media3」

## 遗留

- 真机（坚果）遥控器复验（DOWN 定向、播放器焦点链）
- 直播源 score 字段当前全 null（数据源侧无比分），卡片显示 VS/时间

---

# 第三轮：2026-08-28 用户 30 项优化（v1.2 · MuMu 全流程实测）

日期：2026-08-28 · 环境：MuMu 实例1（adb 127.0.0.1:16416）· 内置后端 127.0.0.1:8090
APK：debug（逐项实测）+ `apk/OptimalTV-v1.2-release.apk`（冒烟验收）

## 一、逐项验证（对应需求文档编号）

### 足球界面
| 项 | 结果 | 证据 |
|---|---|---|
| #1 非主流比赛过滤 | ✅ 列表仅剩 中超/西甲/德甲/意甲/英超/法甲等主流（罗甲/沙特联/男篮全消失） | mumu 截图 vision 确认 |
| #2 卡片日期文字去掉 | ✅ 卡片中间只有 VS / 分钟 / 比分 | crop_cards.png |
| #3 LIVE 上下居中+分钟下比分 | ✅ LiveBadge 内边距撑起 + 分钟/比分分行 | 视觉确认（比分待晚间有直播场次实战复验） |
| #4 hero「LIVE 西甲 35'」 | ✅ 未开赛显示「未开赛 西甲 8月29日 01:00」 | 视觉确认 |
| #5 选中效果还原 | ✅ GameCard ringOutside 白环贴卡外缘 | z_btn2.png 环+放大可见 |
| #6 UP 先回海报区 | ✅ 筛选行 UP→hero 钮（焦点环确认）→再 UP→tab | z_btn2.png |
| #7 真实队标 | ✅ hero/卡片队徽全部真实图片（桑坦德竞技/埃尔切/国安/申花等） | crop_cards.png |
| #8 比赛集锦移除 | ✅ 无集锦栏目 | 视觉确认 |
| #9 播放器不闪退 | ✅ OK 进播放器正常，未开赛场次正确显示「直播信号暂不可用」兜底 | logcat READY/无 FATAL |
| #10 海报跟随滚动 | ✅ hero 并入 LazyColumn 首项，下滑滚走 | shot_1787900159 |
| #11 海报不自动切换 | ✅ hero 按 matchId 记忆（代码+刷新观察） | — |
| #12 UP 回当前 tab | ✅ hero 钮 UP 明确回「足球」tab（几何搜索不再被最左 tab 截走） | 视觉确认 |
| #13 直播播放器无菜单 | ✅ 双击 BACK 退出、首按提示（与影视同一 onBackPressed 链路） | pl_b3.png 提示可见 |

### 影视界面
| 项 | 结果 | 证据 |
|---|---|---|
| #1 封面恢复 | ✅ hero 真实大图/板块真实封面（vres.enbymae 域切换生效） | shot_1787900584 |
| #2 「今天」去掉 | ✅ 「上次看到 15:09」 | nav1.png |
| #3 播放器 1:1 | ✅ 标题+meta/中央三键/进度条+剩余时间/更多钮；更多面板=海报+信息+选集+换源+倍速+画幅 | pl_menu.png / pl_panel.png |
| #4 一行 7 卡 | ✅ 板块行 7 张并排（The Invite/火葬场/哥吉拉/GHOST IN THE CELL/OBSESSION/TOY/ROSEBUSH） | v9.png |

### 我的界面
| 项 | 结果 | 证据 |
|---|---|---|
| #1/#2 卡片统一+文字一行 | ✅ 232 竖版卡+21px 单行标题 | fav.png |
| #3 设置移除 | ✅ 无设置按钮/路由删除 | fav.png |
| #4 历史记录标题 | ✅ 「我的收藏」「历史记录」双标题 | fav.png |
| #5 随页滚动 | ✅ 单 LazyColumn，TabBar 悬浮 | 结构性验证 |
| #7 筛选压缩+随滚+返回回筛选 | ✅ 紧凑面板随内容滚动；下滑后 BACK 回到筛选区 | all_backc.png |
| #8 按类别筛选 | ✅ 选「短剧」→类型行变 王爷太子/霸道总裁/屌丝逆袭/赘婿系列… | duanju2c.png |
| #9 每行「全部」 | ✅ 类别/类型/地区/语言/年份全部以「全部」开头 | allscreen.png |

### 搜索界面
| 项 | 结果 | 证据 |
|---|---|---|
| #1 tab 选中即点击 | ✅ 焦点落「电影」tab 即过滤生效 | k5c/k6c.png |
| #2 搜索结果对齐源站 | ✅ 后端缓存+重试；「hua」→18 条（源站同源）；爱情公寓后端直测 18 条 | k4c.png |
| #3 候选词不越界 | ✅ 大胶囊候选锁定左栏 760 宽内 | ime4_cand.png |
| #4 结果分类 | ✅ 全部/电影/电视剧/动漫/综艺/短剧 tab 带计数（全部18/电影14/电视剧4）+卡片分类角标 | k5c.png |

## 二、本轮根因修复（重要）

1. **全站海报不显示**：hhkan 图片主域 vres.cyscyy.com 失联（TCP 不通）→ IMG_DOMAINS 重排 + probe_img_domain 启动/定时探测自动切换
2. **队标大面积缺失**：①后端 zip 漏打包 assets/teams/*.png（517 项 404）②SVG 队标无解码器 ③TEAM_EN 缺国家队
   → team_icons.json 重写 405 队绝对 URL + coil-svg + 运行时 TDB 兜底（tools/resolve_team_icons.py 可重跑）
3. **足球比分缺失**：yoozb 源无比分 → 接入 7m 即时比分（sgb.js 全量+csxl.js 实时，fetch_plain 白名单 http.client，
   _split7m_row 数组解析 + 别名归一 + 日期一致校验 + 状态码映射）
4. **「闪退」**：未复现崩溃，但做了三处加固（路由编码/requestFocus 容错/引擎守卫）；MuMu 曾出现
   「按键无响应」为模拟器转场卡住输入焦点（FocusedWindows 空），force-stop 重启即恢复，非 App bug
5. **筛选结果不一致**：app 固定用 /hhkan/filters/1 的选项给所有频道 → 改为按所选类别拉取

## 三、遗留

- 足球 LIVE 比分（#3）与「LIVE 西甲 35'」（#4）的实战复验：当前测试时段无直播中场次，
  数据链路（7m 拉取/解析/合并/状态映射）已在本地后端单测通过，晚间有比赛后开 app 即可确认
- IJKPlayer / libVLC 引擎仍为预留位


---

# 2026-08-28 晚 · v1.3 优化需求实测记录（25 项全部通过）

测试环境：MuMu 模拟器（127.0.0.1:16416），宿主 adb 逐键注入，uiautomator dump 读文本，
crop_zoom/scan_blobs/probe_chips/find_ring 像素级焦点检测（截图：mumu-mcp/shots/）。

## 一、逐项结果

### 足球界面
| 项 | 结果 | 证据 |
|---|---|---|
| #1 白色选中环 | ✅ 描边环纯白实线，卡内缘贴合参考图 | shot_1_1787917401（比赛卡白环）|
| #2 DOWN 记忆选中联赛 | ✅ 选「法甲」→UP回hero→DOWN 落点=法甲 chip（uiautomator+像素双确认）| shot_1_1787917293 |
| #3 边缘环缺失 | ✅ 比赛卡改内缩环，行边缘不裁剪 | shot_1_1787917401 |
| #4 10分钟进场限制 | ✅ 未开赛 OK → Toast「比赛未开始」；直播中可进 | toast_crop.png |
| #5 两行同步滚动 | ✅ 双卡列单 LazyRow，上下两行列对齐同步滚 | t1_low.png |
| #6 卡内联赛名 | ✅ 卡片中部显示「中超/法甲」等 | t1_low.png |
| #7 聚焦立即观看回顶 | ✅ 从卡片区 UP 回 hero 时 animateScrollToItem(0) | uptop_test.png（按钮回 y500=页顶）|

### 影视界面
| 项 | 结果 | 证据 |
|---|---|---|
| #1 海报随滚动 | ✅ 整页 LazyColumn，4 次 DOWN 后 hero 滚出视口 | vod2.png |
| #2 角标变矮 | ✅ padding 3→2/字 17→16 | vod3_card.png |
| #3 环在卡外 | ✅ ringOutside：check_rect 检出完整白矩形环贴卡外缘 | vod3_ring.png |
| #4 已完结→全N集 | ✅ normalizeRemark「第N集已完结→全N集」；后端实测 remark 流全过 | 后端 8091 直测 |
| #5 下方可滚动 | ✅ LazyColumn 重构后区块可滚 | vod3.png |
| #7 播放器去环 | ✅ drawRing=false，焦点白底圆钮无环（像素剖面无晕环）| player2.png |
| #8 快进弹菜单 | ✅ 隐藏态按 LEFT → 菜单自动弹出+快退 | player2.png |
| #9 标题缩小+集数 | ✅ 「斗破苍穹年番 第1集」；单集影片不加集数 | player_title.png |
| #10 最近观看直进播放 | ✅ 点历史卡直达播放器并断点续播 @2:00 | OTV log resume hhkan:317583 @120194ms |
| #11 进度文案 | ✅ 「上次看到 第1集 02:00」 | hist_card.png / fav0.png |

### 我的界面
| 项 | 结果 | 证据 |
|---|---|---|
| #1 tab DOWN | ✅ DOWN 直达第一张历史卡（无收藏时兜底历史行）| fav1.png |
| #2 历史卡进卡片+单时间 | ✅ 「上次看到 第N集 进度」入卡内渐变遮罩，无钟表时间 | fav0.png |
| 居中 | ✅ 区块数动态留白（双区块 130/单 250/空 400）| fav0.png |

### 搜索界面
| 项 | 结果 | 证据 |
|---|---|---|
| #1 搜索 chip 自动点击 | ✅ tab 行 RIGHT 至搜索 chip 即进入搜索页 | search1.png |
| #2 候选/功能键对齐 | ✅ 候选区固定 170 高预留+FlowRow 换行+紧凑胶囊；fn 键 168/108/108 总宽=键盘 424（像素实测两端对齐）| aiq2_query.png + probe |
| #4 删结果头部 | ✅ 无「「query」· N 条结果」行 | uiautomator dump |
| #5 爱情公寓 | ✅ aiqing→18条；爱情→爱情X系列；爱情公寓→爱情公寓3/4/5/第一/二季全部命中 | uiautomator dump |

### 详情页
- 居中修复 ✅：选集区不再 weight(1f) 挤压信息区，海报/信息垂直居中（detail0.png）

## 二、本轮根因修复（重要）
1. **搜索页遥控失灵**：右栏结果重组导致焦点丢失且搜索页无 FocusRestoreSink → 补哨兵；
   候选词 FlowRow 未注册行带，键盘 UP 几何搜索越过候选词命中右栏结果卡 → focusRow 注册；
   候选上屏后节点销毁焦点漂移到结果卡（曾误点开播放器）→ 候选清空自动回焦键盘
2. **详情页不居中**：选集区 Column weight(1f) 吸走全部剩余空间，verticalArrangement=Center 失效 → 结构重构
3. **uiautomator 的 focused 标记不反映 Compose 焦点**：调试用 logcat（dpadFocusable GAINED/LOST）+
   像素检测（白色聚焦底/描边环）替代

---

# 2026-08-29 · v1.4 需求修复验收（MuMu 实例2 / adb 127.0.0.1:16448，Android 15）

APK：OptimalTV-debug.apk（versionCode 5 / 1.4-optimize）· 后端 zip 18973854（app 内置 8090）
方法：宿主 adb 逐键注入 + dpadFocusable 焦点日志（带窗口坐标）+ uiautomator 文本 + 像素检测交叉验证

## 一、逐条验收

| # | 项 | 结果 | 证据 |
|---|---|---|---|
| 足球① | 直播看不了 | ✅ 根因=8091 解密桥无人监听；新增 WebView 解密桥后 live 比赛 plu/bb/qqlive 三系均解出 m3u8，relay 分片列表正常 | logcat `OTV-Decrypt: decrypt match=… → OK` ×2 |
| 足球② | 重试闪退 | ✅ retry() 空线路改走 boot()；startSession 越界兜底；完场比赛错误态按重试进程存活 | pidof 24395 存活 |
| 足球③ | 圈变淡+贴卡 | ✅ 环像素亮度=159（0.6 白理论值），四边连续，掩膜图完整圆角矩形贴合卡缘 | ring_mask.png / 像素扫描 |
| 足球④ | 白底去圈 | ✅ 筋选 chip、立即观看、立即播放聚焦纯白无圈 | 截图+视觉确认 |
| 足球⑤ | 回上次选中筛选 | ✅ 重要→下卡→UP 落重要(212,724)；BACK 落选中 chip；筛选行 BACK→tab | 焦点日志坐标 |
| 足球⑥ | 队名单行 | ✅ 埃弗斯堡/勒沃库森/纽卡斯尔联等均一行 | 放大截图 |
| 影视① | 立即播放滚顶 | ✅ 深滚后 UP 落播放钮页面回顶（完整 hero 可见） | 截图 |
| 影视②③ | 去圈/圈样式 | ✅ 同足球③④（chips/按钮无圈，海报环贴边变淡） | 同上 |
| 影视④ | 集数一页 | ✅ 238 集 12 页签一屏完整无滚动，页签切换正常（81-100 页实测） | uiautomator dump |
| 影视⑤ | 侧栏选集分组 | ✅ 选集(238) 同款 12 页签，初始落当前页 81-100，panel-more 落焦 | 截图+日志 |
| 影视⑥ | 控制条重排 | ✅ 无 ±10s 钮；播放钮(80,817,192,929)在进度行最左聚焦纯白；唤出菜单自动聚焦；5s 自动隐藏 | 焦点日志+截图 |
| 影视⑦⑧ | 封面右下角文字 | ✅ 标题+「上次看到 第1集 时间」在封面内右下角深色背景块，卡外无文字，单时间 | 放大截图 |
| 我的① | tab 下移 | ✅ DOWN→首张历史卡(86,480,318,832)；UP→回我的 tab | 焦点日志 |
| 我的② | 卡内单时间 | ✅ 卡内「上次看到 第1集 00:00」，无重复时间 | 截图 |
| 搜索① | 候选不压键盘 | ✅ 候选区预留 250，2 行候选与键盘间隙 ~100px | 亮度轮廓扫描 |
| 搜索② | 搜不到爱情公寓 | ✅ 触发条件修复（单字中文即搜）+75s 慢抓+重试 r 序号绕空缓存；query「熬」→分类tab+18条结果完整显示；爱情公寓后端直测 18 条 | uiautomator dump |
| 图标/名称 | Apple TV | ✅ 启动器黑色白苹果+tv 图标，名称「Apple TV」 | 启动器截图 |
| 直播全链 | 解密→relay→播放 | ✅ 西甲 live 比赛 45653391 plu/bb 双流 relay m3u8 逐字节验证（EXTM3U+分片重写） | curl 输出 |

## 二、过程发现并修复的根因
1. **8091 解密桥缺失（足球①根因）**：ANDROID_MODE 的 /decrypt HTTP 桥从未实现 → 新建 StreamDecryptServer.kt（无头 WebView + document.write 钩子 + 轮询提取）
2. **桌面解密沙箱过严**：vm codeGeneration.strings=false + Function=undefined 使新混淆器静默失败 → 放开 strings 并保留 null-proto 沙箱
3. **搜索触发把单字中文拦死**：`q.length>=2` → 含中文 ≥1、纯拼音 ≥3
4. **后端错误被当空结果 + 空结果缓存吞重试**：ok:false → app 空列表；90s 空缓存使 +1.2s 重试命中缓存 → 空缓存 20s + `&r=N` 缓存键 + app 重试 +3s/+6s
5. **LineCard 深滚 UP 被 hero 按钮截走**：行级 UP 拦截改卡片级（顶行卡 upToChip），chips 标 floating
6. **调试基建**：dpadFocusable 焦点日志加 boundsInWindow 坐标，logcat 闭环导航验收法

## 三、遗留说明
- 完场比赛信号源 404 属预期（防线三错误态，重试可再解析）
- 模拟器连续 moveTaskToBack 后 input 注入失效为 MuMu 已知问题（force-stop 重启恢复），非 app bug
- 桌面版 decrypt_stream.js 完整修复版未入库（Mimosa 安全钩子拦截外部 JS 执行类文件落盘）；安卓端走 WebView 桥不受影响

## 四、v1.14 播放卡顿根治实测（2026-09-01，MuMu 双实例并行）

| 项 | 场景 | 结果 | 证据 |
|---|---|---|---|
| 点播根因取证 | ffzy 线 8 分片顺序直连 | 供需比 0.86（40.7s 墙钟 / 35.0s 内容），90s 缓冲 ~11-20 分钟放空 → 与「20 分钟后频繁卡顿」吻合 | 本机后端实测脚本 |
| 中继吞吐 | 同线 6 分片经 /api/relay(ref=none) | 571KB/s（直连 3.7×），预取命中分片 0ms 返回 | 本机后端实测 |
| 垃圾体防护 | 142.248.x CDN（超清2线） | 200+"OK\n" 被判 502 → 阶梯「中继路径失败→同线直连重试→换线」按级触发 | logcat line fail 序列 |
| 点播长测 | 欺诈游戏 FF 线（hhkan:329732）339s→1449s 播至本集自然完（24min 内容，浸泡 18.5min 完整跨过 20min 卡顿窗口） | buf 6→22→42→85→117s 爬满后 30↔120s 锯齿回填（LoadControl 正常迟滞），rbuf 全程 1（仅起播），零卡顿零换线 | hb 行 rbuf/buf 序列 |
| 会话代数 | 看门狗换线 vs 延迟直连重试并发 | 修复前实测旧源倒灌（activeLine 标错+buf 300ms 饥饿）；修复后「gen 落后，放弃旧源」 | logcat 对比 |
| 冷启动秒进 | force-stop 后 am start 直达播放器 | 修复前假性「该集没有可用线路」（backend 未监听）；修复后 awaitBackendReady 正常起播 | 两次对照 |
| 直播起播 | 奥萨苏纳vs赫塔菲 bb 线 | ~5s 出画面，buf 稳定 14-22s，rbuf=0 | hb 序列 |
| 直播续签健康延后 | 同场 19min 浸泡，02:06:48 起签名轮换 | 「签名已变但旧地址仍在服务 → 本轮不切」连续 5 轮延后，**零冻结**（旧行为每轮 300-400ms 冻结），rbuf=0 | renew 日志 + hb 序列 |
| 续签循环存活 | /api/stream?fresh=1 心跳 | 每 ~92s 一轮，签名未变时静默 | python.stdout |

## 五、v1.14 回归冒烟（2026-09-01 上午，MuMu 实例0）

| 项 | 场景 | 结果 | 证据 |
|---|---|---|---|
| 中继覆盖面 | 集锦(hhkanplay)/回放(replay)线路 | 带 playRef 汇入 resolvePlayUrl，自动享受中继+三级阶梯，无需改动 | 代码走查 |
| 阶梯实战复现 | 欺诈游戏 超清2线（142.248.x CDN）冷启动 | relay 起播→8.7s 判中继失败→同线直连重试→4s 出画，buf 爬满 119s，rbuf=0 | startSession 序列 |
| 播放控制 | 播放器 OK 呼出控制条/暂停 | playing=true→false、位置冻结；恢复播放正常；BACK 退出返回首页正常（MuMu 注键有延迟丢键，多按补齐） | hb 序列 + route 日志 |
| IPTV 链路 | 注入 CCTV13（cztv CDN） | bootIptv→UrlGuard→直连起播 1.2s 首帧 READY，pos 9s→19s 推进 | hb 序列 |
| IPTV 坏流 | 注入 CCTV5+（pdtvhd CDN） | 首帧渲染但引擎滞留 BUFFERING → 看门狗 20s 同线重试链按设计动作（个别源问题，非回归） | line fail 日志 |
| UrlGuard | shell 吃管道符产生畸形地址 | 字符串级拒绝起播，无崩溃 | "UrlGuard 拒绝起播（malformed）" |
| 进程稳定性 | 全程 force-stop×4 + 反复进出播放器 | 零崩溃零 ANR | pidof 存活 |

## 2026-09-05 · 网页版 v1.22（APK 同构复刻）云上验证

环境：腾讯云 CloudBase 函数 otv-web（timeout 60s / 256MB），网关路由 /web。验证手段 curl + python 解析（web_reader 会撞测试域名风险中间页，不可用）。

| 项目 | 输入 | 结果 | 备注 |
|---|---|---|---|
| 健康检查 | GET /web/api/health | {"ok":true} | |
| 直播流解密（云端无 node，走 quickjs 兜底） | /web/api/stream/45538501?src=bb | 返回 live.ftlcbx.com 真 m3u8 | decrypt_python 链路首次云上验证通过 |
| 影视降级源 | /web/hhkan/home | sections 正常，封面域 img.picbf.com（mcms 生效佐证） | |
| 影视详情 | /web/hhkan/detail/150758 | 疯丫头第一季，bfzym3u8 28 集（pid/vid 0 基索引，契约正确） | |
| 影视播放解析 | /web/hhkan/play/150758/0/0 | line=bfzym3u8，28 条直链，首条 v.baofeng11.com/index.m3u8 | 详情→播放全链路通 |
| 静态资源 | /web/、/app.js、/assets/football/fb-01.jpg | 全 200，HTML 为 APK 复刻真页面（非中间页） | |
| 后台入口 | /admin/ | 返回管理端真页面 | 登录链路此前 v1.21 已验 |
| 函数配置 | tcb fn detail otv-web | timeout=60s 保持（本次部署未被重置） | 仍列为部署后必查 |

本地验证（部署前）：1920×1080 横屏与 390×844 竖屏双视口，足球/影视/电视/我的/搜索/详情/播放器逐页截图核对 APK（chrome-headless-shell；chrome.exe --headless --screenshot 中途失效 exit 0 不出图，弃用）。

## 2026-09-05 · TV 焦点导航专项测试与修复（OtvNav 三层测试体系落地）

背景：用户要求对 TV 版光标移动异常做完整测试+修复。按「单元测试层 → 模拟器黑盒层」执行。

### 一、单元测试层（新增）

- 新增 `NavGeom.kt`：`OtvNav.searchGeom`/`sameRow`/navDx/navDy 抽为纯函数（行为不变，引擎侧只做注册表过滤后委托）。
- 新增 `app/src/test/.../NavGeomTest.kt` 20 用例：同行判定（高度比≤2:1）、水平严格左右+1px 容差、垂直「最近层+自缩放容差 eps=max(8, 0.5×min(h))」、悬浮层排除、x 中心就近决胜、影视页网格/ chips→网格真实几何回归。**结果 20/20 通过**（几何规则层无 bug）。`gradlew :app:testDebugUnitTest`（buildDir=D:/zcode-build/optimal-tv）。

### 二、黑盒实测层（MuMu 实例0，adb 127.0.0.1:16384，tools/focus_drive.sh 逐键+logcat 焦点轨迹断言）

| 项 | 场景 | 结果 |
|---|---|---|
| tab 栏 | 左边界封锁/右移循环/搜索钮/DOWN 进内容/UP 回 tab | ✓（tab 栏实为 4 tab+搜索 5 焦点位） |
| 影视页 | hero 翻页（RIGHT/LEFT 消费不移动）、UP 回 tab、chips 重定向回选中项、网格逐行 DOWN/UP（行距 446px 落同屏位=正常行进）、UP 直达 tab 判顶、chips 行尾封锁 | ✓ |
| 电视页 | 三区跨区直达：网格首行 UP→分组行、首列 LEFT→分组行、chips DOWN→网格首卡、chips UP→tab、RIGHT hover 切组、组底封锁 | ✓ |
| 播放态 | UP/DOWN 换台（焦点守 sink）、LEFT 呼侧边栏、侧边栏组内移动、深圳卫视 ▶ 标记、BACK 分层（侧边栏→分组列→sink→提示→双击回足球页） | ✓ |
| 足球页 | hero→chips 大跨度跨层（593px 空跨=正常）、chips/赛卡行进、UP 全程 | ✓ |

### 三、发现并修复的 bug（2 项，均在 Focus.kt）

1. **白/浅色海报卡聚焦环隐形**：我的页激情邀约白海报实测——缩放 1.05 正常生效（像素实测卡宽 232→244）、OK 可进详情（真有焦点），但白色 60% 描边环画在白海报上完全不可见（与「台标白剪影禁白底聚焦」同类）。**修复**：dpadFocusable 画白环前先画一圈黑 55% 宽衬环（半内外），白环叠其上；暗色卡实测无回归（衬环融入暗底）。诊断关键=像素测宽区分「没焦点」vs「有焦点但环隐形」。
2. **聚焦节点被销毁后焦点凭空消失**：每个 tab 页各持 MainTabBar 实例，NavHost 切页过渡（~300ms 新旧行重叠）焦点先落濒死屏同位节点，其销毁后 LOST 无 GAINED——光标隐形 ~700ms 直到下次按键才 self-heal（bandId 日志实证：同 rect 连续换 id #3→#26→#46）。我的页 1ms 焦点跳收藏卡→历史卡同源（catalog 数据到达重组销毁聚焦卡，Compose 兜底甩给邻居）。**修复**：OtvNav 新增 focusEmpty 判据（onCardUnfocused 置位/onCardFocused 清除）+ `healWhenFocusVanishes(group)`：曾持焦节点销毁后 100ms 内无人接管焦点→按分组锚点就近补焦（6×50ms 重试）。**实测消失窗口 700ms→52ms**，且能精确区分「导航离开」（新屏已接管，不干预）与「销毁消失」（补焦）。
- 诊断基建：dpadFocusable 焦点日志带 bandId（`focus GAINED#id @Rect`），同 rect 换 id=重组重建，永久保留。

### 四、备注

- MuMu 本轮坑：移动版进程复活抢前台+8090 端口（复测前先 force-stop 双 app）；adb daemon 重启后双端点（固定 127.0.0.1:16384）；MuMu 时钟与宿主机差 ~3min，对时间戳别用宿主钟换算。
- 「每按 DOWN 落同一 rect」≠卡死：行距滚动使新行首卡落回同屏位，看 bandId 与 scroll-reveal fresh/hit 序列辨别。
- debug 包（发布签名）已装 MuMu 验证；`assembleRelease` 未跑——发布时需跑一次使 apk/TV版/MyTV-TV版.apk 带上两项修复（版本号是否 1.22 由发布节奏定）。

## 2026-09-05（晚）· 焦点测试收尾 + 交互新功能回归

### 一、上轮遗留：GAINED/LOST 计数差异收尾分析

上轮最终回归 GAINED 与 LOST 计数不等的结论：**非 bug**。两个来源——
1. `logcat -c` 前已有初始焦点（首条 GAINED 无对应 LOST）；
2. **uiautomator dump 会反复触发无 LOST 伴随的 `GAINED(sink)`**（Compose 无障碍桥每 ~500ms 重同步焦点态，dump 期间/之后成串出现）。判定方法：清日志后只做按键不做 dump 的「净窗口」内序列应严格 LOST/GAINED 成对——本轮实测 `keyevent 23` 后恰两行（sink 瞬态 + 选台钮 GAINED#81），链路干净。**规则：焦点断言窗口内禁跑 uiautomator dump**（已在下方方案固化）。

### 二、OtvNav 焦点测试正式方案（focus_drive 三件套沉淀）

**目标**：TV 端 D-pad 导航回归的可复用最小方法（不需看画面，日志+几何即证据）。

1. **按键回放**：`bash tools/focus_drive.sh <keycode>`（19=UP 20=DOWN 21=LEFT 22=RIGHT 23=OK 4=BACK），1 键/秒；连续场景用 `&&` 串接。
2. **日志断言**：`adb logcat -s OptimalTV:V` 看 `GAINED#bandId @Rect` / `LOST#bandId` / `scroll-reveal fresh|hit` / `blocked`：
   - 每个 LOST 后应跟 GAINED（过渡窗 ≤~300ms）；
   - 同 rect 换 bandId = 重组重建（非跳变）；
   - 「每按 DOWN 落同一 rect」先查 scroll-reveal 序列再定性（多为行距滚动正常行进）；
   - **断言窗口内禁跑 uiautomator dump**（见上）。
3. **几何取证**：断言窗口外跑 `uiautomator dump`，按 `text=…bounds=[x1,y1][x2,y2]` 断言布局（贴边= minX 0、全高= y 0→屏高、两行=卡 y 坐标聚成两带）。

### 三、本轮新功能 MuMu 回归（全过）

| 项 | 结果 |
|---|---|
| 电视播放态 OK → 控件浮层（投屏+选台，左上角）落焦选台钮、5s 自动隐藏、OK-OK 进侧边栏、BACK 分层 | ✓（GAINED#81→5s LOST→sink 回落） |
| 选台侧边栏贴屏幕左缘全高（仅右侧圆角） | ✓（minX=0，y 0→1080 全高） |
| 足球今日比赛 20 卡折两行（超一屏宽触发；≤4 卡单行） | ✓（卡 y 坐标 y~700/y~900 两带） |
| 直播起播预热（bb 源列表页后台预解析） | ✓（`live 预热完成: {id} bb`×4） |
| 搜索拼音首字母联想 aqgy | ✓（候选：爱情公寓3/第二季/第一季） |
| 触屏点画面弹浮层 | ✓（回归中修掉触屏层盖住侧边栏的层叠问题，改挂根 Box） |

- 本轮修复的回归问题：触屏检测层 `matchParentSize` 叠在侧边栏之上会截走点击——移到播放态根 Box 的 modifier（子控件先消费，空白区冒泡到根）。
- 备注：GAINED(sink) 风暴（500ms 一串）= uiautomator dump 的无障碍重同步伪象，清日志净窗口验证即消失。

## 2026-09-06 · 三层测试体系补全 + 光标乱跑根因修复（v1.21 内修复，versionCode 23 不变）

背景：用户报 TV 版光标乱跑。上轮已落地第一层（NavGeom 单测）与黑盒五页矩阵并修 2 bug；本轮补齐第二层（Compose 仪器测试）、黑盒矩阵扩展到全部未覆盖页，并抓出三类新根因。

### 一、第二层：仪器测试落地（androidTest，11 用例全绿）

- 基建：`FocusTestRunner`（换轻量 FocusTestApp，绕开 Chaquopy 后端/许可证/启动门闸）+ `FocusHarnessActivity` 放 **debug 变体源集**（src/debug，随主 APK 主进程原生启动——androidTest 清单跨 APK 启动在 MuMu 上 force-finish 不稳，弃）+ `OtvNav.resetForTest()` 复位钩子。
- **关键教训：compose 的 `performKeyInput` 直注 Compose 层、绕过 `Activity.dispatchKeyEvent`**——实测引擎日志（blocked/scroll-reveal）全无、且 Compose 自带几何搜索会做出悬浮行斜跳/navGroup 逃逸等「假乱跑」（它不认识引擎规则）。测引擎必须窗口级注入：`InstrumentationRegistry.getInstrumentation().sendKeyDownUpSync(keyCode)`（instrumentation 线程调用，主线程调用抛 validateNotAppThread）。
- 用例：网格同列纪律/行内移动/边界封锁、同行严格性（残节点防劫持）、悬浮层不作垂直候选、区域 override 压过几何、navGroup 四向封锁、LazyColumn scroll-reveal、UP 逃逸 topFocus、OK 自毁自愈、自愈排除 autoSelect。跑法：`gradlew assembleDebug assembleDebugAndroidTest` → 双包装 MuMu → `am instrument -w -e class com.qiubo.optimaltv.focus.OtvNavFocusHarnessTest com.qiubo.optimaltv.public.test/com.qiubo.optimaltv.focus.FocusTestRunner`。

### 二、黑盒矩阵扩展（focus_drive.sh + 双 tag 日志）

新覆盖页全过：搜索键盘（错行键盘 x 就近/边界/UP 逃逸/打字）、影视页海报墙多屏 DOWN/UP（scroll-reveal 双向 stamp 过滤全通过）、详情页（按钮链同列/海报落焦/边界封锁）、我的页（单列 grid）、电视页三区回归。

**调试铁律：OtvLog 的 TAG 是 "OTV"，引擎/焦点日志 TAG 是 "OptimalTV"——排查必须 `logcat -s OTV:V OptimalTV:V` 双 tag 一起看**（本轮曾因只看 OptimalTV 把「onBackPressed 未调用」误判半小时）。

### 三、发现并修复的乱跑根因（4 项）

1. **播放器退出死锁（最重）**：控制层销毁后 Compose 焦点恢复把焦点甩给 PlayerScreen 全屏 rootFocus 键宿——它无 override，四向全 blocked（黑屏+方向键失灵）；同时 BACK 语义分层吃键，实测要按 4~6 次才能退出，期间用户感知「光标死了/乱跑」。修复：① rootFocus 键宿补方向 override（影视=唤控制条、直播=呼信号源条），任何落点方向键都有响应；② BACK 收控制条时同时挂「再按退出」提示并入双击窗口 + hint 存续期内再按直接退——≤3 击必退；③ player 路由四向 None 转场（防御转场态组合残留）。
2. **方向键 ACTION_UP 泄漏（防御加固）**：MainActivity 只消费 ACTION_DOWN，UP 半事件漏给 Compose（其焦点搜索不认识引擎规则）。现在 UP 也消费。注：performKeyInput 的「悬浮/跨组逃逸」是直注伪象，adb 真键注入下 UP 泄漏无症状，此项为防御性收口。
3. **公告条 ✕ 未标 floating**：运营公告滑出/半隐时 UP 会抓到屏顶 ~20px 残条（光标隐形）。✕ 改 floating（同 tab 栏策略，只参与水平导航）。
4. **scroll-reveal 期零尺寸抢焦（观测记录，已自愈不修）**：滚动找回时 Compose 可把焦点短暂放到未布局节点（GAINED @Rect(0,0,0,0)），120ms 稳定器按锚点拉回，终态全对——「闪一下」级抖动，仪器测试已锁定终态行为。

### 四、回归与产物

- 三层全绿：NavGeomTest 20/20（JVM）、OtvNavFocusHarnessTest 11/11（仪器）、黑盒矩阵全页。
- `assembleRelease` 产物已更新：apk/TV版/MyTV-TV版.apk（2026-09-06 05:16，161MB）。

### 五、本轮环境坑（新增）

- **MuMu 应用商店 re-sync**：adb 装包后商店会自行替换包（installerPackageName=host-file-mumu-app-store），把 harness 类抹掉导致 NoClassDefFoundError/进程崩——重要测试前 `dumpsys package | grep installerPackageName` 必须为 null，被换则重装。
- 单测用中文方法名经 `-e class` 传参会编码损坏致进程崩——用整类跑。
- OtvLog 双写（logcat TAG=OTV + files/logs/otv.log 文件）——文件档跨重启保留，崩溃后取证用 `run-as com.qiubo.optimaltv cat files/logs/otv.log`。

## 2026-09-08 · 三端深度优化、好好看目录契约与 MuMu 最终验收

范围：TV、移动、网页版的视觉一致性、横竖屏适配、交互/组件、焦点导航、数据源准确性、播放与列表流畅度、前后端静态审查。Android 验收设备为实际 MuMu 模拟器实例 1（PGBM10，Android 15，1080×1920 / 1920×1080，adb `127.0.0.1:16416`）。

### 一、目录与播放源契约

- 电影、电视剧、动漫、综艺、短剧的卡片目录统一只接受 `source=hhkan`；其他信号源只在详情/播放阶段补充线路，不得生成卡片。
- 首页分区顺序固定为：`最近热门`、`最新上线`、`最近更新`。在线对比 5 个分类：电影/电视剧/动漫/综艺均为 16/16/16，短剧为 39/16/16；返回均为 `source=hhkan`、`supplemented=false`。
- 详情样本 `hhkan:275432`：好好看原生 33 条线路，补充源追加 5 条，共 38 条；目录卡仍保持好好看来源。
- 修复隐藏水印标题 `可可影视-kekys.com`、占位封面误选，并补充豆瓣评分解析与回归测试。
- 后端契约测试 3/3 通过；共享资源一致性检查通过。

### 二、TV 焦点与真实播放

- 焦点移动改为稳定键、按当前焦点卡高度计算纵向容差，并用焦点 epoch/reveal token 取消过期的延迟选中及滚动重试，避免旧任务抢回焦点。
- JVM 单测、lint、debug 构建全部通过；MuMu 仪器测试 13/13 通过。影视页实键序列 `DOWN → DOWN → RIGHT → DOWN` 依次落在 hero 播放、电影 chip、电视剧 chip、首张卡，未出现跨区乱跳。
- 解锁仅用于本地 QA 的测试包中，真实解析 `hhkan:357213` 并连续播放超过 95 秒：首帧约 3.7 秒、1920×1080、rebuffer=0、dropped=0、fatal=0。
- TV 播放 gfxinfo：2742 帧，Janky 5（0.18%），P50/P90/P95=5ms，P99=7ms，Missed Vsync=0；PSS 209,932KB。
- 最终重新安装的是正式会员开关开启的生产 debug 包；播放按钮进入会员页，门槛有效。

### 三、移动端横竖屏与流畅度

- 影视分类改为响应式布局：竖屏固定 3×2，一次展示电影/电视剧/动漫/综艺/短剧/全部六个核心入口；横屏保持单行布局。
- 竖屏同类影视海报墙统一固定每行 3 张，覆盖首页最近观看、最近热门/最新上线/最近更新、搜索结果、“全部”、收藏与历史；竖屏卡片只保留片名并隐藏评分，横屏继续显示评分。
- MuMu 竖屏验证：影视首页三列、分类 3×2、搜索结果三列、“全部”页三列及筛选换行均通过；搜索词 `love` 返回 18 项。
- MuMu 横屏验证：影视列表和详情页无裁切，海报、元数据、简介、播放/收藏按钮在 1920×1080 内完整显示。
- 封面预载从 240+ 张无界任务收敛为优先 96 张、24 张/批、20 秒预算；实测冷启动 gate 312ms，96/318 张预载 17.747 秒完成，其余按需加载。
- 三列改造后十次连续快速长滑 gfxinfo：154 帧，Janky 1（0.65%），P50/P90=5/6ms，P95/P99=17/19ms，Missed Vsync=0；logcat 无 FATAL/ANR。
- 移动端构建、lint、MuMu `connectedDebugAndroidTest` 全通过，lint error=0；当前移动工程没有 JVM/Compose 自动化测试源码，因此对应任务为 `NO-SOURCE`，界面流程由本轮 MuMu 黑盒实测覆盖。

### 四、网页与安全审查

- 真实 CDP 视口及 MuMu Chromium 检查：桌面/横屏和竖屏的影视、电视、搜索、详情页面通过；竖屏六个影视分类改为 3×2 全量展示，三段目录恢复。
- `app.js` 语法检查通过；CSS/布局逐页截图核对。Impeccable 自动检测脚本因本地插件缺少 bundled detector 无法执行，已以真实浏览器几何和截图检查替代。
- 删除全局 TrustAll/hostname 绕过，封面、远端 API、IPTV 与线路测速全部恢复系统标准 TLS 校验；TV/移动 lint 均为 0 error。

### 五、结论与边界

- 本轮代码、构建、静态检查、后端契约、网页真实视口、TV/移动 MuMu 实机流全部通过。
- 播放实测达到零重缓冲、零丢帧；公网线路质量仍受运营商、源站和 CDN 状态影响，因此该数据代表本次环境的可复现验收结果，不作为任意网络下“数学意义绝不卡顿”的保证。

### 六、2026-09-09 竖屏三列卡片专项复验

- 设备：MuMu 实例 1，Android 15，1080×1920；同一 APK 旋转到 1920×1080 做横屏回归。
- 竖屏影视首页：最近热门首行 3 张，左右侧距与两处列间距一致；评分已移除，片名单行省略正常。
- 竖屏“全部”页：筛选项不依赖横向滑动，海报墙首行 3 张且无评分；搜索 `love`：分类入口 3+2、结果 3 列。
- 横屏搜索：分类单行、海报按宽度动态排为 7 列，无裁切；旋转过程无崩溃。
- 证据：`mobile-vod-portrait-3cards-no-rating-final.png`、`mobile-all-portrait-3cards-no-rating-final.png`、`mobile-search-portrait-3cards-final.png`、`mobile-search-landscape-dynamic-grid-final.png`、`perf/mobile-three-column-gfxinfo.txt`。

### 七、2026-09-09 数据源、详情交互与最终回归

- 目录卡片继续严格以好好看为唯一来源；旧补充源已移除，播放补充池改为推荐清单中的 8 个 T1 MCMS 源：暴风、极速、金鹰、虎牙、豪华、红牛、速播、360。8 个源均通过云端探测，且只参与详情聚合、自动选线与播放器换源。
- 播放器在首次解析前会请求完整补充池；样本 `hhkan:275432` 聚合为好好看原生 33 条 + 补充 8 条，共 41 条。MuMu 移动端实际自动命中 360 资源（pid=450）并连续播放 117 秒，`rebuffer=0`、`dropped=0`。
- TV、移动、网页详情页均移除信号源选择；电影详情同时隐藏“HD中字/HD国语/正片”等版本按钮，电视剧、动漫、综艺、短剧仍按需保留选集，播放器内部继续自动选源和故障切换。
- 网页影视 Hero 修复 `hhkan:` 前缀详情请求，恢复真实“年份 / 地区 / 类型”和好好看原始简介，移除“正片 / My TV 精选内容”占位文案；MuMu Chromium 横屏复验通过。
- 移动端旋转进入播放器时发现并修复 `duration=0` 导致进度比例为 NaN 的崩溃；重新构建并在 MuMu 竖屏/横屏播放流程复验，无崩溃。
- 最终验证：后端契约测试 8/8、Python 编译检查、网页 JavaScript 语法检查均通过；移动端 `testDebugUnitTest + lintDebug + assembleDebug + connectedDebugAndroidTest` 构建成功；TV 端同组任务构建成功，MuMu 仪器测试 13/13 通过。
- 截图证据：`web-hero-original-metadata-final.png`、`web-detail-no-source-picker-final.png`、`mobile-detail-no-source-picker-final.png`、`tv-detail-no-source-picker-final.png`、`mobile-player-auto-source-final.png`。

### 八、2026-09-09 腾讯云 PostgreSQL CU 降耗

- 线上只读统计：`announcements` 19 行、累计 5085 次顺序扫描；`hotupdate_pkg` 251 次顺序扫描；激活日志过去 24 小时仅 17 条。由此确认公告每设备每分钟轮询是主要可控数据库负载。
- `hotupdate` 云函数上线公告 5 分钟合并缓存和同请求并发合并，热更新检查增加 2 分钟缓存。缓存上线后连续 10 次带不同查询参数、`Cache-Control: no-cache` 的 `/hotupdate/announce` 请求全部 200；公告表 `seq_scan` 保持 5091、`idx_announcements_status_id.idx_scan` 保持 3，证明请求未逐次进入 PostgreSQL。
- TV/移动客户端改为仅前台每 5 分钟检查公告；`onPause` 后不联网。会员静默复验在最近成功后的 6 小时内直接使用本地验签票据；显式激活与封禁返回仍即时处理。
- 热更新 `applied` 只在版本变化时写入，消除每次冷启动重复日志。线上创建并验证 5 个数据库索引：`idx_announcements_status_id`、`idx_activate_log_at`、`idx_licenses_updated_at`、`idx_licenses_created_at`、`idx_hotupdate_pkg_status_version`。
- 云端：`hotupdate` 部署完成，原超时与必要环境配置保持；`otv-web` v128 部署完成，`/web/api/health` 200，线上 app.js 含 6 小时节流与 Hero 前缀修复。
- 自动化与模拟器：后端/云端契约 13/13；移动端 build/lint/connected task 成功；TV build/lint 成功、MuMu 仪器测试 13/13；移动端前台→HOME 记录 `ON_RESUME → ON_PAUSE`，TV 真机方向键焦点日志连续且无 FATAL/ANR。

### 九、2026-09-09 好好看腾讯云出口容灾

- 线上原始复现：`/hhkan/home`、`/channel/1`、`/latest` 返回“全部镜像域不可用”，`/channel-sections/1` 三栏为空；本机两域均返回 850 挑战且本地解析器可正常求解，确认是数据中心出口风控而非页面解析错误。
- 修复真实镜像切换；新增由好好看页面生成的只读快照（5,669,450B）：首页 9 板块/81 卡、5 轮播，5 分类三栏目均非空，626 张去重卡片、259 份完整详情。macCMS 未进入快照生成链路。
- 强制断源集成测试覆盖 home/channel/channel-sections/latest/show/filters/detail/search；8 个连续接口仅第一个调用实时源，其余由 5 分钟熔断器直接快照返回。Python 后端契约 18/18。
- 腾讯云公网：health 415ms；home 214ms/9 板块；channel 166ms/48 卡；三栏目 174ms/16,16,16；latest 145ms/24 卡；show 162ms/18 卡；detail 168ms/15 原生线；search 214ms/1 命中。目录响应均为 `hhkan-snapshot`，无空数据。
- 播放：快照详情 `海洋奇缘启航` 经补充池获得 `bfzy·bfzym3u8`，`/hhkan/play/256705/100/0` 1.392s 返回 1 条 HTTPS M3U8；好好看卡片来源约束未改变。
- 海报：腾讯云代理同样被资源 CDN 拒绝，网页 v129 改为浏览器直连 HTTPS 图片；本地 Range 请求返回 206、`image/webp`、2048B。HTTP 图片仍经后端中继避免 mixed content。
- MuMu：TV/移动 debug 构建与 lint 均成功；TV Android 15 仪器测试 13/13，移动 connected 任务成功。TV 实键进入影视页后 Hero 元数据、海报墙与焦点正常；移动端实际内置后端 `/hhkan/home` 返回实时好好看 9 板块/5 轮播；两端日志无 FATAL/ANR。

## 2026-09-09 · TV 焦点回归收尾：标准 TV 规格 AVD（13/13）与 Codex 对话归档

### 一、背景与遗留项

- Codex「项目优化」对话末尾未完成项：TV 焦点用例在手机规格 AVD（`MyTV_Mobile_API30`，窗口宽约 411dp）上 12/13 通过，唯一失败用例 `滚动找回期间新焦点接管_旧协程不得抢回光标` 中 `escape` 卡片位于 x=600dp，被手机窗口裁边导致 bounds 失真。
- 判定为测试环境与目标设备不匹配，而非导航产品缺陷；针对该现象试改的 Focus.kt「fallback bounds」方案已撤回，生产导航代码与 MuMu 上 13/13 通过的版本一致（本轮 JVM 单测复验佐证）。
- 正确做法：改用标准 Android TV 规格的 AVD 重测（对话原结论），并按用户要求改用 android-emulator 插件流程、不再使用 MuMu。

### 二、环境搭建（零下载）

- 插件 SDK 根（`AppData\Local\Android\Sdk`）缺 emulator 与 system-images；用目录接合（junction）复用项目 SDK（`C:\Users\63054\Android`）已装组件：emulator 37.1.11（1.1GB）与 android-30 镜像，未发生任何网络下载。此前两次直连下载 android-tv 镜像均失败（33% ZipFile unknown archive），按用户指示放弃下载路线。
- `android_preflight` 全绿：adb 37.0.1、emulator 37.1.11、WHPX 加速可用；junction 后原 `MyTV_Mobile_API30` 恢复可加载。
- 新建 `MyTV_TV_1080p`：`tv_1080p` 设备配置 + `system-images;android-30;default;x86_64`。实测 `wm size` = 1920×1080、`wm density` = 320（即 960dp 宽，600dp 逃逸节点完整可见）、Android 11（=minSdk 30）。

### 三、自动化回归

- JVM：`:app:testDebugUnitTest` 通过（exit 0，含 Chaquopy 既有 pyc 告警非失败）。
- 仪器：`ANDROID_SERIAL=emulator-5554 :app:connectedDebugAndroidTest` — **13/13 通过，0 失败 0 跳过，耗时 17.765s**；报告：`D:/zcode-build/optimal-tv/app/outputs/androidTest-results/connected/debug/TEST-MyTV_TV_1080p(AVD) - 11-_app-.xml`。
- 此前失败用例在 960dp TV 视口下通过，证实「手机窗口裁边」假设；Focus.kt 无需任何生产改动。

### 四、黑盒冒烟（同一 TV AVD，插件 + adb）

- 安装 debug APK（com.qiubo.optimaltv.public，174MB）并显式启动 MainActivity（leanback 应用需 `am start -n`，monkey LAUNCHER 类别不可见属预期）。
- 冷启动 TotalTime 1938ms；主页四标签 + 搜索渲染正常；`live-tab: initial focus OK`（焦点落「足球」tab，bounds 673-783 与 UI 树一致）。
- 方向键导航（插件 keyevent 无 DPAD，改用 `adb shell input keyevent`）：DPAD_RIGHT 焦点「足球」→「影视」（`focus GAINED#2 @Rect(789,54,899,110)`），随即 LOST#2→GAINED#7 同位切换并 `vod-tab: initial focus OK`，即右键进入影视页自动选中的预期行为。
- 影视页 Hero 真实元数据（年份/地区/类型/简介）与「最近热门/最新上线/最近更新」三栏目海报墙完整渲染；截图 `shots/20260909-TV规格AVD焦点回归/tv-smoke-vod-focused.png`。
- logcat 全程无 FATAL、无 ANR。

### 五、结论

- TV 焦点测试体系（JVM 单测 + 13 项仪器用例 + 黑盒实键冒烟）在标准 Android TV 规格环境全量通过；移动端 16 项组件自动化此前已在手机规格 AVD 通过，三端「全部通过模拟器测试」要求闭合。
- 后续 TV 端模拟器测试基线：`MyTV_TV_1080p`（标准模拟器 + android-emulator 插件），MuMu 流程退役（Skill 已按用户要求删除）。

## 2026-09-11 · 移动版电视页三键开关（MuMu 实测）

- 构建：`internal/mobile` assembleDebug 通过（构建目录重定向 `D:/zcode-build/optimal-tv-mobile`）。
- 环境坑（历史踩坑复现并升级）：TV 版包（com.qiubo.optimaltv.public）与移动版抢前台，
  force-stop 后 MuMu 商店 re-sync 又把 TV 版拉起，tap 全部落到 TV 版上造成验证串台
  （logcat 里 TV 版 route → vod/tv/favorites/search 皆为我方 tap 误触）。解法：测试机直接
  `adb uninstall com.qiubo.optimaltv.public`（apk/TV版 可随时重装），环境即干净。
- 授权：`license_gen.py gen --plan weekly --count 1`（tools/out/20260911-weekly-1）→
  `db_import.py` 入库（HTTP 200 → 1）→ debug 包 `--es otvDebugLicense` 注入激活（完整云端
  验证链路，logcat: `license activated: plan=weekly` / `debug license activate → Ok`）。
- 实测通过（1920×1080 横屏）：
  1. 浏览态点 CCTV-1 → 播放态三键齐显：左上返回 ‹ / 右上投屏胶囊 / 左缘选台把手（tv_v_02）
  2. 点画面一下 → 三键齐隐，纯画面沉浸（tv_v_03）
  3. 再点一下 → 三键齐显（tv_v_04）
  4. 点左上返回 → 回电视浏览态（分组 chips + 频道网格）（tv_v_05）
  5. 重进播放后按硬件 BACK → 同样回浏览态（原语义未破坏）（tv_v_06）
  6. 点选台把手 → 两级侧边栏（分组列 + 频道列，当前台「正在播放」高亮）；选 CCTV-5 体育
     换台成功且三键随换台自动亮出（tv_controls_20/21）
- 未覆盖：竖屏旋转（MuMu 窗口固定横屏，`user_rotation` 在 fullUser 下不生效）；竖屏点按切换
  返回/投屏两键待真机抽验（逻辑与横屏共用同一开关）。
- 截图：`shots/tv_v_01..06_*.png`、`shots/tv_controls_*.png`。
# 2026-09-13 · 快闪需求自动化记录

- 基线：Python 50/50；TV `testDebugUnitTest` 成功；移动端 `testDebugUnitTest` 成功（无独立测试源）。
- 新增：后端缓存状态投影 2 项；TV 今日分页/重要球队/选集边界/信号源显示/10 秒 seek 共 6 项策略测试。
- `node --check tools/backend-src/www/app.js` 通过；TV/移动内置 zip SHA-256 一致；`tools/sync_shared.py` 无共享漂移（TV 按设计不含移动端 DLNA）。

## 2026-09-13 · 卡密授权协议 v2 发布验证

- TDD 契约：数据库/重置 7 项、激活函数 4 项、Web 3 项、后台 3 项及既有回归合计
  `python -m unittest discover` **75/75 通过**；`node --check`、后台内联 JavaScript 语法、Python 编译通过。
- Android：`LicensePolicyTest` 覆盖完整 SHA-256、包名隔离、24 小时精确边界和到期边界；TV 全量 JVM
  单测通过。TV 与移动版 `compileDebugKotlin`、`assembleRelease` 均成功，共享代码检查零漂移。
- 数据库：迁移执行成功；事务回滚冒烟覆盖旧协议 426、首/第二设备共享时间、第三设备 402、绑定设备
  verify、续费延长主卡与续费卡消费标记，全程 rollback，不污染正式库存。
- 重置：本地快照 `out/license-v2-reset/20260913-140511/snapshot.json` 记录旧卡 266、旧日志 278；清空后
  按 weekly 8 / monthly 106 / quarterly 51 / yearly 50 / lifetime 51 等量重建。复核总数 266、全新状态
  266、激活日志 0、与旧卡重号 0。
- 云端：旧协议请求返回 426，新协议非法设备摘要返回 400；`/web/api/health` 200，线上 app.js 已包含
  protocol 2 与 24 小时宽限。activate=Nodejs18.15/10s、otv-admin=Python3.9/60s、
  otv-web=Python3.11/60s，均为 Deployment completed。
- 复核修正：Web 设备摘要固定为 `SHA256(seed|web|origin)`；TV/移动 `onResume` 均触发带 6 小时
  节流的 `requestReverify()`。线上 app.js 已核对包含 `|web|`，`/web/api/health` 返回正常。
- `build_backend_zip.py` 固定所有 ZIP 条目时间；连续两次重建 SHA-256 完全一致，消除内容相同但包哈希
  漂移的问题。
- 热更 v16 已发布：40,304,539B，SHA-256
  `8f8cccf2ce0f037af692fb8ab6e50524718e0b093962f22831994974c2d90d0b`；TV/移动内置 zip 同哈希，
  `/hotupdate/check` 已返回 v16。
- v1.25 APK：TV 164,649,988B，SHA-256
  `b6732e0a52741d45040d2d60b772ea2fdd6b7218d32ee7867ae3e777de33c93d`；移动版 164,055,470B，
  SHA-256 `f86d1305a11c22f902235e608c3ee39e0a9e29a233fa5d981dcac8e05c609e7d`。
- 截图：`out/qa-v1.25/tv-final-v16.png`、`tv-my.png`、`tv-activate.png`、
  `mobile-final-v16.png`、`mobile-my.png`；后台卡密页截图确认 266 张新库存及首次激活/统一到期/
  续费去向列，网页版截图确认卡密输入入口。两端 crash buffer 均为空。
